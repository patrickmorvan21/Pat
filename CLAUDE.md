# ALDENHAR — Contexte projet (pour Claude Code)

Jeu narratif mobile dark fantasy. Philosophie centrale : l'IA **arbitre**, elle n'obéit pas — le joueur est contraint, jamais tout-puissant.

Ce fichier condense les décisions déjà verrouillées côté design (prises avec Claude Chat). Il ne remplace pas une lecture du code existant, mais évite de redemander à chaque session ce qui est déjà tranché. À tenir à jour manuellement après chaque décision structurante.

---

## Les 3 piliers non négociables

1. **Permadeath** — une vie par run. La mort ne vient QUE d'un choix/jet raté dans la fiction. Fermer l'app, recevoir un appel, perdre la connexion : **jamais** une cause de mort. La run se sauvegarde localement et reprend exactement où elle s'est arrêtée.
2. **Contrainte du joueur** — via stats de personnalité + dés. Pas de contrôle narratif libre, pas de texte libre.
3. **La mort comme progression** — système de reliques (à détailler séparément quand on y arrive).

---

## Mécaniques verrouillées

- **Stats** : Courage, Ruse, Instinct, Empathie. Fixées via un prologue narratif de 2-4 choix forcés + un peu de hasard en tout début de run. Pas d'écran de répartition de points. Jamais de montée permanente en cours de run.
- **Progression en cours de run** : pas de stats qui montent — des **états narratifs temporaires** (positifs/négatifs, ex. *Aguerri*, *Entaillé*) qui modifient les jets pour un temps, déclenchés par des événements.
- **Choix** : 3 options par défaut par scène (un 4e slot se débloque via relique/objet). Deux types :
  - **Verrouillé** : seuil dur de stat, grisé si indisponible.
  - **Risqué** : dé + stat résolvent la réussite en coulisse. Le texte du bouton est identique pour tous les héros — **aucun chiffre affiché**.
  - Pas de texte libre, jamais.
- **Vie** : pas de barre, pas de chiffre. Le tramage pixel de l'interface s'érode visuellement quand la vie baisse (pixels qui rongent les boutons, instabilité croissante de l'écran).
- **Combat** : pas de système séparé, pas de PV monstre, pas de tour par tour. Une séquence de 1 à 3 scènes de choix+dé comme le reste du jeu. La difficulté s'exprime via le seuil des jets, jamais via une jauge à vider.
- **Temps** : un seul repère visible, "Jour X". Avance uniquement aux points de campement (jamais en temps réel, jamais à chaque scène). Le camp sert aussi de sauvegarde et atténue les blessures légères.
- **Menu** : un seul losange, toujours à la même position sur tous les écrans. Ouvre un écran plein cadre (jamais une popup) : Options, Reliques, Besace, Miroir de l'âme (si mode détail stats actif).

## Le Geôlier (démon central)

- Archétype Jailer-Judge : transactionnel, amusé par les échecs du joueur, voit les héros comme un divertissement.
- Observe **tous** les héros de **tous** les joueurs simultanément — pas une relation 1:1.
- Commente rarement, un peu plus qu'un strict minimum : déclenché par des moments précis (échec/réussite critique, mort, jalon de relique) + occasionnellement au hasard, pour rester imprévisible. Toujours visuellement distinct de la narration (couleur d'accent, jamais la couleur de texte normale).
- Peut réciter des statistiques agrégées comme dialogue ("Douze mille avant toi ont poussé cette porte...") plutôt que les afficher froidement en UI.
- Le twist "Prisonnier" (le Geôlier comme masque) est **v2**, pas une priorité actuelle.

## Illustrations

- Pas de génération IA en direct par scène. Bibliothèque d'assets **pré-faits**, tramés en amont.
- L'image ne change que sur vrai changement d'environnement ou événement clé (monstre, objet, moment fort) — reste statique sinon.

---

## ⚠️ DIRECTION ARTISTIQUE — mise à jour importante

**La palette a changé. L'ancienne trichromie Nuit/Brique/Sable est obsolète comme accent visuel.**

| Rôle | Ancienne DA (obsolète) | **DA actuelle (à utiliser partout)** |
|---|---|---|
| Fond | `#211e33` (nuit) | **`#1c1a16`** (charbon, quasi-noir) |
| Accent (stats, Geôlier, tramage) | `#d8a25f` (sable) | **`#e0632a`** (orange) |
| Danger | `#ac2e26` (brique) | fusionné avec l'orange d'accent, à ne plus dupliquer |

**Si tu vois `#d8a25f`, `#ac2e26`, ou `#211e33` n'importe où dans le code (variables CSS, valeurs codées en dur, canvas 2D pour les textures du dé, matériaux Three.js) : ce sont des restes de l'ancienne DA. Remplace-les systématiquement.**

Pièges fréquents constatés (Claude Code retombe dessus) :
- Une variable CSS renommée mais une valeur laissée en dur ailleurs (ex : texture canvas du dé qui référence encore `#d8a25f` au lieu de `#e0632a`).
- Copier-coller d'un composant plus ancien qui traînait encore l'ancienne palette.
- **Vérifie par grep** (`grep -rn "d8a25f\|ac2e26\|211e33" .`) avant de considérer une passe DA terminée.

Specs du générateur de dithering (pour tout ce qui est tramé, illustrations comme UI) :
- Palette : Charbon `#1c1a16` (fond) / Orange `#e0632a` (trait)
- Style : Floyd-Steinberg (organique) — pas de damier Bayer pour les illustrations photo
- Taille de pixel : 1
- Seuil clair/sombre : 182
- Contraste : 151%
- Pas d'inversion

Typographie inchangée : `Jacquard 12` (titres, verdicts) + `VT323` (tout le reste).

---

## Le dé 3D — ce qui marche déjà, ne pas réinventer

Un prototype fonctionnel existe (testé et validé) avec Three.js. Points techniques à reprendre tels quels :

**Geste tactile façon Pokémon Go** :
- Il faut toucher le dé lui-même (rayon de tolérance ~60px autour de son centre), pas une zone autour.
- Vélocité du lancer calculée sur les mouvements des **~110 dernières ms** avant relâchement (pas toute la durée du drag) — sinon un geste qui ralentit avant de lâcher donne un lancer mou alors que le joueur a "senti" un lancer franc.
- Si relâché sans élan suffisant (vélocité sous un seuil bas), le dé **reste posé**, pas de lancer accidentel.

**Rebonds contre les bords — c'est ce que Patrick a préféré dans ma version, à repartir de là** :
```js
function onBounce() {
  if (navigator.vibrate) navigator.vibrate(12); // haptique proportionnel à l'impact, pas fixe si possible
  angVel.x = -vel.y * 0.01 + (Math.random()-0.5)*0.02; // la rotation change de sens en fonction du rebond
  angVel.y = vel.x * 0.01 + (Math.random()-0.5)*0.02;  // + un petit bruit aléatoire pour éviter un rebond mécanique
}
```
Le point clé du "choc" qui a plu : ce n'est **pas** juste une inversion de vélocité (`vel.x *= -BOUNCE`) — c'est le fait que la **rotation angulaire change aussi** au moment de l'impact, couplée à la vitesse linéaire du rebond. Un rebond qui inverse juste la position sans perturber la rotation a l'air raide et mécanique. Ajouter aussi, si pas déjà fait :
- Un léger squash/stretch de l'échelle du mesh au moment de l'impact (ex. scale 0.85 sur l'axe de l'impact puis retour élastique sur 2-3 frames) pour vendre le choc visuellement, pas seulement physiquement.
- La restitution (`BOUNCE`, ~0.72) doit légèrement varier avec la violence du choc plutôt qu'être une constante pure, sinon les rebonds successifs ont l'air identiques.

**Rendu** : Three.js en vraie 3D mais rendu sur un canvas **basse résolution puis upscalé** (`image-rendering: pixelated`, facteur ~5) pour rester dans l'esthétique pixel-art malgré la vraie géométrie 3D. Ne pas essayer de pixelliser via un shader — le sous-échantillonnage + upscale CSS suffit et coûte beaucoup moins cher.

**Cycle de vie** : idle (flotte doucement au repos) → held (suit le doigt) → flying (physique de rebonds) → settling (ease vers le centre, la face gagnante s'aligne lentement — c'est le moment de tension) → revealed (verdict, puis retour à idle).

**Armement** : le dé n'est saisissable QUE quand un choix risqué vient d'être tapé (état "armé"). Le hint sous le dé passe de vide à `"Lance le dé — [Stat]"` en couleur d'accent (orange, pas sable) à ce moment-là.

---

## ⚠️ CTA / choix — bug de variété constaté

**Problème observé** : les 3 slots de choix ont toujours le même rôle scène après scène — le 3e est systématiquement verrouillé, celui du milieu est systématiquement neutre (sans dé). C'est un bug de logique de génération, pas un choix de design : **la position à l'écran ne doit jamais prédire le type de choix.**

Corrige en randomisant l'assignation **avant** de peupler les slots visuels :
```js
function assignerTypesAuxSlots(choix) {
  // 1. Mélanger l'ordre des choix eux-mêmes (Fisher-Yates), pas juste leur contenu
  for (let i = choix.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [choix[i], choix[j]] = [choix[j], choix[i]];
  }
  return choix;
}
```
Et surtout : la génération de la scène elle-même doit tirer au hasard **combien** de choix sont verrouillés (0, 1, jamais plus de 1 par scène pour rester jouable) et **combien** sont neutres (rare — la plupart des scènes n'ont aucun choix neutre, un choix neutre systématique appauvrit la tension). Le risqué doit être le cas par défaut le plus fréquent, pas une variante parmi d'autres à fréquence égale.

Vérifie après génération de plusieurs scènes de suite (log ou test manuel sur ~15 scènes) qu'aucun pattern de position ne se répète.

---

## Illustrations — sourcing depuis Google Drive

Le dossier Drive **"2. Sauvegardé"** contient les illustrations validées et prêtes à l'emploi (déjà tramées Charbon/Orange). Utilise-les pour varier l'image en haut de chaque scène, adaptée au contexte plutôt qu'une image fixe.

**⚠️ État actuel du dossier** : la plupart des fichiers sont nommés génériquement (`dithering-aldenhar (23).png`, etc.) sans tag de contexte — seul un fichier suit la convention propre (`monstre_geryon.png`). Deux options :
1. **Renommer les fichiers existants** dans Drive avec le préfixe de contexte avant de les intégrer (convention ci-dessous).
2. **Demander à Patrick** de valider/renommer au fur et à mesure des prochains lots (déjà en cours côté Claude Chat).

**Convention de nommage à adopter pour tout nouvel asset** : `{categorie}_{sujet}.png`
- `scene_*` → environnements (corridor, chambre, cave, forêt, ruines, pont...)
- `monstre_*` → créatures/adversaires
- `objet_*` → objets clés, reliques, coffres

**Logique de sélection recommandée** : matcher le tag de catégorie de la scène en cours (ex. scène de combat → chercher un `monstre_*`, scène d'exploration → chercher un `scene_*` du bon lieu si possible, sinon un `scene_*` générique) plutôt que de piocher au hasard dans tout le dossier. Si aucun match exact, fallback sur la catégorie large plutôt que sur une image hors-sujet (jamais un `monstre_*` sur une scène de repos, par exemple).

---

## Outils & stack

- Prototypage : Claude Code Desktop, HTML/JS/Three.js
- Design : Figma (MCP connecté à Claude Chat)
- Docs : Notion (MCP connecté)
- Design system in-game : trichromie devenue duotone Charbon/Orange (voir plus haut) + Jacquard 12/VT323
- Design system marketing/landing (séparé, ne pas mélanger) : système "Dispatch" — Inter + Space Mono + Fraunces, charcoal/cream/orange
- Base44 explicitement écarté (vendor lock-in, pas de code custom)

---

## Notes de session (à compléter par Claude Code au fil de l'eau)

_Ajouter ici les décisions prises en session Claude Code qui ne remontent pas automatiquement à Claude Chat — ce fichier est le seul pont entre les deux environnements._

### Session 2026-07-11 (Claude Code)
- Passe DA duotone appliquée : bloc `theme-brief` et tokens sable/nuit/brique supprimés de `aldenhar/app/globals.css` (grep vérifié propre sur les sources). Flash de critique 20 = blanc (ink), critique 1 = orange accent.
- Rebonds du dé : rotation recouplée à la vitesse linéaire du rebond (`angVel = f(vel) + bruit`) conformément au snippet validé, en plus du squash/éclat/secousse déjà en place.
- Dé armé : hint « Lance le dé — [STAT] » en orange accent ; verdict « Touche pour continuer » en blanc.
- Bug de variété CTA corrigé : compositions par scène variées (risqué = défaut, 15 risqués / 5 verrouillés / 3 neutres sur 8 scènes, jamais >1 verrouillé) + Fisher-Yates seedé par le pas de progression. La scène 0 garde l'ordre exact de la frame Figma 124:2885.
- Écarts assumés avec ce fichier (Figma = source de vérité pour les écrans reproduits) : typo écran en Roboto Mono (le Figma actuel ne montre pas VT323 hors dé/verdicts stylés), menu = grille 3×3 (pas losange), PIXEL_FACTOR du dé = 2.4 (valeur du fichier de référence validé, pas ~5). À trancher côté design si besoin.
- Illustrations Drive : dossier « 2. Sauvegardé » lu (25 png). Transfert binaire via connecteur trop coûteux → champ `illustration` ajouté par scène dans `lib/scene-data.ts` (fallback portail). Reste à déposer les png renommés `{categorie}_{sujet}.png` dans `aldenhar/public/assets/` (commit direct ou upload en session).
- Spec Notion (« Spec fonctionnelle — pour Claude Code ») appliquée : dé flottant en permanence (armé au tap d'un choix risqué, §4) ; états narratifs temporaires AGUERRI +2 / ENTAILLÉ −2 sur 3 scènes, critiques sur le dé naturel, état affiché en mot dans le hint (§2) ; santé invisible avec érosion UI à 2 paliers + instabilité d'écran (§5) ; scène campement dans la rotation — dormir avance « Jour X », soigne +0.35 et purge les états négatifs (§7) ; brique #ac2e26 réintroduite comme --color-drama, réservée à FUNESTE et au flash du 1 naturel (§13 Notion, en contradiction avec la ligne « brique obsolète » plus haut — le Notion étant plus récent, il prime).
- Retouches Patrick (11/07 soir) : en-tête réduit à « ALDENHAR — J X » (numéral de chapitre supprimé, jour en opacité pleine) ; onde d'impact affinée à un filet 1-2px ; le dé n'apparaît qu'au clic d'un choix risqué (décision Patrick, remplace le « flotte en permanence » de la spec §4 Notion) ; PIXEL_FACTOR abaissé 2.4 → 1.6 pour un dé plus net.
- Mise à jour Notion 11/07 (§16, structurante) appliquée : le modèle « un écran plein par scène » est remplacé partout par un **flux scrollable unique** façon transcript (`components/Scene.tsx` réécrit) — historique complet jamais déchargé, choix ancrés en bas, action choisie affichée avant sa conséquence, texte tapé lettre par lettre (~15ms narration / ~22ms Geôlier) avec révélation séquentielle (un bloc de texte à la fois, pas tous en même temps), interruptible par tap. Le fil complet est persisté dans `localStorage` (`RunState.feed`) pour que la reprise de run restaure le scrollback exact sans réanimer l'historique déjà lu.
- Bug de course résolu en session : le tap qui dismiss l'écran de résultat du dé tombe presque toujours aussi dans la zone scrollable du dessous, incrémentant par erreur le compteur global « skip » avant même que la nouvelle entrée n'existe. Corrigé à deux niveaux : (1) le fil ne capte plus les taps tant que `rolling` est vrai (`pointer-events-none`), (2) chaque `TypedText` ignore les valeurs de `skip` antérieures à son propre montage (`baseSkipRef`). Deuxième bug découvert dans la foulée : les entrées narration/Geôlier encore en attente dans la file de révélation séquentielle s'affichaient déjà en entier au montage au lieu de rester invisibles — corrigé via un `revealedIds` distinct de `activeTypingId` (un bloc pas encore atteint dans la file ne rend rien).
- Santé (spec §5, précisée 11/07) : 4 paliers discrets implémentés (Intact / Marqué / Entaillé / Au seuil) avec seuils 0.75 / 0.5 / 0.25. Au palier critique, tous les pixels de l'écran vibrent brièvement toutes les 10s (`critical-vibrate`) en plus de l'érosion dense des CTA (densité ÷4 approx., bites jusqu'à 9px de jitter).
- Reste à faire, en attente d'éléments de Patrick :
  - **Mini-jeux tactiles** (frotter/tracer un glyphe/retenir son souffle/crochetage) : le fichier `prototypes_aldenhar.html` référencé dans le Notion n'a pas encore été fourni dans une session Claude Code — demandé à Patrick, à intégrer dès réception (ne pas réinventer les interactions déjà validées, même logique que pour le dé).
  - **Pipeline de dithering des illustrations** (Floyd-Steinberg, seuil 182, contraste 151% pivot 128, MAXDIM 1170) : specs verrouillées dans le Notion mais pas de nouvelles images sources à traiter pour l'instant — le champ `illustration` par scène est prêt à recevoir des assets `{categorie}_{sujet}.png`.
  - **Campement** : décision de design en attente côté Patrick (repos = soin complet vs. cicatrices persistantes vs. jet de nuit risqué) — comportement actuel (soin partiel +0.35, purge des états négatifs) laissé tel quel en attendant l'arbitrage.
  - Lore environnemental, PNJ/compagnons/trahison, éléments de surprise (choix qui expire, retour au lieu de mort, fausse mort) : design validé côté Notion mais pas encore de contenu écrit dans `lib/scene-data.ts` — prochaine étape de contenu, pas de blocage technique.
- Mini-jeux & Surprises (Notion « Mini-jeux & Surprises — catalogue pour Claude Code », 11/07/2026) : les 25 mini-jeux validés (4 références + 21 additionnels) implémentés via 12 moteurs canvas/pointer partagés (`components/minigames/engines/*` : RubReveal, GlyphTrace, HoldSteady, TimingTap, DialAlign, RhythmTap, PickTarget, SlowSwipe, SequenceExecute, StraightSwipe, SteadyCaress, SingleGesture) — plusieurs entrées du catalogue partagent explicitement le même moteur (ex. tracé miroir/fil d'Ariane/nœud du pendu = GlyphTrace, cf. spec "même moteur que Glyphe"), donc la mutualisation suit l'intention du design, pas un raccourci. Catalogue complet dans `lib/minigames-data.ts` (25 entrées) et `lib/surprises-data.ts` (11 surprises).
- Galerie de revue à `/minijeux` (2e route Next dans la même app) : chaque mini-jeu est rejouable avec un sélecteur de palier de stat (bas/moyen/haut) pour visualiser "la stat module la difficulté" ; les 11 surprises ont chacune une prévisualisation (érosion de choix, fausse mort, menace du jour, choix fantôme, pluie de cendres interactive, ou mock textuel pour les surprises purement narratives comme Le retour/Le marchand mort/etc.).
- Portée assumée pour cette passe (galerie de REVUE, pas intégration finale) : les mini-jeux/surprises ne sont pas encore câblés dans la rotation infinie de `lib/scene-data.ts` — c'est la prochaine étape une fois que Patrick a donné son feedback sur la galerie (comme pour le Crochetage, déjà élu "meilleur du lot" avant même la galerie). `prototypes_aldenhar.html` (référence originale des 4 mini-jeux fondateurs) toujours pas reçu dans une session Claude Code — les 4 références ont été implémentées depuis les descriptions textuelles détaillées du Notion (corrections du 11/07 incluses : progression séquentielle stricte du glyphe, suie orange/texte crème du frottage, fenêtre Ruse du crochetage) ; à réconcilier avec le fichier si Patrick le fournit.
- Bug trouvé en testant la galerie : Crochetage (mode "track", curseur oscillant) échouait au premier tap manqué (maxAttempts par défaut = 1, hérité du mode "point" à essais limités) alors que la nature oscillante du curseur appelle plusieurs essais naturels — corrigé avec `maxAttempts: 3` dans sa config catalogue.

### Session 2026-07-11 (suite, Claude Code) — retours Figma + demande de « plus de fond »
- Narration enrichie (`lib/scene-data.ts`) : `Scene.narration` passe de `string` à `string[]` (2-3 paragraphes courts par scène, toutes les 9 scènes réécrites) ; `Scene.tsx` pousse un `FeedEntry` narration par paragraphe au lieu d'un seul bloc, gardant la révélation séquentielle beat par beat.
- Fréquence du Geôlier fortement réduite (retour Patrick : « il arrive trop souvent ») : critiques (jet naturel 1 ou 20) le déclenchent toujours (nouveau pool `JAILER_TAUNTS_CRITSUCCESS` pour le 20 nat), sinon ~12% de chance par scène (contre ~34% avant) — plus du tout systématique sur échec normal. Bannière du Geôlier agrandie (min-height 128px, icône 116×136 qui déborde à gauche) pour matcher le redesign Figma (node 1909-794) ; les nouvelles citations de Patrick pour ce redesign n'ont pas encore été fournies texte par texte — pool de citations existant conservé en attendant.
- Illustrations d'ambiance : `advance()` insère occasionnellement (~25% de chance) une illustration même sans changement de contexte/lieu, en plus du changement systématique sur nouveau lieu — utilise la photo de Patrick (`assets/dithering-portal.jpg`) en placeholder comme demandé (Figma node 1909-454), pas encore remplacée par de vrais assets contextuels.
- CTA masqués pendant la lecture (Figma 1909-454) : au clic d'un choix, `choicesHidden` passe à `true` (fade + translateY 10px via `.choices-bar.choices-hidden` dans `globals.css`, transition en escalier `steps(4)` pour rester dans l'esthétique pixel), CTA remasqués tant que la file de révélation séquentielle n'est pas vide, réapparaissent (`choicesHidden=false`) uniquement quand toute la narration/Geôlier de la scène suivante a fini de s'afficher.
- Passe réalisme sur 5 mini-jeux (demande explicite : ne traiter que ces 5, attendre validation Patrick avant les 20 restants) :
  - **Frotter l'écran** (`RubReveal.tsx`) : tablette de pierre avec cadre rongé (`erodedRectPath`), grain de fond stable (pré-rendu une fois dans un canvas offscreen, plus de scintillement à chaque frame), fissures fixes, inscription révélée en `Jacquard 12` (plus VT323), vignette aux 4 coins, anneau de curseur pendant le frottage.
  - **Tracer un glyphe** (`GlyphTrace.tsx`) : disque-sigil avec halo tramé pulsé, cadre à 32 crans, runes décoratives éparses, points du tracé en croix gravée plutôt qu'un carré plein, ligne tracée qui pulse légèrement.
  - **Retenir son souffle** (`HoldSteady.tsx`) : silhouette de créature qui dérive lentement à travers l'écran pendant l'appui (remplace l'anneau de progression abstrait), jauge de souffle en demi-arc, la créature tourne la tête (« alarmée ») + léger flash tramé plein écran aux moments de graze.
  - **Crochetage** (`TimingTap.tsx`, mode "track" uniquement) : mécanisme de serrure à goupilles — pivot en bas façon trou de serrure, bras du crochet qui pivote en éventail, goupilles (tumblers) en trait qui s'illuminent près de la fenêtre cible, fenêtre de réussite en lueur tramée sur l'arc plutôt qu'un rectangle plein. Modes "release" et "point" (Piège à mâchoire / Sceau de cire, partagent le fichier) volontairement laissés simples — hors scope des 5.
  - **Le cadran runique** (`DialAlign.tsx`) : plaque de montage gravée, lunette fixe à 24 crans, runes = glyphes anguleux à 3 traits (pas de simples carrés), poignée de préhension qui tourne avec le geste (ligne + moyeu + poignée qui s'allume en orange pendant qu'on tourne).
  - Deux helpers ajoutés à `lib/dither.ts` pour supporter ces rendus : `bayerFillClipped` (trame Bayer confinée à un chemin/clip quelconque — cercle, anneau, arc) et `noiseSpecks` (bruit de texture stable, tiré une fois puis mis en cache, jamais régénéré par frame).
  - Vérifié en Playwright headless (drag simulé sur chacun des 5 canvases, capture d'écran) : aucune erreur JS, les 5 rendus s'affichent et réagissent à l'interaction sans régression sur la logique de score/échec sous-jacente.
- **En attente de Patrick avant de continuer** : validation des 5 mini-jeux repensés avant de décliner le même traitement « réalisme » aux 20 autres du catalogue ; citations texte du Geôlier redessiné (Figma 1909-794) ; assets d'illustration contextuels pour remplacer le placeholder photo dans le flux de scènes.

### Session 2026-07-11 (suite #3, Claude Code) — retours sur le dernier déploiement + premier combat
- CTA : réapparition passée d'un pop par paliers (`steps(4)`) à un vrai fondu (`ease-out`, 0.4s) suite au retour « je veux que ça apparaisse en fade » ; la disparition reste rapide et discrète (`ease-in`, 0.15s). `.choices-bar` dans `globals.css`.
- Bug corrigé sur le bandeau du Geôlier : le portrait (`position: absolute`, sans z-index) passait visuellement au-dessus du texte (`<p>` statique) et rognait le début des lignes qui chevauchaient sa zone — z-index explicite ajouté (`.jailer-portrait z-0` / texte `relative z-[1]`) pour garantir le texte toujours au-dessus, comme demandé.
- Dégradé de pixels manquant sur le portrait ajouté : nouveau helper `ditherFadeMaskDataUrl()` dans `lib/dither.ts` (masque alpha tramé Bayer, généré une fois côté client via canvas, mis en cache au niveau module) appliqué en `mask-image` CSS sur l'image — le bord droit/bas du portrait se dissout en pixels épars plutôt qu'un cadre net, conforme à la règle « pas de dégradé CSS lisse » (§11 Notion). Scintille (`mask-position` en `steps(2)`) tant que la citation est en cours de frappe (`typed=true`), se fige net dès qu'elle est terminée.
- Vérifié que l'action choisie (`kind: "chosen"`) était déjà à `opacity-50` dans le code existant (différenciation des descriptions) — rien à changer, déjà conforme à la demande.
- **Premières rencontres de combat implémentées** (spec §6, jusque-là seulement théorique dans le code) : deux rencontres ajoutées à la rotation de `lib/scene-data.ts` — une meute de limiers sur 2 scènes chaînées (`tunnel-embuscade` → `tunnel-affrontement`, insérées après le pont d'os) et Geryon en rencontre unique plus difficile (seuil 13-14, insérée après la cage). Nouveau champ `Scene.combat?: boolean` — purement un marqueur, aucune mécanique séparée (pas de PV, pas de tour par tour, mêmes choix+dé que le reste du jeu). À la résolution d'un jet non-critique sur une scène de combat, un état temporaire est appliqué : AGUERRI (+2, 3 scènes) en victoire, nouveau ÉBRANLÉ (-1, 2 scènes, ajouté à `NarrativeEffect` dans `lib/state.ts`) en échec — distinct des états liés aux jets critiques (qui continuent de s'appliquer indépendamment). Vérifié en Playwright : progression jusqu'à la meute, résolution du jet, effet AGUERRI bien affiché sur le hint du jet suivant (« Lance le dé — INSTINCT · AGUERRI »).
- Pas d'asset `monstre_*` disponible localement (dossier Drive jamais déposé dans `public/assets/`) — les scènes de combat retombent sur le placeholder portail comme toutes les autres scènes pour l'instant.

### Session 2026-07-12 (Claude Code, opus) — synchro Notion §17/§18/§19
Trois nouvelles sections structurantes ajoutées au Notion (sessions 2/3/4 de Claude Chat) implémentées d'un bloc. Décision d'architecture (recommandée explicitement au §19) : **centraliser tout ce qui survit à la mort du héros dans `lib/player-memory.ts`** — structure persistante attachée au COMPTE (localStorage `aldenhar-player`), distincte de `RunState` (`aldenhar-run`) et des reliques. Évite les flags épars dupliqués entre Registre / dette de sang / persistance environnementale / saisons.
- **Saisons du Geôlier (§17)** : `jailerPosture(mem)` renvoie 3 paliers discrets (Amusé / Intéressé / Respectueux) selon `runsStarted`, `bestDays`, moyenne de survie, reliques rares — jamais un score affiché. `JAILER_BY_POSTURE` fournit 3 pools de taunts critiques par posture ; `jailerTaunt(result, posture)` pioche dans le bon. Wiré dans `advance()`. `bestDays` mis à jour au campement, `runsStarted` au seed d'une run neuve.
- **Persistance environnementale (§17)** : `Choice.setsEnvFlag` pose un flag compte (ex. `porte-balafree-defoncee` sur « Pousser la porte balafrée »). Au seed d'une run neuve, si le flag est présent, un paragraphe de trace est ajouté à la narration d'ouverture (« le bois éclaté n'a pas repoussé »). Vérifié Playwright.
- **Prix différé (§17)** : `Choice.debt` (`{id, settleInSteps, text}`) pose une dette silencieuse sur un choix « gratuit » (ex. « Longer le mur, sans toucher »), stockée dans `RunState.debts`, réglée `settleInSteps` scènes plus loin dans `advance()` (insérée comme narration, rétrospectivement lisible). Vérifié Playwright (le règlement apparaît bien plus loin dans le fil).
- **La main qui hésite (§18)** : `RollRequest.highStakes` (posé via `Choice.risky.highStakes`, ex. charge sur Geryon, bond sous l'éboulement) → dans `Die3D`, la phase `settling` s'allonge (finishAt 1.7→2.6) et ajoute une micro-oscillation + secousse d'échelle décroissante avant l'arrêt. **Purement visuel, ne touche jamais `result`** (déjà tiré). 
- **La scène qui se résout sans toi (§18)** : `Scene.timed` (`{ms, timeoutNarration, timeoutChoices}`). Scène `eboulement` ajoutée. Compte à rebours VISUEL (`.timed-countdown`, jauge qui se vide en `steps()` + pulse, jamais de chiffre) armé seulement quand les choix sont réellement jouables. À expiration : `onTimedExpire` insère la narration d'inaction et **bascule vers `timeoutChoices`** (l'inaction ouvre de nouvelles options, pas un échec sec). Vérifié Playwright.
- **Le faux choix évident (§18)** : implémenté comme **contenu** (scène `porte-7e` : l'inscription invite explicitement à frapper — option « évidente » — dont l'échec est le plus retors), volontairement pas une mécanique détectable (sinon l'effet s'inverse, cf. Notion). Documenté en commentaire.
- **Le silence comme vraie option (§19)** : nouveau type `Choice.passive` (`{consequence}`) à côté de risqué/verrouillé/repos — résolution instantanée sans dé, avec une conséquence dédiée (ex. « Écouter, immobile » sur la scène d'ouverture, « Quitter la salle sans lire » au Registre). Vérifié Playwright.
- **Le Grand Registre (§19)** : `Scene.registre` + scène `grand-registre`. Nouveau `FeedEntry` `registre` rendu inline (`.registre` dans `globals.css`) — classement défilant des héros par jours de survie, la ligne du joueur (nom de run + jour courant) insérée et marquée en accent (`.is-player`). Données locales (`buildRegistre` : héros de fond figés + héros tombés du joueur + ligne en cours) **en attendant l'endpoint agrégé réel** (même besoin d'infra que « le fantôme de passage »). Nom de héros par run ajouté à `RunState.heroName` (+ `randomHeroName()`). Vérifié Playwright.
- **La dette de sang (§19)** : `Scene.foe` (identité stable de l'adversaire). Un 1 naturel en combat enregistre une `BloodDebt` au compte (stand-in du vrai flux de mort, pas encore construit) ; si ce même `foe` réapparaît et qu'une dette existe, une ligne de reconnaissance est insérée avant la scène. Fondation posée, reconnaissance câblée dans `advance()`.
- **Laissé en attente** : **Le Pacte à la marge (§19)** dépend du système de reliques/inventaire (pas encore construit côté proto, écrans en cours de design par Patrick) — la fondation `player-memory` (`relicsRare`) est prête à l'accueillir. Le vrai **flux de mort** (écran de fin, forge de relique, inscription au Registre, enregistrement de la dette de sang « propre ») reste à construire — actuellement la santé plafonne à 0.05 sans jamais tuer ; la dette de sang utilise le 1 naturel en combat comme déclencheur provisoire.
- Vérifié en Playwright headless (localStorage réinitialisé entre cas) : registre, scène chronométrée + bascule d'options, prix différé, choix passif, persistance environnementale — tous fonctionnels, aucune erreur JS. Lint + build propres. Déployé sur les deux routes.

### Session 2026-07-14 (Claude Code, fable) — journal Notion 13/07 + Figma 221:197 + correctifs Patrick
Le Notion a maintenant une section « 📋 Journal des mises à jour » append-only en bas de la spec — à relire en priorité à chaque synchro. Tout le lot du 13/07 est implémenté :
- **Résolution graduée à 5 paliers** (remplace le binaire partout) : `resolveTier()` dans `lib/scene-data.ts` — marge ≥5 = Éclatante, ≥2 = Réussite, ≥0 = De justesse (coût léger −0.06), >−5 = Échec (−0.12), sinon Critique (−0.2) ; nat 20 = Destin, nat 1 = Malédiction (−0.25). Prose : les paliers intermédiaires réutilisent les 4 textes écrits (le mot de verdict + le visuel portent la nuance). Animations par palier dans `Die3D` : halo TRAMÉ (data-URL Bayer radial, jamais un dégradé CSS) intense/franc/sobre, face TERNE sans surbrillance sur les échecs, vacillement orange/charbon « de justesse » (face + mot), flash brique→noir en critique, variante plus sombre en Malédiction, ralenti au settle du Destin. **Destin = récompense Besace rare/légendaire, jamais une Relique** (`randomRecompenseDestin`).
- **Écran de mort + permadeath réel** (`components/DeathScreen.tsx`) : santé plancher passée de 0.05 à 0 — à 0 sur palier d'échec, mort. Séquence : dé brisé (pictogramme 2D tramé, deux moitiés qui dérivent — la « vraie » fracture 3D est notée à affiner) → épitaphe Jacquard = LA PROSE EXACTE du jet fatal (suffixe ♦ retiré) → dissolution convergente 4 bords→centre (canvas) → « Jour X · N rencontres survécues » → CTA relique → cristallisation (70% commune / 25% rare / 5% légendaire, pools de noms dans `player-memory.ts`) → « Accepter et recommencer » (location.reload). `recordDeath()` s'exécute AU MOMENT de la mort (Registre, dette de sang réelle — l'ancien déclencheur provisoire nat-1 est retiré —, deaths/totalDays/bestDays, relique) et la run est réinitialisée immédiatement : fermer l'app pendant l'écran ne ressuscite jamais.
- **Besace** (`lib/besace.ts`, séparée des Reliques) : départ = dague simple fixe, 4 emplacements, tags de rareté en mot. Soin aléatoire en exploration (~22% des scènes hors combat/registre/campement, si place) → bandeau tramé « OBTENU — … » inline (`kind: "obtenu"`), jamais de popup. Vidée à la mort. Pas encore d'écran d'inventaire (maquettes Patrick en cours) — la consommation du soin est auto au campement en attendant.
- **Blessure persistante (précise §5)** : échec en combat = ENTAILLÉ `scenesLeft: 999` (ne se dissipe plus tout seul) ; le campement l'ATTÉNUE (−2 → −1) au lieu de le purger ; un soin de Besace le referme complètement (consommé au camp, avec ligne de narration). ÉBRANLÉ supprimé (remplacé par ce système).
- **Combat épique (amende §6)** : durée par poids narratif — Rôdeur = anecdotique (1 scène), meute = sérieux (2), **Geryon = épique en 3 scènes** (`geryon` montée avec choix « Jauger la bête » [Instinct, l'info est la récompense] → `geryon-2` échange, paliers en prose « chancelle »/« rugit, blessée » → `geryon-3` climax « s'effondre »). Un seul dé, jamais de PV.
- **Correctifs UI Patrick (13/07)** : (1) opacité 50% des actions choisies — le VRAI bug était `fadeup`/`fill both` qui figeait `opacity:1` par-dessus la classe ; corrigé via `--enter-opacity` dans le keyframe (appliqué aussi à la puce Jour). (2) Bande de dissolution en bas des illustrations : la PJ `bande_dissolution_haut.svg` n'est PAS arrivée dans la session (Gmail token expiré, rien sur disque) → régénérée procéduralement (1170×260, pixels épars, seed stable) sous le même nom dans `public/assets/` — À REMPLACER par l'original de Patrick si le rendu diffère ; appliquée en `scaleY(-1)`, 42px (cf. Figma 228:33149). (3) Bloc Geôlier redessiné (Figma 239:49164) : 87px, portrait 101×119 débordant haut/gauche, franges de pixels 9px (`frange_geolier.svg`, généré) sur bords haut/bas qui SCINTILLENT pendant la frappe de la citation seulement (`.jailer-speaking`), figées après. Le mp4 de référence du Geôlier qui parle n'a toujours pas été transmis — premier essai selon le principe, à affiner à réception.
- **Mise en page rencontre (Figma 221:197)** : bannière sans cadre (tag ✦ RENCONTRE ✦ + nom en Jacquard 27px orange, centrés) placée AVANT l'illustration ; en-tête « Aldenhar — [chapitre romain] » à gauche (chapterLabel) + menu à droite.
- Vérifié en Playwright : opacité chosen = 0.5 calculée, bande présente, bannière rencontre, DESTIN (verdict + halo + Obtenu), franges Geôlier, séquence de mort complète (épitaphe → chiffres → relique → nouvelle run à step 0/santé 1, mémoire deaths/fallen/relics incrémentée). Lint + build propres, déployé sur les deux routes.
- **Différé / en attente** : compagnon persistant via Destin social (contenu, dépend des rencontres sociales pas encore écrites) ; généralisation du système de rencontre au social (« Jauger »→« Lire ») — le modèle de données le permet déjà, contenu à écrire ; tutoriel diégétique du prologue (pas encore de prologue) ; fracture 3D réelle du dé à la mort ; mp4 du Geôlier ; SVG original de la bande de dissolution.

### Session 2026-07-14 (suite, Claude Code, fable) — retours de playtest Patrick (message + 3 PJ images)
- ⚠️ **Les 3 PJ (portrait démon HD, frange, bande de dissolution) ne sont PAS arrivées sur le disque de la session** (visibles dans le message, aucun octet déposé ; Gmail token expiré). Rapprochement procédural fait pour frange (grappes massives 3px, `frange_geolier.svg` v2, 12px) et bande (plateau dense 26%, courbe adoucie). **Pour brancher les originaux : les déposer tels quels dans `aldenhar/public/assets/` sous `frange_geolier.svg`, `bande_dissolution_haut.svg`, et le portrait sous `dithering-demon.jpg` (ou nouveau nom + maj `Scene.tsx`) — aucun code à changer si mêmes noms.**
- Timer de scène chronométrée : **minimum 6 secondes** (règle actée, éboulement 2200→6000ms, commentaire de garde dans le type `Scene.timed`).
- **État KO** (santé au palier critique) : bites des CTA ×~1.7 (95, jusqu'à 12px de jitter, 75% scintillants), nappe plein écran de pixels morts charbon + braises orange (`getDecayOverlay()`, data-URL en cache module, `.decay-overlay` opacity ~0.5 en flicker steps), textes de narration qui tremblent (`.erosion-3 .feed-narration`, keyframes `ko-tremble`). Lisibilité conservée.
- Rencontres : **illustration systématique** sur toute scène de combat (plus de tirage 25%) ; titre de rencontre en **Instrument Serif** (ajouté au link Google Fonts de `layout.tsx` — c'était la vraie typo du Figma, pas Jacquard).
- **Web/scroll** (« je dois sans cesse scroller ») : double cause corrigée — (1) le cadre 390×800 débordait des viewports < 800px → `max-h-[100dvh]` + ancrages `bottom:` pour `.die-hint`/`.die-verdict`/`.die-halo` (le dé se positionnait déjà depuis le bas) ; (2) pendant la frappe, le texte poussait sous la ligne de flottaison → intervalle de suivi du bas du fil (160ms, scroll `auto`) tant qu'un bloc tape.
- **Plus AUCUN rouge dans le jeu** (retour explicite, remplace le « brique réservée à FUNESTE » du 11/07) : `--color-drama` → `#ffffff` et `DRAMA` (lib/dither) → blanc. FUNESTE/MALÉDICTION en blanc, flashs critiques blanc→noir. La brique #ac2e26 est bannie partout, galerie mini-jeux comprise.
- **Butin du Destin cohérent** : `randomRecompenseDestin(allowArme)` — arme seulement si scène de combat ET jet de COURAGE (fuir un Rôdeur avec un 20 ne forge pas de lame) ; sinon babiole/soin.
- **Cohérence narrative** : règle éditoriale — le DERNIER acte d'une rencontre doit CLORE le combat dans chacune de ses issues (réécrits : les 12 outcomes de `tunnel-affrontement` — la meute détale/reflue/prend son tribut et disparaît —, les issues ouvertes du Rôdeur et de `geryon-3`). Ponts d'environnement ajoutés aux ouvertures (salle ronde→couloir du Rôdeur→escalier→éboulement→rivière→tunnels→camp ; arche morte→hall des échos→table). Lore créature-lieu : limiers = chiens des premières expéditions restés à attendre, l'arche = tanière couchée par Geryon, le Rôdeur = ancien héros nourri par les couloirs. Les scènes 1 d'une chaîne (embuscade, geryon 1-2) restent volontairement OUVERTES.
- Micro-fix : libellés de CTA longs tronqués en ellipse avant le tag de stat (`max-w-[68%]`).
- Vérifié en Playwright (viewport court 700px) : cadre 700px sans scroll de page, autoscroll actif pendant la frappe, drama=#fff et MALÉDICTION blanc calculé, Instrument Serif calculée sur le titre de rencontre, illustration présente après la bannière, decay-overlay + ko-tremble actifs au palier critique. Lint + build propres, déployé sur les deux routes.

### Session 2026-07-14 (suite #2, Claude Code, sonnet) — récupération des assets via Google Drive
Patrick a indiqué le chemin Drive (`Mon Drive/Professionnel/APP/Assets`, dossier créé le jour même). Trouvé et récupéré via `mcp__Google_Drive__*` (recherche `title contains 'Assets' and mimeType = folder`, puis `parentId = <id>`), 4 fichiers :
- **`Group 36706.svg`** (24 082 octets, texte, décodage base64 direct fiable) → intégré tel quel comme `frange_geolier.svg` (remplace la version procédurale du 11/07). Vrai export Figma du groupe "Group 36706" (bordure basse du bloc Geôlier, confirmé par les metadata du node 239:49483/49706) : rectangles charbon 2×2 en grappes irrégulières, motif répété en X.
- **`dithering-aldenhar (11) 1.png`** (140 464 octets, 1144×1346, RGBA) → décodage propre (checksum zlib valide), c'est le portrait HD du démon déjà tramé Charbon/Orange (ratio quasi identique au cadre 101×119 du bloc). **Faisait face à droite** — retourné horizontalement (`ImageOps`/PIL `FLIP_LEFT_RIGHT`) pour rester compatible avec le mirror CSS déjà en place (`-scale-x-100`), qui suppose une source faisant face à gauche pour que le rendu final regarde vers le texte. Sauvegardé en écrasant `dithering-demon.jpg`.
- **`bande_dissolution_haut 3.png`** (13 206 octets, 1560×168 annoncé) → **fichier corrompu à la source** : le flux zlib de l'IDAT échoue le contrôle Adler-32 (`incorrect data check`), et une inspection ligne par ligne confirme que seules les ~57 premières lignes sur 168 (34%) contiennent des pixels réels — tout le reste est vide. Vérifié sur 2 téléchargements indépendants (hash MD5 identique les deux fois) et la taille reçue correspond exactement à la taille annoncée par Drive (`fileSize: 13206`) : ce n'est pas un problème de transfert MCP, le fichier déposé sur Drive est lui-même tronqué (upload interrompu ou export bugué côté Patrick). **Version procédurale conservée** (`bande_dissolution_haut.svg`) — à remplacer si Patrick re-exporte un fichier complet.
- `Group 36706.png` (914 octets, miniature raster du même SVG) — ignoré, le SVG est la source de vérité.
- **Leçon technique retenue** : pour tout fichier binaire volumineux à intégrer depuis un outil MCP, préférer laisser le tool écrire son résultat sur disque (`/root/.claude/projects/.../tool-results/*.txt` quand la réponse dépasse la limite de tokens) puis décoder avec un script (`python3 -c "json.load(...); base64.b64decode(...)"`) plutôt que de recopier un très long base64 à travers le contexte — cette dernière méthode a fonctionné pour les deux premiers fichiers (hash/taille vérifiés) mais introduit un risque si le fichier est corrompu à la source, il faut alors vérifier explicitement (parsing des chunks PNG, test `zlib.decompress`) plutôt que faire confiance à la taille de fichier seule.
- Vérifié en Playwright : le portrait fait maintenant face au texte (bannière du Geôlier), les franges hautes/basses utilisent le vrai motif Figma. Lint + build propres, déployé sur les deux routes.

### Session 2026-07-14 (suite #3, Claude Code, fable) — lot Notion 14/07 : nouveaux écrans + correctifs
Journal Notion « 🗓️ 14 juillet 2026 » implémenté (écrans Figma 1933:525 / 1928:424 / 1925:524 / 1925:559 + boards d'assets 1938:563 « Etat » et 1938:3673 « Rencontre »).
- **Récupération d'assets — technique établie** : `curl` vers les URLs `figma.com/api/mcp/asset` échoue systématiquement à travers le proxy (exit 56, connection reset) et `download_assets` plafonne à 20 images. Chemin fiable : `get_screenshot` avec `enableBase64Response: true` par node, puis **extraire les octets PNG depuis le transcript de session** (`/root/.claude/projects/-home-user-Pat/<session>.jsonl` contient le base64 des images de tool results — chercher l'UUID de l'`image_url`, décoder, vérifier la signature PNG). 9 assets intégrés dans `public/assets/` : `accueil_demon.png` (1933:528), `monstre_rodeur.png` (1938:40478, l'errant encapuchonné à la lanterne), `monstre_limiers.png` (1938:34344, la bête sous l'arche), `monstre_geryon.png` (1938:89550, la gueule aux crocs multiples), `etat_aguerri.png` (1938:584, poing levé), `etat_entaille.png` (1938:578, torse transpercé), `objet_dague.png`/`objet_crane.png`/`objet_masque.png` (1925:529/528/535). ⚠️ Les variantes Figma/Drive sont ANONYMES (« VariantN ») — l'attribution monstre/état ↔ image est mon choix visuel, à faire valider par Patrick. Les 15 autres rencontres et 18 autres états restent dans Figma (et Drive `Photos/4_Etats`, mêmes noms VariantN) pour quand les scènes/états correspondants existeront.
- **Correctif scroll (14/07)** : l'intervalle de suivi 160ms pendant la frappe (posé la veille) est SUPPRIMÉ — il déplaçait l'écran à chaque retour à la ligne. Le scroll ne se fait plus QUE dans `onTypedDone` (paragraphe entier révélé), comme spécifié. Règle actée : jamais de scroll pendant l'écriture.
- **Bug TypedText corrigé au passage** (préexistant, vu en test) : quand un tap terminait un bloc via l'effet `skip`, le tick suivant de l'intervalle de frappe (encore vivant ~15ms avant cleanup) écrasait le texte complet par une tranche partielle → paragraphes figés à 2-3 mots (« Au », « Il fait un »). Double garde : l'intervalle s'arrête si `doneRef` est posé, et le rendu affiche toujours le texte entier hors frappe active (`typed ? slice : text`).
- **Écrans d'accueil** (`components/Home.tsx`, `app/page.tsx` rend `<Home/>`) : fond orange plein, marque **PACTUM** (mono gras espacé, charbon), titre Instrument Serif (« Bienvenue en enfer » 2 lignes / « Bon retour... »), démon `accueil_demon.png` ancré en bas, liseré charbon en pied. Sans sauvegarde : COMMENCER (plein charbon, texte orange) + OPTIONS ; avec sauvegarde (`hasSavedRun()` dans `lib/state.ts` : feed non vide ou step>0) : REPRENDRE (plein) + RECOMMENCER (filet) + RELIQUES / GRAND REGISTRE / OPTIONS. Les tags de stat « COURAGE » des maquettes = bug de composant Figma, ignorés comme demandé. OPTIONS inerte (écran pas designé). RELIQUES et GRAND REGISTRE ouvrent des pleins cadres simples (liste des reliques du compte / registre via `buildRegistre`). Écart assumé : liens de pied en crème (charbon illisible sur le poitrail sombre du démon).
- **Menu plein cadre** (`components/GameMenu.tsx`, ouvert par l'icône d'en-tête de `Scene`, croix de fermeture pixel partagée `CloseX`) : onglets STATS · INVENTAIRE · OPTIONS (Options inerte). **Inventaire** : détail de l'objet sélectionné (icône tramée agrandie au pixel 74→276, nom en Instrument Serif orange + tag de rareté, **flavor réel de l'objet** — le texte Geryon sous « Dague simple » dans la maquette est un mauvais mapping confirmé), 4 slots Équipements depuis `run.besace` (vides = « VIDE », sélection = bord blanc), rangée Reliques depuis la mémoire du compte. Icônes par `kind` : arme=dague, soin/babiole=crâne, relique=masque — un seul jeu d'icônes génériques en attendant des assets par objet. **Essence/STATS** : radar 4 axes (Instinct haut / Courage droite / Ruse bas / Empathie gauche), guides en losanges tiretés à 3 niveaux, **remplissage en nuage de pixels tramés** (échantillonnage par rejet seedé par les stats, densité qui décroît vers les bords — conforme à l'image de référence de Patrick, jamais un aplat) ; États avec image (négatif = orange telle quelle, positif = `saturate(0) brightness(1.6)` → blanc/crème) + nom + description (textes de la maquette) ; Compétences = texte seul, état vide (« Rien encore. Certaines rencontres enseignent »). NB : `RunState.stats` (`RunStats`, sur 10, profil aléatoire une dominante/une faiblesse) ajouté pour AFFICHAGE SEUL — les jets utilisent toujours seuil+états ; à brancher quand le prologue existera.
- **Illustrations de rencontre réelles** : champ `illustration` posé sur les 6 scènes de combat de `lib/scene-data.ts` (rodeur, 2× limiers, 3× geryon) — plus de placeholder portail sur les combats.
- **PAS touché, comme demandé** : le bloc Geôlier (redesign voulu mais aucune spec — chantier ouvert côté Patrick) ; l'écran Options (pas designé).
- Vérifié en Playwright (accueil 1er lancement → COMMENCER → jeu ; reload → « Bon retour... » → registre depuis l'accueil → REPRENDRE ; menu Essence radar ~2300 px orange + 2 états affichés + filtre désaturation calculé ; Inventaire dague + vrai flavor + 3 VIDE ; rencontre Rôdeur avec `monstre_rodeur.png`) : aucune erreur JS. Lint + build propres, déployé sur les deux routes.

### Session 2026-07-14 (suite #4, Claude Code, fable) — assets HD (PJ + Drive) + fidélité menus + bloc Geôlier 1925:614
- ⚠️ **Nouveauté : les PJ des messages SONT récupérables** — leurs octets sont dans le transcript de session (`.jsonl`), même technique que les screenshots Figma (chercher les entrées `type: "image"` du dernier message user, décoder le base64). Les 3 PJ intégrées : `pactum_logo.png` (1306×205, fond transparent — remplace le texte « PACTUM » de l'accueil, hauteur 19px comme le Figma), `accueil_demon.png` (1815×2000, webp→png quantisé 8 couleurs 950 Ko→139 Ko), `geolier_portrait.png` (1100×914, nouveau portrait du bandeau, face à DROITE, dissolution cuite dans l'image).
- **Qualité des images (retour « toutes les images sont de mauvaise qualité »)** : `get_screenshot` Figma ne rend JAMAIS au-dessus de la taille native du node (maxDimension ne fait que réduire) — pour du HD il faut les fichiers sources. États : Drive `4_Etats` porte les MÊMES noms de variantes que Figma (`Property 1=VariantN.png`, 750×750) → `etat_aguerri.png` (Variant7) et `etat_entaille.png` (Variant8) refaits en 800×800. Rencontres : Drive `1_Rencontres` = 19 originaux PORTRAIT (506×900) aux noms anonymes, les vignettes Figma en sont des recadrages → identification par **template matching** (fenêtre 390:352 glissante en niveaux de gris, MSE) : rodeur = `dithering-aldenhar (10).png` (crop y218), limiers = `(8).png` (y312), geryon = `(23).png` (y202) — recadrés à l'offset trouvé et exportés en **780×704 (×2)**, quantisés 16 couleurs. Téléchargement de masse Drive fait par un sous-agent (19 fichiers, les réponses inline auraient noyé le contexte principal).
- **Bloc Geôlier refait (Figma 1925:614 + PJ maquette)** : portrait HD ancré au bord GAUCHE (100×118, débord -15px en haut, `object-cover object-left`), plus AUCUN mirror CSS ni masque tramé (`getDemonMask`/`ditherFadeMaskDataUrl` retirés de `Scene.tsx` — la dissolution est dans l'image), texte mono gras charbon à 122px, interligne 1.6. Franges + scintillement pendant la frappe inchangés. `dithering-demon.jpg` n'est plus référencé.
- **Fidélité menus (retour « tu n'as pas respecté mon design »)** : `SectionHead` refait — filet 7px depuis le bord GAUCHE du device, libellé en casse de phrase (« Équipements », « États »...), puis **filet étiré jusqu'au bord DROIT** (les conteneurs d'onglets n'ont plus de padding horizontal, chaque contenu pose le sien). Onglets : actif = crème gras, inactifs = ORANGE (maquette). Inventaire : nom en Instrument Serif 32px, flavor mono 13px, cadres des slots rognés des PNG d'icônes (3px — ils créaient un double cadre et une barre grise sur le détail agrandi). Essence : radar recentré pleine largeur, géométrie maquette (R=101, guides 202/126/46, croix ±105, labels gras DANS le cadre à 14px des bords), **fill toujours en nuage de pixels tramés** (la référence collée de Patrick prime sur l'aplat vectoriel de la maquette — décision documentée en commentaire) ; vignettes d'état 66px avec CADRE (orange si négatif, gris si positif) ; Compétences avec filet vertical gauche (état vide en attendant le système).
- Vérifié en Playwright (accueil logo+démon HD, bandeau Geôlier injecté dans le feed, Essence avec 2 états, Inventaire) : aucune erreur JS. Lint + build propres, déployé sur les deux routes.

### Session 2026-07-15 (Claude Code, fable) — repasse écrans 15/07 + DA « pactum-style » + MCP Playwright
- ⚠️ **Skill `pactum-style` (Notion « SKILL pactum-style — source de vérité ») : pièces jointes NON récupérables** — `SKILL.md` et `references/dithering.md` ont été uploadés par l'intégration de Claude Chat ; l'API Notion refuse leur téléchargement à l'intégration de Claude Code (404 object_not_found) et la page est privée (403 en accès direct). **La procédure de synchro décrite sur la page ne marche donc pas depuis Claude Code.** Alternative à mettre en place côté Patrick : coller le contenu des deux fichiers en blocs de code dans la page (lisibles via notion-fetch), ou les déposer dans le Drive. En attendant, la ligne de résumé de la page a été appliquée : palette 3 couleurs Orange `#e0632a` / Charbon `#1c1a16` / **Blanc `#ffffff`** + opacités (déjà en place), typographie **Instrument Serif + Roboto Mono uniquement**, CTA à cadres décalés (le composant Figma « Group 36691 » = fond charbon, bordure blanche 1px, entailles de coins — déjà conforme).
- **Jacquard 12 et VT323 supprimés partout** : `--font-title` → Instrument Serif (registre-head, écrans de mort, relic-name, inspiration-text), `--font-die` → Roboto Mono, texture de face du dé (`Die3D` canvas) en Roboto Mono bold 36px, tous les moteurs de mini-jeux + previews de surprises passés en Roboto Mono/Instrument Serif, familles retirées du link Google Fonts. Le mot de verdict du dé reste volontairement en Roboto Mono uppercase (`--font-body`, design d'origine).
- **La page Figma 1903:324 ne contient plus que 7 nodes** : les 5 écrans App (1925:524 Inventaire, 1925:559 Essence, 1925:614 scène Geryon, 1928:424 Bon retour, 1933:525 Bienvenue) + les 2 boards d'assets passés en HD (composants **1000×1000** pour les 19 états, **1000×903** pour les 18 rencontres — mêmes node IDs qu'avant). Les 5 assets en jeu ré-exportés depuis ces composants : états 800×800, rencontres 780×704, nets cette fois.
- **Repasse 1925:614** : (1) l'en-tête n'a PLUS de titre — seule l'icône menu (32×32, 3×3 points, fond charbon 80%) flotte en haut à droite, le fil défile dessous (`chapterLabel` n'est plus rendu) ; (2) **l'illustration de Geryon dans la maquette est le démon assis au trône** (node 1938:3672 « Property 1=1 » du board Rencontre — le nom de calque « dithering-aldenhar (8) 1 » est un reliquat de fill remplacé, ne pas s'y fier) → `monstre_geryon.png` remplacé par ce visuel ; la gueule aux crocs (V19) reste dispo sur le board si Patrick préfère.
- **MCP Playwright** : installé (`claude mcp add playwright -- npx @playwright/mcp@latest --browser chromium --executable-path /opt/pw-browsers/chromium` — les args `--browser/--executable-path` sont NÉCESSAIRES : sans eux le serveur cherche Chrome système à `/opt/google/chrome/chrome` et échoue). ⚠️ L'instance d'une session en cours garde les anciens args : il faut redémarrer la session après modification. Testé : outils répondent, lancement navigateur OK après redémarrage attendu. Attention aussi au scope : `claude mcp add` enregistre pour le CWD courant (`/home/user/Pat` ≠ `/home/user/Pat/aldenhar`).
- Vérifié en playwright-core : plus de titre d'en-tête, menu 32px flottant, aucune erreur JS, verdict Roboto Mono. Lint + build propres, déployé sur les deux routes.

### Session 2026-07-15 (suite, Claude Code, fable) — skill pactum-style installé + application
- **Skill `pactum-style` synchronisé** : Patrick a contourné le blocage Notion en uploadant l'archive en PJ (`*.skill` = zip). Extraite byte à byte dans `.claude/skills/pactum-style/` (SKILL.md + references/dithering.md), committée au repo. Date de « Dernière mise à jour » lue sur la page : **15/07/2026**. Pour les prochaines synchros : les PJ des messages restent le canal fiable (le téléchargement des attachments Notion inter-intégrations est impossible).
- **Appliqué depuis le SKILL** : Geôlier ralenti à **42ms/caractère** (§5) ; curseur **▌ clignotant en steps(1)** pendant toute frappe (`.type-cursor`) ; `prefers-reduced-motion` → texte instantané (TypedText) ; `CREME` (lib/dither) passé de #e8dfc8 (obsolète §2) à #ffffff — touche tous les moteurs de mini-jeux qui écrivaient en crème ; **nommage PACTUM** dans tout le copy visible (title « PACTUM », galerie « PACTUM — Mini-jeux & Surprises », « les couloirs d'Aldenhar » → « les couloirs ») — les clés localStorage `aldenhar-*` restent inchangées (données joueurs existantes).
- **Conflits SKILL ↔ maquettes Figma (Figma gardé, à trancher par Patrick)** : corps de texte ~19-20px au SKILL vs 13-14px sur toutes les maquettes 390px ; CTA « bordure orange 2px, actif = inversion orange » au SKILL vs bordure blanche 1px + entailles de coins dans le composant Figma (Group 36691, vérifié dans le code du composant) ; Geôlier « texte blanc + tag ◉ le geôlier te regarde » au SKILL (§8 marqué « À confirmer ») vs bandeau orange à texte charbon sans tag de la maquette 1925:614 ; menu « losange 34px » au SKILL vs grille 3×3 32px de la maquette. Règle maintenue : **Figma = source de vérité pour les écrans reproduits**, le SKILL pour tout le reste.
- NB : `chapterLabel()` (scene-data) n'est plus rendu nulle part depuis la suppression du titre d'en-tête — cohérent avec le SKILL (« chip JOUR X, jamais Chapitre »).

### Session 2026-07-16 (Claude Code, fable) — nouvel accueil animé (héros Geôlier)
- **Nouvel écran d'accueil** (Figma 1963:370 « Première partie » / 1970:458 « Reprendre partie ») : héros = `geolier_detoure.png` (1560×1720, fond transparent, extrait du data-URI du prototype `accueil_geolier_v5_2.html` fourni en PJ) posé sur fond orange plein ; panneau charbon dessous avec PACTUM en Instrument Serif 46px (le logo png n'est plus utilisé sur l'accueil), tagline par écran (« Tu choisis. Le dé décide. / Lui, il regarde. » vs « Aucune partie ne se ressemble. / Aucune mort n'est gratuite. »), CTA à segments décalés, liens en pied sur charbon (plus par-dessus l'image).
- **Animation reprise du prototype validé, tel quel** (`components/Home.tsx`, `HeroGeolier`) : 3 couches (canvas fumée dessous / image `position:relative` z-2 devant / bande « sol » charbon 4px) ; respiration par PALIERS ENTIERS via `BREATH=[0,-1,-2,-3,-3,-3,-2,-1,0,0,0]` et `BSTEP=380ms`, jamais de transition CSS ; cendres = carrés de 1px stricts sur canvas à 1/3 d'échelle, montent du bas, se raréfient par probabilité de dessin décroissante (jamais d'alpha) et meurent à la ligne des 2/3 ; `seedWisps()` repris (peuple la colonne au chargement, sinon plusieurs secondes de vide) ; `prefers-reduced-motion` → image fixe, pas de boucle.
- **Adaptation au layout** (demande explicite : pas les tailles fixes du prototype) : héros fixé à 368px (frontière orange/charbon à ~46% comme la maquette), image plus haute que le héros → posée avec 40px d'air au-dessus des cornes, le bas du buste coupé par la frontière (overflow hidden). Le « sol » 4px est conservé même si le crop rend la ligne impossible.
- **CTA accueil = composant du prototype** : primaire orange plein texte charbon, secondaire contour orange, segments décalés haut/gauche/bas aux offsets exacts du prototype (top 3px décalé droite, gauche 3px, bas 5px décalé gauche — le bas plus épais), `:active` = flash blanc.
- Au passage : directives eslint de TypedText réordonnées (le disable servait à la branche reduced-motion, pas au reset de frappe).
- Vérifié en playwright-core : cendres présentes dès le chargement (seedWisps), respiration cyclique (-3px→0), les deux écrans (première/reprendre), aucune erreur JS. Lint + build propres, déployé sur les deux routes.
- **Correctif CTA accueil (retour Patrick 16/07)** : les segments décalés du prototype sont RETIRÉS de l'accueil — les maquettes 1963:370/1970:458 utilisent le composant classique (298×46, bordure orange, entailles de coins charbon 2px, texte 14px espacé 2.8px ; primaire = plein orange texte charbon, secondaire = contour + texte orange). Le « CTA à segments décalés » du SKILL reste pour les contextes sans maquette — sur un écran maquetté, la maquette prime, toujours. ⚠️ Piège récurrent sur ces boutons : ne JAMAIS mettre la bordure CSS sur le bouton lui-même — les entailles de coin absolues se positionnent dans la boîte de padding et se décalent de 1px (liseré orange qui dépasse dans l'angle, vu par Patrick au zoom). Structure correcte = fond+bordure en calques enfants inset-0, entailles 2×2 par-dessus au ras du coin (cf. ChoiceButton et HomeCta).

### Session 2026-07-16 (suite, Claude Code, fable) — Prologue « Le Seuil » (journal Notion 16/07)
Lot du 16/07 implémenté (hors accueil animé, déjà fait). Le prologue précède désormais toute nouvelle run : le Geôlier feuillette la vie d'avant du héros et rend un VERDICT — jamais un écran d'allocation.
- **Données** : `lib/prologue-data.ts` = EXPORT de la base Notion « 🎲 Prologue — Pool de souvenirs » (collection `593e12ca-ef11-4a14-9c85-954166925963`), 120 entrées (30 par stat), toutes actives au 16/07. La base Notion reste la source de vérité éditoriale — Patrick l'enrichit dans Notion, on REGÉNÈRE l'export en session (l'app est un export statique GitHub Pages : pas de lecture Notion au runtime, l'export au build est l'application du « lire via connecteur ou export »). Convention constante A=engagement direct(+3) / B=mesuré(+2) / C=retrait(+1).
- **Flux** (`components/Prologue.tsx`, câblé dans `Home` : COMMENCER/REPRENDRE/RECOMMENCER passent par le Seuil tant qu'il n'est pas rendu) : amorce 2 beats (voix du Geôlier, 42ms/car) → 4 souvenirs (ordre fixe Courage→Ruse→Instinct→Empathie, narration 15ms + 3 choix) → clôture → verdict → Jour I. AUCUN dé (ni visible ni lancé). Illustration unique = `HeroGeolier` (extrait dans `components/HeroGeolier.tsx`, paramétré `density`/`bstep`) — **progression dramatique** : cendres 2→3→4→5 à chaque choix validé, respiration 380→520ms au dernier souvenir. Boutons contour blanc + tag de stat orange (spec explicite — la maquette Figma ne montre pas les tags mais la spec du 16/07 les demande). Tap = finir la frappe puis avancer (beats sans choix).
- **Verdict** (`computeVerdict`) : base 1 + A/B/C, puis jet SILENCIEUX −1/0/+1 par stat, clamp **1..5** (nouvelle échelle des stats — le radar Essence normalise sur 5, `migrateStats()` ramène les anciennes sauvegardes sur 10 à /2). `RunState.prologue` persisté à chaque beat : fermer l'app en plein Seuil reprend au beat exact (`hasSavedRun` compte un prologue entamé) ; les anciennes runs sans prologue sont marquées `done` et reprennent directement.
- **Première apparition du dé = Jour I** : ligne du Geôlier « À partir de maintenant, il décide avec moi. » insérée dans le feed d'ouverture de `Scene` (seulement pour les runs qui ont traversé le Seuil).
- Vérifié en Playwright (parcours complet : amorce → 4 souvenirs → clôture → verdict stats {5,4,3,3} → Jour I avec ligne du Geôlier ; reprise en plein prologue via REPRENDRE au beat exact ; aucun dé présent ; aucune erreur JS). Lint + build propres, déployé sur les deux routes.
- NB : les stats restent AFFICHAGE SEUL (radar) — le branchement sur la résolution des jets n'est pas demandé par le journal du 16/07 ; les seuils + états continuent d'arbitrer. À trancher côté design si/quand les stats doivent moduler les jets.

### Session 2026-07-16 (suite #2, Claude Code, fable) — retours Patrick (prologue, CTA, bloc démon, menu)
- **Prologue sans tags de stat** (maquette 1997:578) : les libellés « COURAGE/RUSE/... » sont retirés des boutons du Seuil — la stat engagée reste invisible. La ligne de spec du 16/07 qui les demandait est ANNULÉE par ce retour ; le libellé peut occuper 90% de la largeur.
- **CTA après la description** : le bloc de choix porte `choices-hidden` tant que `activeTypingId` est non nul — il n'apparaît (fondu 0.4s existant) qu'une fois TOUT le texte de la scène écrit, y compris à l'ouverture d'une run (avant, seuls les choix post-sélection étaient masqués). Le compte à rebours des scènes chronométrées attend ce même signal.
- **Bloc démon refait** (maquette 1946:447 dans 1925:614) : NOUVEAU portrait gros-pixels fourni en PJ (1050×872, fond orange cuit) → `geolier_portrait.png`, posé pleine hauteur au bord gauche SANS débord (h-full w-auto, plus de top:-15px) ; franges **inversées horizontalement** (`.jailer-fringe-top` scaleX(-1), `.jailer-fringe-bottom` scale(-1,-1)) — le scintillement pendant la frappe est conservé.
- **Menu** : croix de fermeture = asset pixel art `croix_menu.png` (export du composant Figma « Group 15 », 32×32 — la PJ de Patrick n'était pas encore lisible dans le transcript au moment du fix, l'export Figma est identique ; remplacer par la PJ si le rendu diffère). Le `CloseX` n'est plus deux traits CSS. **Décalage burger/croix réglé** : la croix du menu ouvert est positionnée exactement comme le burger (top 11px / right 10px, 32×32) — vérifié dx=0/dy=0 en Playwright.
- Vérifié en Playwright : prologue sans tags, CTA masqués pendant la frappe puis révélés, nouveau bloc démon rendu, alignement burger/croix parfait, aucune erreur JS. Lint + build propres, déployé sur les deux routes.

### Session 2026-07-16 (suite #3, Claude Code, fable) — retours sur le dernier déploiement
- **Libellés de CTA jamais tronqués** (prologue ET jeu) : nouveau `components/FitLabel.tsx` — la POLICE descend par demi-points (14→9px plancher) jusqu'à ce que tout le libellé tienne, le bouton ne change jamais de taille, plus de « … ». Branché dans `ChoiceButton` et `PrologueChoice`.
- **Bloc de choix ABSENT pendant la frappe** (précision de Patrick : pas juste invisible — le fond sticky + filet cachaient la description) : `.choices-hidden` passe en `display:none`, le fil récupère toute la hauteur ; la réapparition rejoue le fondu 0.4s via un keyframe `choices-fadein` (une animation se rejoue au retour de display:none, contrairement à une transition). Disparition instantanée assumée.
- **Franges du bloc démon — le vrai sens** (2e passe) : l'asset `frange_geolier.svg` est la BORDURE BASSE du bloc (plateau dense au ras du bord bas, dents vers le haut). Donc bas = TEL QUEL, haut = `scaleY(-1)`. Le « inverser horizontalement » de la 1re passe (scaleX) était une mauvaise lecture — c'était vertical. Si un souci de sens revient sur cet asset : raisonner « plateau collé au bord, dents vers l'intérieur du bloc ».
- Vérifié en Playwright : aucun span de bouton en overflow (prologue + jeu), `.choices-bar` en display:none pendant la frappe puis `flex` après, transforms des franges (haut matrix scaleY(-1), bas none), rendu du bloc conforme à la maquette 1946:447. Lint + build propres, déployé sur les deux routes.
- **Retouches Patrick (16/07 soir)** : état actif/hover blanc des CTA d'accueil supprimé (le bouton reste identique au repos/survol/clic) ; franges du bloc démon passées de 12px à **9px de haut = la taille exacte de l'asset** — la bande plus haute laissait 3px d'orange entre la frange et le bord du bloc (bloc vérifié à 87px pile, écarts 0px).
- **Commande `/leo-import` créée (16/07 soir)** : `.claude/commands/leo-import.md` + `tools/dither_batch.py` (nouveau — le fichier n'existait pas dans le repo ni dans les PJ). Pipeline : entrées = images d'un dossier (défaut `~/Downloads`) ou URLs `cdn.leonardo.ai` (le script force `?w=1875`, jamais la miniature) → recadrage carré centré → 1000×1000 max (jamais d'upscale) → dithering canonique (portage fidèle des specs de `references/dithering.md` : luminance 0.299/0.587/0.114, contraste linéaire pivot 128 ×1.51 — PAS ImageEnhance —, Floyd-Steinberg seuil strict 182, buffer non clampé, PNG palette Charbon/Orange) → rangement par préfixe dans le Drive `Photos/01_En attente/` (`monstre_*`→1_Rencontres, `scene_*`→2_Environnement, `objet_*`→3_Objets, inconnu→`_a_trier`) avec vérification de chaque copie (taille + PNG relisible) et récapitulatif. Testé en session (routage, palette 2 couleurs exactes, resize 1600→1000, no-upscale 506→506). ⚠️ Conformément à `dithering.md`, ce nouveau portage doit encore être VALIDÉ visuellement côte à côte avec l'outil HTML canonique sur 2-3 vraies sources Leonardo — à faire par Patrick au premier import réel.
- **Diagnostic /leo-import (17/07)** : premier lancement en échec chez Patrick — cause racine trouvée via l'API Drive : le dossier `APP` a été renommé **`PACTUM`** le 17/07 (chaîne réelle vérifiée : `Mon Drive/Professionnel/PACTUM/Photos/01_En attente`, le dossier existe). Trois correctifs : chemin par défaut mis à jour ; **UA de navigateur** sur les téléchargements (le CDN Leonardo/CloudFront rejette les UA non-navigateur) ; **garde-fou destination** — si la racine de rangement n'existe pas (renommage futur), le script échoue bruyamment en conservant les sorties dans son dossier de travail, au lieu de créer une arborescence fantôme hors synchro Drive (le `mkdir(parents=True)` initial rangeait « OK » dans le vide). Leçon : re-vérifier le chemin Drive par l'API à chaque commande qui écrit dedans, les dossiers de Patrick bougent.

### Session 2026-07-18 (Claude Code, session distante) — lot Leonardo 18/07, grain ×3, envoi Drive
- Import du lot Leonardo (14 images : 6 monstres, 6 scènes, 2 objets) exécuté EN SESSION DISTANTE (pas sur le Mac de Patrick) : le script a rangé les sorties dans `drive_outbox/01_En attente/` du repo (le vrai Drive n'est pas monté ici), et les sources téléchargées survivent dans `/tmp/leo-import-*` de la session.
- **Grain de dithering ×3** (retour Patrick « c'est encore trop net », validé ensuite « beaucoup mieux ») : `dither_batch.py` dithère désormais sur une grille réduite `TARGET // pixel_size` (défaut `PIXEL_SIZE_DEFAULT = 3`, option `--pixel-size`) puis ré-agrandit en blocs pleins via NEAREST — même logique que le canvas basse résolution du dé. Seuil/contraste/FS inchangés. Documenté dans `references/dithering.md` (le pixel size 1 reste celui de l'outil HTML canonique).
- **Envoi vers le Drive depuis une session distante — leçons durement acquises** :
  - Le connecteur Drive MCP écrit bien (`create_file` + `base64Content`, `parentId` du sous-dossier, `disableConversionToGoogleType: true`, vérif par `fileSize` de la réponse), MAIS le base64 doit transiter par le contexte du modèle : fiable jusqu'à ~5 Ko de base64, au-delà la retranscription perd des blocs de façon DÉTERMINISTE (3 tentatives chevalier = même taille fausse 6811≠7027 ; réessayer ne sert à rien). Réencoder les PNG en palette 2 couleurs (`putpalette` 6 valeurs, sans padding — divise la taille par 2-3 et supprime les runs de « A » intranscriptibles) a permis de passer les 2 objets ; le reste dépasse la limite quoi qu'on fasse (les 333px ne gagnent presque rien, la compression exploitait déjà la redondance ×3).
  - **Chemin fiable pour les gros lots : page Artifact + capability `mcp`** — la page embarque les images en base64 (écrites par script, zéro retranscription) et appelle elle-même `create_file` sur le connecteur « Google Drive » du spectateur via `window.claude.mcp.callTool`, avec vérif de taille par fichier et copy d'erreur par code. Artifact « PACTUM — Envoi Drive » (363262d3-ce4b-464f-a853-b5c93b6d6ee4), générateur dans le scratchpad de session. Patrick ouvre la page depuis claude.ai et clique — à réutiliser pour les prochains lots si la session reste distante.
  - Ménage Drive à faire par Patrick (aucun outil delete côté MCP) : `test_upload_claude.png` (01_En attente) + 3 copies corrompues de `monstre_ruines_chevalier.png` (6 811 octets, 18/07 18h17-18h35) + les anciennes versions du lot (42-102 Ko) s'il les avait déjà glissées à la main.

### Session 2026-07-19 (Claude Code, opus) — PWA + retour écran par écran (annule §16)
- **PWA installable** (zip fourni par Patrick) : `public/manifest.json` (chemins relatifs, robuste au basePath), `public/sw.js` (précache résilient `allSettled` + stale-while-revalidate ; APP_SHELL = pages + assets à nom STABLE, PAS les bundles `_next` hashés → cachés au runtime), 4 icônes `public/icons/`, favicon Geôlier `public/favicon.ico`. `layout.tsx` : `metadata.manifest/appleWebApp/icons` + `viewport.themeColor/viewportFit` + balise `apple-mobile-web-app-capable` (iOS ancien) + enregistrement SW, préfixés du basePath. ⚠️ Piège Next 16 : `app/favicon.ico` (convention fichier) injecté en JS SANS basePath en export statique → favicon cassé sous `/Pat/aldenhar/` ; corrigé via `public/favicon.ico` + `metadata.icons.icon` explicite. Incrémenter `CACHE_VERSION` (sw.js) à CHAQUE déploiement.
- **Déploiement gh-pages (session distante)** : Pages sert depuis la branche `gh-pages`, sous-dossier `aldenhar/` (URL `patrickmorvan21.github.io/Pat/aldenhar/`, basePath `/Pat/aldenhar`). Process : `PAGES_BASE_PATH=/Pat/aldenhar npx next build` → worktree sur `origin/gh-pages` → remplacer le sous-dossier `aldenhar/` par `out/` (préserver la racine `game.js`/`index.html`/`style.css`/`.nojekyll`) → push `gh-pages`. La sandbox ne joint PAS github.io (proxy) — vérifier via l'API GitHub (get_file_contents sur `refs/heads/gh-pages`, actions_list « pages build and deployment »).
- **Retour ÉCRAN PAR ÉCRAN (`Scene.tsx` réécrit, Figma 2072:54) — ANNULE le flux scrollable §16.** Un écran par scène : illustration en haut (fixe), texte au milieu (se tape, défile seulement s'il dépasse), bloc Geôlier dans le flux, CTA ancrés en bas (fixes, adaptés à la hauteur du device via `flex-1 min-h-0` + `max-h-[100dvh]` + image en `dvh`), qui n'apparaissent qu'une fois le texte tapé à 100 %. Plus d'historique accumulé.
  - **L'IMAGE ne change QUE si le contexte change** (`contextChanged` = illustration de scène différente) **ou si un objet est obtenu** ; sinon immobile (`key={image}`). `lastSceneIlloRef` = illustration « au repos » ; image d'objet (`objet_dague/crane/masque` par `kind`) = remplacement momentané. Conséquence d'un choix = enchaîne sur l'écran suivant (option A). Action choisie plus ré-affichée ; entrées `illustration`/`chosen` du `FeedEntry` plus produites (image = état séparé).
  - **État** : `beats` = SEUL écran courant (remplacé), `image`+`imageKind` séparés. `run.feed` repurposé = beats courants (petit) → garde `hasSavedRun()` vrai dès la 1re scène (teste `feed.length>0`) ; sinon fermer l'app juste après le prologue (step 0) perdrait la partie. Reprise = reconstruction à neuf de `sceneAt(step)`, l'ancien fil complet jamais réaffiché.
  - Conservé : dé tactile, résolution graduée + santé/érosion, AGUERRI/ENTAILLÉ, permadeath + écran de mort, scènes chronométrées, bannières Rencontre/Obtenu, Grand Registre, menu, Fisher-Yates.
  - ⚠️ React Compiler (Next 16) strict : fonction utilisée avant déclaration = erreur (remonter `persist` avant `showScreen`) ; mutation en place dans `useMemo` = erreur (sortie en fonction pure module `shuffleChoices`) ; `ref.current` pendant le rendu = erreur (`GameMenu run={loadRun()}`).
  - CSS `.image-swap` (fondu Bayer `steps(4)`, ne joue qu'au changement de src). Vérifié Playwright (viewport 760) : image top 334px, CTA après frappe, transition passive (conséquence → écran suivant, image conservée si contexte inchangé), aucune erreur JS. Lint + build propres.
- **Reste à faire** : appliquer l'écran-par-écran au Prologue si souhaité ; illustrations d'objet dédiées (icônes génériques par `kind` en attendant).

### Session 2026-07-19 (suite, Claude Code, opus) — textes de description ÷2 (playtest)
- Retour Patrick (avant playtest) : **réduire de moitié le texte de description**. Les 17 blocs `narration` de `lib/scene-data.ts` condensés à ~50 % (≈95→50 mots pour les scènes longues), passés de 3 à 2 courts paragraphes, voix PACTUM et accroches conservées (portes balafrée/blanche, Rôdeur, souffle de l'escalier, décompte de la 7e porte, etc.). Non touché : outcomes des jets, `timeoutNarration`, consequences passives (déjà brefs). `CACHE_VERSION` → v4. Vérifié Playwright : scène d'ouverture en 2 paragraphes, choix OK, aucune erreur. Lint + build propres, déployé.

### Session 2026-07-19 (suite #2, Claude Code, fable) — l'Anneau du dé (journal Notion 19/07, VERROUILLÉ)
- **L'Anneau du dé implémenté** (maquettes Figma 2085:19167 « choix d'actions » / 2085:22943 « lancement du dé », qui font foi) — clôt le débat « Lancer requis : 3+ » (la proposition chiffrée type Gemini est REJETÉE : aucun chiffre de mécanique affiché, jamais).
  - Au tap d'un choix risqué : CTA disparus (déjà le cas), **illustration + narration estompées** (voile `die-veil` renforcé 0.5→0.72 — vérifié au pixel : orange bandeau → (83,46,27)), le dé au centre-bas, et **20 encoches carrées** (canvas 200×200, rayon 89, tournées vers la tangente) apparaissent **une par une à ~25ms**, face 1 → face 20.
  - Encoches PLEINES orange = faces qui réussissent — calculées avec la **vraie arithmétique de résolution** (`resolveTier(n, clamp(n+modifier), threshold)`) : les états narratifs (AGUERRI/ENTAILLÉ) modulent visiblement la proportion. Encoches RONGÉES (ruine de pixels épars seedée par face, JAMAIS un contour vide — grammaire « jauge » interdite) = échecs. **Sommet BLANC = face 20, le Destin**, toujours. Disposition : 20 au sommet puis 19→1 en horaire (l'arc orange à droite, comme la maquette). Marque de relique sur encoche : en attente du système de reliques.
  - **Phrase d'aide** « Les encoches pleines sont tes chances de réussite. » + lien souligné « Ne plus afficher » (SEUL élément souligné de l'UI). Préférence `localStorage` clé `aldenhar-aide-de` `{off, ok, ko}` : tap = off définitif ; auto-retrait après première réussite ET premier échec vécus (flags posés à `finishRoll`). Réactivation « Réafficher les aides » = à câbler quand l'écran Options existera.
  - **Au lancer (saisie du dé) : TOUT disparaît sauf le dé** — anneau, aide, hint, et les textes derrière via classe `die-rolling` sur le phone-frame. ⚠️ Deux pièges CSS : (1) l'illustration porte l'animation `image-swap` en `fill both` qui FIGE `opacity:1` — et Chromium fige les `var()` des keyframes au démarrage, donc la parade `--var` du 13/07 ne marche pas ici → la règle `.die-rolling .illustration-frame` doit poser `animation: none; opacity: 0`. (2) Un build `next build` lancé depuis un mauvais cwd échoue en silence (« Couldn't find pages/app ») et on teste alors l'ANCIEN `out/` — vérifier le log de build avant de conclure qu'un fix CSS « ne marche pas ».
  - Relâché sans élan : anneau (redessiné instantané) + aide + hint reviennent. Hint armé = « **Lancer le dé** » sobre (plus de stat affichée — l'Anneau porte l'info, la maquette fait foi ; l'ancien « Lance le dé — STAT · ÉTAT » est retiré).
  - Tag bannière rencontre : « ✦ RENCONTRE ✦ » → « **• RENCONTRE •** » (maquette 2085:19167).
  - Retours Patrick (19/07 soir, 2e vague — AMENDENT la spec du matin) : (1) **plus d'encoche blanche au sommet** — la face 20 est une encoche orange comme les autres (le « sommet blanc = Destin » est annulé) ; (2) **au lancer, l'image et le texte RESTENT visibles** sous le voile sombre (0.72) — seuls l'anneau, l'aide et le hint disparaissent (le « tout disparaît sauf le dé » est adouci ; les règles CSS `die-rolling` sont retirées) ; (3) **bandeau « État temporaire »** en tête de l'écran qui suit un jet : nouveau `FeedEntry` `etat` (state.ts) poussé dans `advance()` si des effets restent actifs après décrément, rendu `.etat-banner` (libellé 10px + vignette `etat_{id}.png` 24px + nom — négatif orange, positif désaturé blanc, convention Essence, jamais un chiffre). `CACHE_VERSION` → v7.
  - Retouches Patrick (19/07 soir) : rayon de l'anneau 89 → **70** (l'encoche la plus basse laisse ~8px d'air au-dessus de « Lancer le dé », mesuré) ; apparition des encoches en **SENS HORAIRE depuis le sommet** (20 → 1 : le Destin blanc ouvre la ronde, l'arc orange suit, les rongées ferment). `CACHE_VERSION` → v6.
- Le reste du journal 19/07 (dé modifiable par Reliques, Temps ressource, Colporteur, Domaine 3 actes/13 zones, Figma table narrative) = concepts validés NON verrouillés, pas de contenu à implémenter encore.
- Vérifié Playwright (build servi localement) : 20 encoches dessinées (pixels orange + blanc + rongé comptés), aide visible et soulignée, hint sobre, saisie → `die-rolling` + texte ET image à opacité 0, verdict + prose inchangés, flags `ko` posés, classes nettoyées à l'écran suivant, aucune erreur JS. Lint + build propres. `CACHE_VERSION` → v5.

### Session 2026-07-20 (Claude Code, fable) — LES LANDES gelée : données structurées de zone
- **`data/zones/landes.json` + `data/zones/SCHEMA.md` créés** (racine du repo — matière de production, pas encore câblée dans l'app). Sources : fiche Notion « 🌍 Le Domaine — Actes & Zones » (règles), spec fonctionnelle (gardiens Intact/Balafré/Rompu, indices, Colporteur), carte Figma 2112:325 (contenu gelé, source de vérité). Le SCHEMA.md documente le format pour les 8 zones suivantes : zone {id, nom, acte, statut, heure_figee, particularites[], bruit_ecrit, motif_cache, sortie} + 4 collections au gabarit commun {id, nom, note, lieu_attache, histoire_bailli (◆), rarete (commune/rare/evenement), textes{decouverte, familiarite, recontextualisation}, illustration, portrait_requis} + champs optionnels gardien/etats et recurrent.
- Comptes vérifiés (script d'intégrité) : **15 lieux · 17 rencontres · 15 créatures · 18 objets** ✓, 9 éléments ◆, sortie = nœud `la_descente` (hors lieux). Aucune illustration générée (temps 2), toutes les strates à null (écriture à venir).
- **Décisions de lecture à connaître** (détaillées dans SCHEMA.md) : (1) « Le Hameau des Renonçants » est un cadre POINTILLÉ (région), pas orange — compté comme 15e lieu (sinon aucun compte ne tombe juste, et 5 chips s'y attachent directement) ; (2) les 6 chips isolées dans la lande (Meute Grise + sa Dent, Recousu, Épouvantail Tourné, Voix Basses, Troupeau sans Berger) = `lieu_attache: null` (errants) ; (3) champ `note` ajouté au gabarit (sous-titres des chips = matière gelée à ne pas perdre) ; (4) rareté « rare » ajoutée aux 2 valeurs prévues (la carte tagge « ◇ rare » sur la Fille, distinct de « ◇ évén. ») ; (5) les 7 portraits de créatures = CHOIX de Claude Code (Juge de Cendre, Fiancée du Gibet, Pendu Mal Fixé, Chien du Bailli, Bête des Chemins Creux, Meute Grise, Recousu — les 8 autres traités en phénomènes/silhouettes, dont l'Appelé toujours vu de dos), la fiche ne disait que « ~7 » : à valider par Patrick ; (6) Corbeaux du Compte et le trio Juge/Fiancée/Ombre attachés à la Colline aux Gibets (chips à ~20-70px du cadre, cohérence thématique gibet), ambiguïté légère.
- Le détail des « strates anti-répétition » n'existe pas encore dans la spec Notion (le journal s'arrête au 19/07) — champs `textes` structurés depuis la fiche zone + la mission ; à re-synchroniser si Patrick détaille la règle.

### Session 2026-07-20 (suite, Claude Code, fable) — LE JEU NE JOUE PLUS QUE LES LANDES (prose réelle)
Demande Patrick : « qu'on puisse tester que la zone les landes. Efface le reste. » Option retenue (validée) : écrire de VRAIS textes maintenant (palier découverte), pas des scènes jetables. Modèle confirmé par Patrick : Fable 5.
- **`lib/scene-data.ts` : les 17 scènes génériques (portes, salle ronde, Rôdeur, escalier, éboulement, pont d'os, limiers ×2, campement-anfractuosité, cage, Geryon ×3, écho, table, grand-registre, porte-7e) sont SUPPRIMÉES**, remplacées par 17 scènes Les Landes écrites depuis `data/zones/landes.json` + la carte Figma 2112:325 : borne-frontiere (0, bruit écrit « quelque part, une corde grince » posé ici) → chemin-creux (Marcheur à rebours, grelot = prix différé) → bete-chemins-creux (combat 1 scène) → colline-aux-gibets (écharde = NOUVEAU flag d'env `echarde-gibet-prelevee`, remplace `porte-balafree-defoncee` dans Scene.tsx avec sa trace d'ouverture réécrite) → pendu-qui-parle (GARDIEN, non-combat : le Bailli juge — foe `bailli-pendu`, trancher sa corde = highStakes 14) → champ-des-fixes → pendu-mal-fixe (combat) → serment-hameau (particularité de zone : jurer = passif tenu, jurer faux = dette §17 `serment-creux`, refuser = Courage 13) → marche-muet (Colporteur + Rebouteux) → campement (id CONSERVÉ pour l'exclusion du soin aléatoire — c'est le Moulin sans Ailes, « Dormir malgré le crépuscule », la Fille entrevue dans les issues de la garde) → chapelle-des-cordes → puits-condamne (scène CHRONOMÉTRÉE 7000ms → Mains du Puits en timeoutChoices) → chien-du-bailli (combat, option Empathie) → petit-tribunal (registre:true = le Registre des Pendaisons rejoue le Grand Registre) → meute-grise-1/2 (combat 2 scènes, chaque issue clôt) → palissade-sud (Veilleur + l'Appelé de dos, « Franchir la Descente » VERROUILLÉ = tease Acte II). Mécaniques toutes conservées et re-réparties (risqué majoritaire, ≤1 verrouillé/scène, passifs §19, dettes, env-flag, timed, registre, rest, highStakes, foe/dette de sang). Taunt Geôlier « os du pont » → « corbeaux », cause de mort par défaut « les couloirs » → « les Landes ».
- **`data/zones/landes.json` : 40 strates `decouverte` remplies** (12 lieux, 10 rencontres, 10 créatures, 8 objets — celles réellement mises en scène) ; 25 entrées restent à null (mare, verger, tour de guet, la Fille, l'Hésitant, Juge de Cendre, Fiancée, Recousu, Épouvantail, Troupeau, Sonneur, Gamin, Femme au Seuil, Époux, Déserteur + objets non touchés) → matière pour les prochaines scènes de la zone.
- **Aucun asset d'illustration Landes** : toutes les scènes (combats compris) retombent sur le portail placeholder — à remplacer via `/leo-import` (suggestions de noms : `monstre_bete_chemins_creux`, `monstre_meute_grise`, `monstre_pendu_mal_fixe`, `monstre_chien_bailli`, `scene_colline_gibets`, `scene_hameau`, `scene_moulin`, `scene_palissade`…). La règle « illustration systématique sur les combats » est temporairement non tenue, faute d'images.
- Le prologue (Seuil), l'accueil, le Registre du compte, la Besace, la mort, l'Anneau : inchangés — seuls les contenus de scène ont changé. `CACHE_VERSION` → v8.
- Vérifié en Playwright (build servi localement) : ouverture borne-frontière + CTA + anneau + jet (FUNESTE) + enchaînement chemin-creux, aucune erreur JS applicative (seul le fetch Google Fonts est bloqué par le proxy sandbox, préexistant). Lint + build propres. NB test : un run injecté avec `feed: []` et `step: 0` est traité comme « pas de sauvegarde » (hasSavedRun) — seed avec un feed non vide.
- Gardien (Intact/Balafré/Rompu) et Serment (récompense/mémoire du Geôlier) : posés en CONTENU, la mécanique inter-runs reste à construire (comme prévu par la spec, pas de régression).

### Session 2026-07-20 (suite #2, Claude Code, opus) — numéro de version visible + SW réseau-d'abord
Demande Patrick : afficher la version du jeu à l'accueil (« genre 1.2 »), bas à droite, blanc opacité 50 %, et la bumper à chaque déploiement selon la grandeur du changement. Motivation : « j'ai relancé le jeu et j'ai pas l'impression que les changements ont été mis à jour ».
- **`lib/version.ts`** = SOURCE UNIQUE (`APP_VERSION = "1.2.0"`), avec la règle de bump documentée (PATCH = correctif/tweak, MINOR = contenu/écran/mécanique notable, MAJOR = bouleversement structurel) + rappel « toujours bumper `CACHE_VERSION` de sw.js en parallèle ». **Réflexe à tenir : à CHAQUE déploiement, monter `APP_VERSION` ET `CACHE_VERSION` ensemble.** 1.2.0 = build Les Landes.
- Affichée dans `Home.tsx` (`.app-version`, `globals.css`) : `position:absolute` dans le `phone-frame`, right 10 / bottom 8, 10px, blanc `#fff` opacité 0.5, `pointer-events:none`. Vérifié Playwright (v1.2.0, opacity 0.5, rgb(255,255,255), à 10px/8px du coin).
- **VRAIE CAUSE du « les changements n'apparaissent pas » corrigée dans `public/sw.js`** : le handler `fetch` était cache-first pour TOUT, y compris les navigations → au relancement, l'ancien HTML en cache s'affichait tant que le réseau n'avait pas révalidé en fond. Désormais : **navigations (`request.mode === "navigate"` / `destination === "document"`) = RÉSEAU D'ABORD**, cache en secours uniquement hors-ligne (règle permadeath préservée : fallback sur la page cachée puis sur `./`). Le reste (bundles `_next` hashés, assets, illustrations) reste en stale-while-revalidate — pas de risque de vieux bundle, le HTML frais pointe déjà vers les noms hashés neufs. `CACHE_VERSION` → v9.
- NB pour Patrick : une PWA déjà installée peut encore montrer l'ancien écran UNE fois au tout premier lancement après ce déploiement (le temps que le nouveau SW s'active), puis c'est réglé pour toutes les fois suivantes. Le numéro de version est le juge de paix : s'il affiche v1.2.0, le build est à jour.

### Session 2026-07-21 (Claude Code, opus) — retours playtest 21/07 (VAGUE 1 : dé, overlay, courbe, jalon)
⚠️ Ces retours (journal Notion « 🗓️ 21 juillet 2026 », page Spec fonctionnelle) n'avaient PAS été vus/faits : ils datent d'APRÈS le lot Landes (20/07). 7 points, ordre de priorité imposé : 1. Traversée & liaisons → 2. Physique du dé → 3. Overlay texte → 4. Objets actifs/passifs → 5. Courbe d'entrée → 6. Jalons 1re fois → (7. plus tard : simulateur MC, variantes de texte). Livré par vagues. **Vague 1 = points 2, 3, 5, 6** (les contenus, sans toucher l'architecture de navigation).
- **Point 2 — Physique du dé = le tirage (VERROUILLÉ)** : `Die3D.beginSettle()` réécrit. Plus AUCUN `result` pré-tiré. À l'immobilisation, on lit la face réellement tournée vers la caméra (+Z) : pour chaque face, normale sortante × quaternion courant → max de la composante Z = face gagnante. `result = bestFace`. Alignement = `delta·q` (petit redressement qui pose CETTE face à plat en préservant la rotation dans le plan, pas un grand snap). La face montrée NE PEUT PLUS diverger du chiffre annoncé (garantie structurelle : `result` dérivé de l'orientation qu'on aligne). L'Anneau + les paliers + le tremor highStakes fonctionnent inchangés (result est connu au beginSettle). Vérifié Playwright : verdict émerge du geste (RÉUSSITE/FUNESTE selon le lancer).
- **Point 3 — Overlay de texte long** : quand le contenu déborde la zone, le panneau MONTE sur l'illustration (`.scene-text-zone.raised` fond charbon, `marginTop` négatif mesuré), bordure haute = trame de pixels charbon (`.text-raise-fade`, masque alpha Bayer via `ditherFadeMaskDataUrl(48,40,(_,ny)=>1-ny)` : dense en bas → clairsemé en haut, JAMAIS un dégradé CSS). CTA ancrés en bas, intouchés. Convergence additive mesurée post-layout (`raise` grandit tant qu'il reste du débordement, plafond 62 % de la hauteur d'illustration), remis à 0 à chaque écran. Hauteur d'illustration sortie en state (`illoH`) pour ne pas lire un ref au rendu (React Compiler).
- **Point 5 — Courbe d'entrée invisible (« dès maintenant »)** : `entrySoftening(mem)` (player-memory) → seuil abaissé selon `deaths` : 1re run −2, après 1re mort −1, après 2e −1, puis 0. Appliqué à l'armement du dé (`threshold - soft`, min 2) : AUCUN affichage, et l'Anneau (calculé sur ce seuil) montre juste un peu plus d'encoches pleines — cohérent.
- **Point 6 — Jalon de première mort** : `forgeRelic(..., floorRare)` → la 1re mort du compte forge une relique garantie rare+ (« fragment fort ») ; `recordDeath` lit `deaths===0` avant l'incrément. `DeathScreen` prop `firstDeath` → ligne d'accueil du Geôlier en accent (`.death-jailer-welcome`), ton qui accueille au lieu de railler + sous-titre adapté. (Le 2e volet du point 6 — marqueur « 1re traversée complète » au Registre — dépend de la traversée, fait en vague 2.)
- **PAS ENCORE FAIT (vagues suivantes)** : point 1 (traversée & liaisons — le gros chantier, décision Patrick : Descente = fin sèche « Acte II à venir »), point 4 (objets 2 actifs + 2 passifs, Besace, actif en 4e choix contextuel — maquette Figma 1925:524 fournie), 2e volet du point 6. `APP_VERSION` 1.3.0, `CACHE_VERSION` v10.

### Session 2026-07-21 (suite, Claude Code, opus) — playtest 21/07 VAGUE 2 : TRAVERSÉE & liaisons (chantier n°1)
Le point 1 (priorité n°1 du 21/07, plainte principale de Patrick : « les scènes s'enchaînent sans déplacement joué »). Grammaire implémentée : liaison → lieu → liaison → rencontre → … → Descente. Décision Patrick : **Descente = fin sèche** (« Acte II à venir »).
- **`lib/scene-data.ts`** : couche traversée ajoutée après `sceneAt`. `sceneById(id)`, `ENTRY_SCENE = "borne-frontiere"`, `APPROACH` (phrase sensorielle d'orientation par lieu — jamais un danger frontal, habitat du « Vent qui ment »), `TRAVERSAL_POOL` (15 lieux tirables), `LIAISON_AMBIANCES`/`LIAISON_JAILER`, `makeLiaison(a,b,seed)` (scène de liaison générée : 1 beat d'ambiance + « Deux directions s'ouvrent » + 2 choix d'orientation), `pickLiaisonOptions(visited,seed)` (2 lieux non visités, Fisher-Yates seedé → stable à la reprise), `DESCENTE_SCENE` (terminal). Nouveaux champs `Scene` : `approach`, `chainNext` (meute-grise-1 → -2 sans liaison, chaîne = 1 lieu), `liaison`, `terminal`. Nouveau `Choice.orient: { dest }`.
- **`lib/state.ts`** : `TraversalState { phase, current, visited[], target(3-4), liaisonOpts, seed, done }` + `freshTraversal()`. Ajouté à `RunState.trav`, à `fresh()`, et migration `loadRun` (runs d'avant → traversée amorcée depuis leur scène linéaire courante). Import `ENTRY_SCENE`/`sceneAt` de scene-data (pas de cycle).
- **`components/Scene.tsx`** : `scene` n'est plus `sceneAt(step)` mais un **state** posé par advance/orient/restore. `sceneFromTrav(trav)` (pur) reconstruit l'écran courant (Descente / liaison / lieu) — utilisé au rendu ET à la reprise. `advance()` refactoré : résout le prochain nœud (toDest d'une orientation / chaîne / Descente si `visited>=target` / sinon nouvelle liaison), pose `run.trav`, `setScene(nextScene)`. Les états temporaires ne vieillissent qu'en quittant un LIEU (pas une liaison — sinon usure ×2). `onSelect` : nouvelle branche `choice.orient` (→ `advance({toDest})`, sans dé ni conséquence) et court-circuit `scene.terminal` (→ resetRun + reload). `step` reste un compteur monotone (seed du Fisher-Yates, puce Jour). Liaisons exclues du drop de soin aléatoire.
- Vérifié Playwright (build servi localement) : borne → choix risqué → dé → **LIAISON avec 2 boutons d'orientation** (« Vers les rangées de poteaux… » / « Vers un craquement de bois… ») → lieu choisi (Champ des Fixés) ; atteinte de la Descente à `visited>=target` (« Tu as traversé les Landes vivant » + « Repartir de la Borne ») ; reprise en pleine Descente restaurée ; redémarrage → accueil première partie. Aucune erreur JS. Lint + build propres. `APP_VERSION` 1.4.0, `CACHE_VERSION` v11.
- **Reste du 21/07** : point 4 (objets 2 actifs + 2 passifs — maquette Figma 1925:524 capturée : détail objet en haut, 2 slots Passifs à gauche + 2 Actifs à droite, Reliques dessous ; actif = 4e choix contextuel) ; point 6 volet-2 (marqueur « 1re traversée complète » au Grand Registre — la mémoire `zonesCleared` reste à ajouter) ; « Vent qui ment » sur les orientations (leurre sensoriel) noté mais pas implémenté (orientations honnêtes pour l'instant).

### Session 2026-07-21 (suite #2, Claude Code, opus) — playtest 21/07 VAGUE 3 : OBJETS actifs/passifs (point 4)
Point 4 du 21/07 (maquette Figma 1925:524). Deux types d'objets SEULEMENT, Besace 2+2, actif en 4e choix contextuel.
- **`lib/besace.ts`** : `BesaceItem` gagne `slot: "actif" | "passif"` + effets. **Actif** (`heal`, `cure`) = usage unique consommé sur décision. **Passif** (`passiveMod`, `passiveScope: "combat"|"all"`) = modificateur permanent au jet effectif tant que porté (jamais un chiffre — l'Anneau reflète). `BESACE_ACTIF_SLOTS = 2`, `BESACE_PASSIF_SLOTS = 2`. Helpers : `besaceBySlot`, `passiveMod(besace, isCombat)`, `hasBesaceRoom(besace, slot)`, `normalizeItem` (compat sauvegardes d'avant : soin→actif heal+cure, arme/babiole→passif +1). Dague de départ = passif +1 combat. SOINS_MINEURS = actifs ; RECOMPENSES_DESTIN = mix actifs (soins) / passifs (babioles/armes), la Larme du Geôlier = passif +2 all (légendaire).
- **`lib/state.ts`** : besace normalisée au chargement (`p.besace.map(normalizeItem)`).
- **`components/Scene.tsx`** : (1) modificateur du dé = états temporaires **+ `passiveMod`** des objets portés (combat vs all) ; (2) **4e choix contextuel** — un effet (hors rendu, lit la Besace/santé) pose `activeChoice` quand un soin est pertinent (santé <0.75 ou blessure active), ajouté en bas des choix (`renderedChoices`) ; le choisir consomme l'objet (`choice.useItem`), applique heal/cure, puis la scène se résout ; (3) campement : **plus d'auto-consommation** de soin (« rien d'automatique ») — le camp ne fait plus que jour+soin partiel+atténuation ; (4) drop de soin en explo et ajout du Destin gardés par `hasBesaceRoom` (slot plein = objet perdu, vrai arbitrage).
- **`components/GameMenu.tsx`** : Inventaire refait en **2 groupes côte à côte Passifs · Actifs** (maquette 1925:524), détail avec tag « Actif/Passif · Rareté », bouton **Utiliser** sur un actif → `onUse` remonte à Scene qui consomme côté run (santé/effets synchronisés) ; copie locale de la Besace pour refléter le retrait immédiatement. `SectionHead` gagne `inset` (label compact sans filet pleine largeur pour les 2 colonnes).
- **`lib/scene-data.ts`** : `Choice.useItem?: { itemId }` (4e choix contextuel).
- Vérifié Playwright : 4e choix « Utiliser — Baume » présent quand ENTAILLÉ+santé basse ; à l'usage l'objet est consommé, l'ENTAILLÉ se referme, la santé monte ; menu affiche Passifs/Actifs. Lint + build propres. `APP_VERSION` 1.5.0, `CACHE_VERSION` v12.
- **Reste du 21/07** : point 6 volet-2 (marqueur « 1re traversée complète » au Registre — mémoire `zonesCleared` à ajouter) ; « Vent qui ment » sur les orientations. Point 7 (simulateur MC, variantes de texte) = plus tard, comme prévu.

### Session 2026-07-21 (suite #3, Claude Code, opus) — écran OPTIONS (Figma 2137:406)
Patrick a maquetté l'onglet Options (3e onglet du menu). Toutes les fonctionnalités listées, mais seules les RÉELLES sont branchées ; les autres grisées à 50 % (demande explicite).
- **`lib/settings.ts`** (NOUVEAU) : store compte `aldenhar-settings` (cache module) — `textReveal` (lente/normale/instantanee), `textSize` (petit/normal/grand), `animations` (completes/reduites), `vibrations` (bool). `loadSettings`/`saveSettings`/`mutateSettings`, `applySettingsToDom` (pose `textsize-*` / `anim-reduced` sur `<html>`), `revealFactor` (0=instant, lente ×2.2), `haptic(pattern)` (respecte Vibrations), `animReduced()`.
- **Branchements** : (1) **Apparition** → `TypedText` lit `revealFactor()` : instantanée = texte immédiat, lente = frappe ×2.2 ; (2) **Taille** → `.textsize-petit/grand .feed-narration { font-size }` (spécificité 0,2,0 > utilitaire text-[13px]) ; (3) **Animations réduites** → classe `anim-reduced` sur `<html>` : CSS coupe flashs/halos/secousses/franges/nappe KO/edge-wave/image-swap, et `HeroGeolier` rend l'image fixe (comme prefers-reduced-motion) ; (4) **Vibrations** → tous les `navigator.vibrate` du dé passent par `haptic()` ; (5) **Réafficher les aides** → supprime `aldenhar-aide-de` (l'aide du dé revient) ; (6) **Effacer la progression** → 2 taps (confirme) → supprime run+memory+aide → reload. `applySettingsToDom()` appelé au montage de `Home` (racine).
- **UI (`GameMenu` OptionsTab)** : onglet OPTIONS activé (plus inerte). Boutons segmentés `SegControl` fidèles à la maquette (bordure BLANCHE + entailles de coins charbon, sélection par opacité 1 vs 0.5, texte mono 12px 2.4px). Sections grisées 50 % + inertes : Musique (toggle), Lecture à haute voix (non/oui), Vitesse de lecture (lente/normale/vive), Restaurer mes achats, liens de pied (Crédits / Confidentialité / Envoyer un retour). Libellés exacts pris du Figma via get_design_context.
- Vérifié Playwright : onglet Options affiche tous les libellés ; Taille→grand + Apparition→instantanée + Animations→réduites posent `textsize-grand anim-reduced` sur `<html>` et persistent dans `aldenhar-settings` ; Réafficher les aides vide `aldenhar-aide-de`. Lint + build propres. `APP_VERSION` 1.6.0, `CACHE_VERSION` v13.
- Écarts assumés : Musique en 2-seg (la maquette montre un toggle à glissière — rendu en segments inertes, à affiner si Patrick veut le vrai switch) ; le helper « Utilise la voix du système » sous Réafficher les aides (copie maquette) remplacé par un texte adapté aux aides.

### Session 2026-07-22 (Claude Code, opus) — retouches UI (CTA, texte long, Musique, Geôlier, reliques)
Lot de 5 retouches Patrick après le déploiement Options.
- **Libellés de CTA raccourcis** (`lib/scene-data.ts`) : 57 libellés + 15 phrases d'orientation `APPROACH` condensés (≤ ~24 car pour les choix à tag de stat) → ne débordent plus. `FitLabel` : plancher `min` 9 → 8 en filet de sécurité. Vérifié Playwright : aucun span de CTA en overflow.
- **Texte long : fondu de pixels SUPPRIMÉ** (Patrick : « pas beau ») — `.text-raise-fade` / `.scene-text-zone.raised` / le masque tramé / les états `raise`/`illoH` retirés. Remplacé par un **rétrécissement de l'illustration** (`compact` : 44dvh/352px → 30dvh/236px) déclenché UNIQUEMENT si le texte fini déborde vraiment la zone (mesuré `scrollHeight>clientHeight`, évalué à la fin de la frappe, une seule fois par écran, pas de saut ni d'oscillation). Sur viewport très court, le reliquat scrolle proprement. Image pleine par défaut quand le texte tient.
- **Interrupteur Musique** (`GameMenu` OptionsTab) : le segment non/oui remplacé par une vraie glissière (rail + pavé carré, état OFF, inerte à 50 %) conforme à la maquette.
- **Bloc Geôlier** (`Scene.tsx` case jailer) : texte passé de 13px/interligne 1.6 à **11px / interligne 1.2** (demande Patrick).
- **Inventaire limité à 3 reliques** (`GameMenu` InventaireTab) : `allRelics.slice(-3)` (les 3 plus récentes) partout (détail + slots).
- Vérifié Playwright : CTA sans overflow, switch Musique présent, 3 slots de reliques (sur 5 en mémoire), image compacte au bon moment, aucune erreur JS. Lint + build propres. `APP_VERSION` 1.6.1, `CACHE_VERSION` v14.

### Session 2026-07-24 (Claude Code, fable) — intégration des 30 illustrations Landes validées (Drive « 03_Validé »)
Les images validées du pipeline arrivent désormais dans le Drive `.../PACTUM/Photos/03_Validé/` (sous-dossiers `1_Rencontres`, `2_Environnement`, `3_Objets`). Mission : les copier dans `public/assets/` (nom exact `{scene|monstre|objet}_{sujet}_{variante}.png`), les rattacher aux ids de `landes.json`, ne rien toucher côté Drive.
- **Technique de copie (rappel, éprouvée)** : le connecteur Drive renvoie le base64 INLINE dans le résultat de `download_file_content` — les petits fichiers (< ~50 Ko de b64) ne sont PAS déchargés sur disque. Le harnais écrit fidèlement CHAQUE résultat d'outil dans le transcript de session `.jsonl`. Donc : télécharger par lots (~5 à la fois pour limiter le contexte), puis un script (`scratchpad/extract_drive_pngs.py`) lit le b64 par `title` DEPUIS le `.jsonl`, décode, vérifie la signature PNG **et la taille exacte** annoncée par le Drive, écrit dans `public/assets/`. Zéro retranscription de b64 par le modèle (la retranscription > 5 Ko corrompt de façon déterministe — voir 18/07). 30/30 fichiers écrits, taille + signature OK.
- **30 illustrations intégrées** (`public/assets/`) : 20 `monstre_*` (chien_du_bailli, colporteur, corbeaux_du_compte, corde_vive, doyenne, ecrivain_public, femme_au_seuil, fiancee_du_gibet, hesitant, juge_de_cendre, mains_du_puits, marcheur_a_rebours, meute_grise, pendu_mal_fixe, pendu_qui_parle, petite_fixee, rebouteux, recousu, sonneur_sans_cloche, troupeau_sans_berger), 9 `scene_*` (champ_des_fixes, chapelle_des_cordes, colline_aux_gibets, marche_muet, mare_aux_regards, moulin_sans_ailes, petit_tribunal, puits_condamne, verger_noir), 1 `objet_*` (denonciation_vierge). Les 30 champs `illustration` de `data/zones/landes.json` étaient déjà câblés (session 20/07) — vérifiés : chaque `assets/<fichier>` pointe sur un PNG présent.
- **⚠️ `landes.json` n'est PAS lu au runtime** (c'est de la matière de production) — le jeu lit `lib/scene-data.ts`. Donc, pour que les images apparaissent RÉELLEMENT en jeu, câblage direct dans `scene-data.ts` : champ `illustration` posé sur les **12 scènes dont l'id correspond exactement au nom d'un asset** (colline-aux-gibets, champ-des-fixes, chapelle-des-cordes, puits-condamne, petit-tribunal, marche-muet, campement→`scene_moulin_sans_ailes` [le camp EST le Moulin sans Ailes], pendu-qui-parle, pendu-mal-fixe, chien-du-bailli, meute-grise-1, meute-grise-2). Fini le placeholder portail sur ces 12 scènes.
- **Non câblé, volontairement (règle « signale au lieu de deviner »)** : les scènes SANS asset de même id (borne-frontiere, chemin-creux, bete-chemins-creux, serment-hameau) restent sur le portail — aucun `scene_borne`/`scene_chemin`/`monstre_bete_chemins_creux` dans le lot. Et ~18 assets de PERSONNAGE (marcheur, petite_fixee, colporteur, rebouteux, mains_du_puits, corde_vive, corbeaux, juge_de_cendre, fiancee, doyenne, femme_au_seuil, sonneur, hesitant, troupeau, recousu, ecrivain, denonciation_vierge, mare/verger non mis en scène) sont dans `assets/` et câblés dans `landes.json` mais PAS choisis comme illustration d'écran : une scène = une image, et pour un lieu comme chemin-creux « montrer le Marcheur » vs « montrer le décor » est un choix de DA à trancher par Patrick. À faire au coup par coup.
- Vérifié : build `next build` OK (TypeScript valide les 12 champs `illustration`), 30 assets exportés dans `out/assets/`, câblage présent dans le bundle, assets servis en 200 (taille exacte) via serveur statique local. Lint propre (seul le warning police préexistant). `APP_VERSION` 1.6.1 → **1.7.0**, `CACHE_VERSION` v14 → **v15**.

### Session 2026-07-24 (suite, Claude Code, opus) — objets manquants du Drive + 3 scènes câblées
Retour Patrick : « dans les photos objets tu es censé en avoir beaucoup plus » (je n'avais tiré qu'1 objet du dossier `03_Validé/3_Objets`) + 3 mappings retrouvés par Patrick.
- **23 icônes d'objet des Landes** récupérées du Drive (même technique éprouvée : `download_file_content` par lots → lecture du b64 dans le transcript `.jsonl` → `scratchpad/extract2.py` décode/vérifie signature PNG + taille exacte → `public/assets/`). Fichiers : craie_condamne, dent_meute, ordonnance_fixation, corde_coupee_fille, offrandes_borne, brin_chanvre_beni, battant_cloche, fruit_cendre, grelot_charretier, registre_pendaisons, carnet_fossoyeur, cle_maison_muree, echarde_grand_gibet, sceau_bailli, miroir_poche_fele, jouet_petite_fixee, grimoire, couronne_brisee, fiole_baume, cle_rouillee, miroir_ame, lanterne_rouillee, dague_os. **⚠️ Ce sont des ICÔNES d'objet (Besace/Inventaire), pas des illustrations de scène** — posées sur disque, PAS encore câblées sur les objets de `lib/besace.ts` (les icônes y sont encore génériques par `kind`). Câblage objet-par-objet = étape suivante (dépend du contenu réel de Besace, pas juste du fichier).
- **3 scènes câblées sur leur vraie image** (mappings fournis par Patrick, assets récupérés) : `borne-frontiere` → `scene_transition_borne_sud_a.png`, `bete-chemins-creux` → `monstre_bete_chemins_creux_a.png`, `serment-hameau` → `monstre_juge_de_cendre_c.png` (juge de Cendre déjà en assets depuis le 1er lot). Posé dans `scene-data.ts` (runtime) ET `landes.json` (cohérence prod). Il reste `chemin-creux` sur le portail (aucun asset de décor de couloir-creux ; l'image de la Bête est réservée à la scène de combat qui suit).
- **« ~18 assets de personnage » — clarification demandée par Patrick** : ça voulait dire simplement que beaucoup de PNG de créatures/PNJ des Landes sont bien dans `assets/` et référencés dans `landes.json`, mais qu'une SCÈNE n'affiche qu'UNE image — donc pour un lieu qui a à la fois un décor et un personnage rattaché, il faut choisir lequel montrer. Je n'ai pas voulu deviner à sa place. Ce n'est pas un bug ni un manque : juste des images en réserve, prêtes à servir dès qu'on écrit la scène qui les met en avant.
- Vérifié : `next build` OK (TS valide les 3 nouveaux champs), eslint propre (warning police préexistant seul). `APP_VERSION` 1.7.0 → **1.7.1**, `CACHE_VERSION` v15 → **v16**.
- **Lots en attente** (Patrick : « tu peux enchaîner ») : journal Notion 23/07 (densité narrative & difficulté), 24/07 (écran du Nom du prologue), musique.

### Session 2026-07-24 (suite #2, Claude Code, opus→fable) — lot Notion 23/07, vagues 1-4 (chantiers 1/2/3/4/6)
Ordre imposé par Patrick : 23/07 → 24/07 → musique. Le lot 23/07 (« trop rapide, trop facile, on ne ressent pas l'histoire ») livré par vagues, chacune déployée : v1.8.0/v17 → v1.11.0/v20.
- **Chantier 1 — Objets réels & usure (v1.8.0)** : `LANDES_OBJETS` (besace.ts) = 5 objets réels ancrés aux lieux (Offrandes/borne·actif, Écharde/colline·passif combat, Chanvre/chapelle·actif cure, Carnet/champ·passif all, Lanterne/palissade·passif all), champ `illustration` sur `BesaceItem` (repris par le menu et l'image d'objet obtenu). Deux canaux : `Scene.loot` (ramassage à l'arrivée, 1× par run via `RunState.looted`) et `Choice.grantsLoot` (gagné sur RÉUSSITE d'un choix d'examen — Fouiller les offrandes) ; un seul « Obtenu » par écran. Usure : échec dur (critique/malédiction) HORS combat = **+1 Jour affiché** (puce mid-écran + ligne) ; soin générique 22 %→12 % ; dernier tiers de traversée (visited ≥ target−1) durci de +1 seuil. Courbe d'entrée vérifiée déjà bornée aux 3 premières morts.
- **Chantier 2 — Chapitres garantis (v1.9.0)** : `lib/chapters-data.ts` = 4 chapitres du Bailli (le-proces/petit-tribunal · le-registre/champ-des-fixes · la-fille/campement · le-gibet-vide/colline), 3 beats chacun. `RunState.chapter {id, stage 0-3}` tiré au mount (rotation compte via `PlayerMemory.chaptersSeen`, complétée au stage 3). Amorce → 1re liaison ; développement → arrivée au lieu dédié, lequel est GARANTI dans les orientations (`pair[seed%2]`) tant que stage<2 ; résolution partielle → insérée AVANT la narration de la Descente. ⚠️ Le texte du « Procès » est de moi — le « document de démonstration » de Patrick est introuvable côté Notion (probablement Claude Chat) : à réconcilier s'il le fournit.
- **Chantier 6 — Signature garantie (v1.9.0)** : la Colline aux Gibets est OFFERTE à chaque liaison tant que non visitée (`pair[(seed+1)%2]`, slot opposé à la garantie de chapitre). Le joueur peut l'éviter en choisissant l'autre direction (quasi-totalité, pas obligation).
- **Chantier 3 — Le Soupçon (v1.10.0)** : `RunState.soupcon` 0..6 + `soupconSeen`, `PlayerMemory.fixations`. JAMAIS affiché — manifestations en monde (`SOUPCON_PALIERS` 1-5, jouées 1× chacune). Monte : `Choice.soupcon` (+1 : refuser/tricher Serment, parler au Pendu, trancher sa corde, Rebouteux) · `Scene.soupconOnArrival` (colline +1) · échec d'un jet EMPATHIE hors combat (+1, dans onComplete). Baisse : jurer (−1), **« Donner un nom à la plume »** au petit-tribunal (−2, passif, jamais suggéré, `setsEnvFlag: "a-denonce"`). Palier 6 → la traversée est DÉROUTÉE (advance) vers `proces-du-heros` (dans SCENES mais hors APPROACH/pool ; `fixationTrial: true`) : Plaider serré (Ruse 13) / Prendre le hameau à témoin (Empathie 13), highStakes. Réussite → soupcon=soupconSeen=3 et la traversée continue ; échec → **mort par fixation quelle que soit la santé** (recordDeath cause « le Hameau des Renonçants », fixations+1). fixations ≥ 2 → ligne d'accueil altérée au seed d'une run neuve.
- **Chantier 4 — Liaisons contextuelles (v1.11.0)** : 30 ambiances (5 génériques conservées en fallback + 25 variantes `LIAISON_VARIANTS` : 10 provenance, 10 Soupçon, 3 santé, 2 chapitre, 2 objet porté [match par sous-chaîne de NOM], 3 destination offerte). Sélection = éligibles → spécificité max → seedé (`pickLiaisonAmbiance`). `makeLiaison(a,b,seed,ctx?)` ; ctx (`LiaisonCtx`) construit dans Scene.tsx (`liaisonCtx(run, from)`) aux 2 points : `advance` (from = scene.id) et `sceneFromTrav(t, run)` (from = dernier visited) → même texte à la reprise (vérifié).
- Vérifié Playwright à chaque vague (état seedé en localStorage + drag réel du dé ; mémoire deaths:3 pour neutraliser entrySoftening) : loot arrivée/examen + icônes réelles + usure J+1 ; 3 beats de chapitre + rotation ; deltas Soupçon + manifestation palier + déroutage procès + relaxe (6→3) + mort par fixation (FUNESTE → fixations 1, deaths+1, run reset) ; liaison contextuelle + déterminisme reprise. ⚠️ Piège de test : `addInitScript` re-seed à CHAQUE navigation — pour tester une reprise, capturer le localStorage et ouvrir une NOUVELLE page seedée avec (jamais `page.reload()`). Jet highStakes : attendre le verdict jusqu'à ~9s (settling rallongé).
- **RESTE du 23/07 (non bloquant)** : reliquats du Soupçon (objet « Craie du Condamné » [icône en assets], issue « objet/témoin à dette » au procès, dépôt aux Offrandes qui fait baisser) ; lacunes de couverture (maison_du_bailli explorable, rencontres sans scène, la Fille jouable) — non prioritaires per Notion ; point 7 (simulateur MC, variantes de texte) = « plus tard » per spec. Réconciliation du texte du Procès si Patrick fournit le doc de démonstration.

### Session 2026-07-24 (suite #3, Claude Code, fable) — lot 24/07 : écran du Nom du prologue (v1.13.0)
- **Écran du Nom** (`components/Prologue.tsx` réécrit) : nouveau beat entre les 4 souvenirs et la clôture. Prompt Geôlier 42 ms (« …Il me manque comment on t'appelait. ») ; le champ n'apparaît qu'une fois la phrase FINIE (jamais de formulaire pendant qu'il parle). Signature = input Instrument Serif 32px centré, caret orange, **trait d'inscription en semis de pixels** (canvas 280×4 généré une fois, caché module — jamais un filet net), libellé « Signe le pacte », max 16 car. CTA **SCELLER LE PACTE** (orange plein, segments décalés du prototype 16/07 — écran sans maquette Figma donc langage du SKILL), inerte < 2 caractères. Lien souligné blanc-50 « Qu'il choisisse pour moi » → un des 8 noms validés (Cendre · Le Muet · Sans-Nom · Corbeau · Le Tardif · Braise · L'Onzième · Suie). Note « Ce nom entrera au Grand Registre… » affichée seulement si `runsStarted <= 3`. Le nom scellé → `run.heroName` (Registre/dette de sang/mort). Clavier mobile : `HeroGeolier` gagne un prop `height` (368 → 232 sur l'écran du Nom).
- **Clôture auto** : « Je sais qui tu es. Le dé, lui, décidera qui tu deviens. » (texte déjà en place) → temporisation **4 s** après la fin de frappe puis Jour I TOUT SEUL — plus de tap (le verdict `computeVerdict` tombe dans ce timer ; React Compiler interdit `ref.current =` au rendu → logique repliée dans l'effet).
- **« Touche pour continuer »** (correctif de test 24/07) : mention Roboto Mono blanc-50 en bas des beats d'amorce (écrans de narration sans bouton), estompée après acquisition (`runsStarted <= 3`).
- **⚠️ Exception inscrite** : le champ du nom = seule saisie de texte libre du jeu (pas un choix de gameplay, aucune agentivité fiction) — documentée en commentaire de Prologue.tsx. **Modération des noms** (Registre public, App Store) : décision Patrick EN ATTENTE, à poser avant la mise en ligne du Registre agrégé (le Registre actuel est local).
- Vérifié Playwright (parcours neuf complet) : affordance amorce, prompt → champ → note → lien auto (« Braise ») → clamp 16 car. → SCELLER → clôture → **auto-départ à ~4 s sans tap** → borne Jour I, `heroName` persisté, `prologue.done`. Aucune erreur JS. Lint/build propres.
- **MUSIQUE (v1.14.0)** — moteur complet branché, FICHIERS EN ATTENTE. Patrick a indiqué le Drive `PACTUM/Assets/Musique` (Intro/intro.mp3 7,7 Mo · Acte 1/Les Landes/Landes_1.mp3 6,5 Mo · Landes_2.mp3 12,3 Mo · Landes_3.mp3 6,5 Mo — NB Landes_1 et Landes_3 ont la MÊME taille exacte, possible doublon à vérifier). **⚠️ IMPOSSIBLE de les rapatrier en session distante** : `download_file_content` échoue (« session expired ») dès ~1 Mo — c'est une limite de TAILLE du connecteur (un fichier de 914 octets passe) — et le proxy sandbox bloque `drive.google.com` en direct (CONNECT 403), lien public ou pas. **Chemin de livraison** : Patrick dépose les 4 mp3 via l'UI web GitHub (branche dev, dossier `aldenhar/public/audio/`, noms `intro.mp3`/`landes_1.mp3`/`landes_2.mp3`/`landes_3.mp3` — cf. `public/audio/README.md`) ou en PJ de message ; ensuite simple rebuild+deploy, AUCUN code à changer.
- Moteur (`lib/audio.ts`) : singleton Audio, intro en boucle (accueil + prologue), rotation aléatoire des 3 boucles Landes en jeu (enchaînement via `ended`), fondu de sortie au changement, autoplay débloqué au premier `pointerdown` (`armAudio`), 404 mémorisés → silence propre si fichiers absents. **MP3 INTÉGRÉS (v1.14.1)** : Patrick a fourni les 4 fichiers en PJ → `public/audio/` (320 kbps ; Landes_1 et Landes_3 = même taille mais md5 DIFFÉRENTS, pas des doublons). Vérifié Playwright : intro au 1er geste sur l'accueil, bascule en fondu vers landes_* à l'entrée en jeu. `Settings.music`/`musicVolume` (défauts true/0.7) ; Options : interrupteur à glissière RÉEL (pavé qui change de côté + orange) + volume à 8 crans (jamais un dégradé lisse) → `syncMusicSettings()`. Montages : `Home`/`Prologue` → `playMusic("intro")`, `Scene` → `playMusic("landes")`. Vérifié Playwright (toggle + volume persistés, 404 gérés). ⚠️ Ne PAS précacher les mp3 dans APP_SHELL du SW tant que la politique de poids PWA n'est pas tranchée (12 Mo × 4).
- **Chantier 5 — Profondeur par scène (v1.12.0)** : 9 lieux + 2 rencontres scindés en SÉQUENCE via `chainNext` — écran d'arrivée sensorielle (garde l'id du POOL : orientation/visited/chapitre/soupconOnArrival/loot d'arrivée) avec 1-2 examens optionnels (passif de lore + risqué) + un choix « continuer », puis écran événement « -2 » (l'ancien contenu). Scindés : colline (écharde → `grantsLoot` sur le choix au sommet, plus d'auto-loot), pendu-qui-parle (foe sur -2), champ-des-fixes (carnet → grantsLoot sur aider-fossoyeur), serment-hameau, campement/moulin (rest sur -2), chapelle, petit-tribunal (registre sur -2), marche-muet, puits-condamne (**timed sur -2** — l'arrivée n'est pas chronométrée, l'approche déclenche), chien-du-bailli (combat+foe+bannière sur -2), palissade-sud. Nouveaux examens = ~22 textes (lore Bailli/Fille/palissade cohérent avec les chapitres). Règles mécaniques : les états ne vieillissent qu'en quittant un lieu COMPLET (`!scene.chainNext`), le `from` des liaisons est normalisé (suffixe -2 retiré) pour le matching des variantes, exclusion soin `campement*`. Vérifié Playwright : arrivée→examen passif→événement→sortie-liaison, soupçon d'arrivée, visited correct, écharde présente au sommet.

### Session 2026-07-24 (suite #4, Claude Code, opus) — playtest Patrick + retours 2 vagues (immersion + fidélité)
Patrick a fait tester le jeu. Deux vagues de retours, chacune déployée.
- **v1.15.0 — Immersion** : (1) **traversée ×3** (`freshTraversal` target 3-4 → **9-11 lieux** avant la Descente ; le pool en compte 15, les garanties chapitre+colline tiennent) ; (2) **vraies transitions** — la MARCHE a son visuel (`makeLiaison` pose `illustration` = une des 4 vues génériques `scene_lande_generique_1..4` seedées, fini le portail figé entre deux lieux) + **phrase d'APPROCHE** (`APPROACH_NARRATION` par lieu, poussée dans `advance` quand `opts.toDest` : on voit le lieu se dresser et on y marche) ; (3) **image qui s'adapte au beat** — le personnage qui apparaît a son visuel sur l'écran-événement `-2` : chemin-creux→`monstre_marcheur_a_rebours_d`, serment-hameau-2→`monstre_doyenne_b`, marche-muet-2→`monstre_colporteur_b`, petit-tribunal-2→`monstre_ecrivain_public_d`, puits-condamne-2→`monstre_mains_du_puits_a` ; palissade-sud sort du portail (générique). Les 4 génériques récupérées du Drive (`PACTUM/Photos/.../2_Environnement`, dossier des `_1..4`) via download_file_content → extraction transcript.
- **v1.15.1 — Fidélité maquettes + correctifs** : (1) **écran du Nom refait FIDÈLE à la maquette Figma 2167:203** — champ bordé simple « Ton Nom » (mono, aligné à gauche, entailles de coins comme les CTA) + bouton **plein** SCELLER LE PACTE pleine largeur + lien centré souligné. **Retirés** (incohérences signalées par Patrick) : la signature Instrument Serif 32px, le trait d'inscription tramé, la note « Ce nom entrera au Registre », les segments décalés du bouton. NB : la note Registre disparaît de l'UI — la modération des noms reste à trancher AVANT le Registre agrégé, indépendamment de l'affichage. (2) **Prologue amorce/clôture** : texte **centré largeur 300px** (revient à la ligne, maquette 1997:523) — les souvenirs et le Nom restent alignés à gauche ; **« Touche pour continuer »** en bas opacité 50 %, **toujours affiché** sur les écrans d'amorce (le gate « 3 premières runs » est retiré — Patrick le veut visible). (3) **Musique dès l'ouverture** : `playMusic` TENTE `play()` immédiatement (repli `armAudio` sur 1er geste si le navigateur bloque encore l'autoplay — limite navigateur incompressible hors PWA installée). Rotation des 3 boucles Landes via `ended` inchangée (vérifiée : 1 boucle aléatoire à l'entrée en jeu, enchaînement à la fin). (4) **OPTIONS cliquable sur l'accueil** : `OptionsTab` exporté de GameMenu, rendu dans un overlay plein cadre `Home` (« options ») aux côtés de Reliques/Registre.
- ⚠️ Piège de test relevé : `waitUntil: "networkidle"` ne se stabilise JAMAIS quand l'audio joue (mp3 en flux) — utiliser `"domcontentloaded"`. Autoplay Playwright : lancer Chromium avec `--autoplay-policy=no-user-gesture-required` pour vérifier le lancement sans clic.
- Vérifié Playwright (les deux vagues) : target 10, image de marche générique en liaison, approche « les mâts deviennent des potences », image marché→Colporteur ; musique au chargement sans clic, Options ouvre, champ Ton Nom + bouton pleine largeur + lien sans note, texte amorce 300px centré, Touche pour continuer. Lint/build propres.
- APP_VERSION 1.15.0 puis 1.15.1, CACHE_VERSION v25 puis v26.

### Session 2026-07-24 (suite #5, Claude Code, opus) — lot Notion « 24/07 (suite) — Profondeur des scènes » (3e playtest)
Deux sources : la section de spec « 🗓️ 24 juillet 2026 (suite) — Profondeur des scènes : décisions du 3ᵉ playtest » et la page « 🌾 Les Landes — Scripts » (contenu validé par défaut). Livré en vagues.

**Diagnostic acté par la spec** : le problème n'est PAS la longueur des beats, c'est la **LINÉARITÉ**. Un lieu était un nœud qu'on traversait ; il devient un ESPACE qu'on parcourt.

- **v1.16.0 — vague 1 : POINTS D'INTÉRÊT** (`PointInteret` dans `lib/scene-data.ts`). Grammaire imposée : **voir de loin → marcher → toucher**. On ne se téléporte jamais sur un point.
  - Type : `{ id, label (ce qu'on voit de loin), approche (beat de MARCHE, 2-3 phrases), examen (plan rapproché), zoom (défaut 2.2), focus (object-position), soupcon?, grantsLoot?, setsEnvFlag? }`. `Scene.pointsInteret?: PointInteret[]`.
  - **Le « plan rapproché » = un crop CSS de l'illustration du lieu** (`transform: scale()` + `transformOrigin` = `focus`, transition `steps(4)`) — pas un nouvel asset à produire. Un POI consomme un tour d'écran mais **ne consomme pas le choix** : après examen on revient à la même scène, le point disparaît de la liste (`RunState.poiSeen`), les autres restent ouverts.
  - Les POI sont rendus **en tête** des choix (`poiChoices` avant `shuffledChoices`) : ce qu'on voit vient avant ce qu'on décide.
  - 4 lieux garantis réécrits d'après les scripts Notion, 3 points chacun : Colline aux Gibets, Petit Tribunal, Chapelle des Cordes, Champ des Fixés.
  - ⚠️ React Compiler : ajouter du code dans `advance()`/`onSelect()` a fait remonter 3 erreurs de pureté (`Math.random` ×2, `Date.now`) — extraites en helpers de MODULE (`chance(p)`, `nowMs()`), et `grantLandesLoot` remontée APRÈS `persist`. Réflexe : toute impureté atteignable depuis le corps du composant doit vivre hors du composant.
- **v1.17.0 — vague 2 : LE HAMEAU = halte scriptée HORS TIRAGE** (spec §3, verrouillé). « On ne visite pas le Hameau : on y fait halte. » Deux séquences garanties encadrent la traversée, aucune n'est dans le pool de lieux.
  - **ENTRÉE, 5 beats** (remplace les 2 anciennes scènes `serment-hameau`) : `serment-hameau` (approche au loin — « aucun chien n'aboie ») → `hameau-entree-2` (seuil, POI croix à la craie) → `hameau-entree-3` (barrage des trois hommes) → `hameau-entree-4` (**le Serment — imposé, jamais proposé**, POI « pourquoi trois aubes ») → `hameau-entree-5` (`hameauEntree: true`).
  - **HALTE, 5 beats** jouée à la fin de traversée AVANT la sortie de zone : `hameau-halte-1` (le vieux te trouve : « on sait toujours où tu es ») → `-2` (la grange, la barre posée DEHORS, POI bâtons de comptage) → `-3` (la nuit) → `-4` (l'aube, −1 Soupçon si le Serment a tenu) → `-5` (escorte au portillon, `hameauHalte: true`). Variante **`hameau-halte-dehors`** en un seul beat si le Serment a été REFUSÉ : aucune porte ne s'ouvre à qui n'a pas juré.
  - État : `RunState.hameau { entree, serment: "jure"|"faux"|"refuse"|null, halte }` + `Choice.serment` + `Scene.hameauEntree`/`hameauHalte`. Jurer = −1 Soupçon ; jurer du bout des lèvres = +1 et **dette différée** `serment-creux` (§17) ; refuser = +1 et nuit dehors.
  - Routage dans `advance()` — **l'ordre des branches compte** : soupcon≥6 (procès) → `opts.toDest` → `scene.hameauHalte` (→ Descente) → `scene.chainNext` → `visited >= target` (→ Halte si entrée faite et halte pas faite, sinon Descente) → nouvelle liaison.
  - ⚠️ Pièges de test rencontrés (les deux étaient du SETUP, pas du code) : (1) un locator `hasText: "Jurer"` matche aussi « Refuser de jurer » et « Jurer du bout des lèvres » — et les choix sont mélangés → utiliser une regex ancrée `/^Jurer$/` ; (2) pour atteindre la branche `visited >= target`, la scène courante seedée ne doit PAS avoir de `chainNext` (sinon la branche chaîne passe avant) → seeder `marche-muet-2`, pas `marche-muet`.
  - Vérifié Playwright : les 5 beats d'Entrée, les 2 POI, `serment: "jure"` persisté, `hameau.entree`, déclenchement de la Halte en fin de traversée, variante « nuit dehors » sur Serment refusé, sortie vers la Descente avec `hameau.halte: true`. Aucune erreur JS. Lint + build propres.
- **RESTE du lot (vagues suivantes)** :
  - **Vague 3 — rencontres au format beats obligatoires** (approche → échange → enjeu → résolution) : Femme au Seuil (étalon fourni par les scripts), Hésitant, Époux du Verger, Marcheur à rebours, Veilleur de la Palissade ; + points d'intérêt sur les lieux extérieurs (Moulin, Mare, Borne, Verger, Chemin Creux, Palissade Sud).
  - **Vague 4 — les 18 liaisons ÉCRITES** de la page Scripts (remplacent le pool générique actuel) et **les 4 chapitres réécrits** depuis les Scripts (le texte actuel de `chapters-data.ts` est de moi, pas de Patrick — à remplacer, ça résout au passage la réconciliation du « Procès » en attente depuis le 23/07).
  - **Images** : la page Scripts contient 10 + 4 prompts Leonardo pour le pipeline de Patrick (`hameau_approche`, `hameau_ruelle`, `hameau_grange`, `colline_cercle`, `tribunal`…). Ces assets n'existent pas encore — les scènes du Hameau retombent sur les vues génériques des Landes en attendant, à recâbler à réception.

#### Vague 3 (v1.18.0) — lieux extérieurs à points d'intérêt + rencontres en beats
- **`PointInteret.leadsTo`** (nouveau) + **`advance({ toScene })`** : un point d'intérêt peut OUVRIR sur une rencontre (notation « PI 3 — les Époux → rencontre » des scripts). L'approche et l'examen se jouent normalement, puis l'écran bascule sur le premier beat ; la rencontre rejoint ensuite le lieu par son `chainNext` (l'écran-événement `-2`). `toScene` n'entre PAS dans `trav.visited` — une rencontre n'est pas un lieu traversé (vérifié).
- **Format des rencontres** : approche → échange → enjeu → résolution. **Le beat d'APPROCHE est porté par le point d'intérêt** (voir de loin → marcher → être à hauteur) : c'est ce qui rend la rencontre refusable sans ajouter un choix mort « passer au large » — il suffit de ne pas choisir le point. Décision documentée dans le code.
- **5 rencontres écrites** (textes des scripts) : `femme-seuil-1..3` (étalon, ouverte depuis `hameau-entree-2`, rejoint `hameau-entree-3` — une rencontre ne fait jamais dérailler la séquence garantie), `hesitant-1..3` (Borne), `marcheur-1..3` (Chemin Creux, rejoint `chemin-creux-2`), `epoux-1..3` (Verger), `veilleur-1..3` (Palissade, rejoint `palissade-sud-2`).
- **4 lieux passés au format à points** : `borne-frontiere` (gravures / éclat descellé → Pierre de Retour / l'homme immobile), `chemin-creux` (charrette → Grelot / talus / le Marcheur) + nouvel écran-événement `chemin-creux-2` (le coude aveugle), `campement` = Moulin (croix d'ombres — **zoom 0.9, le seul point où il faut RECULER** pour voir / lucarne / intérieur), `palissade-sud` (rondins aux pointes tournées vers l'intérieur / portillon griffé / le Veilleur).
- **2 lieux ajoutés au pool** (17 destinations désormais) : `mare-aux-regards` + `-2` (berge usée, le reflet en retard d'une demi-seconde, miroir fêlé dans les roseaux ; événement = un Renonçant qui vient vérifier) et `verger-noir` + `-2` (fruits de cendre, souche du premier arbre, les Époux ; événement = le fruit qui tombe encore chaud). Entrées ajoutées à `APPROACH` **et** `APPROACH_NARRATION` (les deux sont nécessaires : `APPROACH` définit le pool, `APPROACH_NARRATION` la phrase d'arrivée).
- **Correctif** : un choix **passif** peut maintenant donner un objet. `grantsLoot` n'était honoré que dans `onComplete` (résolution d'un jet) — or les scripts prévoient des dons SANS jet (« Raconter ta mort — pas de jet », accepter la mèche). Le don passe par `grantLandesLoot` (qui persiste) et `advance({ grantedItem })` (qui ne fait que le bandeau) : pas de double ajout.
- **5 objets** ajoutés à `LANDES_OBJETS` : grelot-charretier, pierre-retour, miroir-poche, fruit-cendre, meche-nouee (`passiveMod: 0` — elle ne pèse rien, c'est la promesse qui pèse).
- ⚠️ **Piège de test** : sous Chromium, `innerText` applique `text-transform` — le bandeau « Obtenu » est en uppercase, donc comparer en minuscules pour chercher un nom d'objet. Deux assertions ont paru fausses pour cette seule raison (l'objet était bien en Besace).
- Vérifié Playwright : POIs des 4 lieux + zoom appliqué (matrix 2.6) + point consommé, POI mémorisé à la reprise, les 5 rencontres de bout en bout, retour à l'écran-événement du lieu, `visited` inchangé par une rencontre, loot du Veilleur et de la Femme. Aucune erreur JS. Lint + build propres.
- **RESTE (vague 4)** : les **18 liaisons écrites** de la page Scripts (remplacent le pool générique) et les **4 chapitres réécrits** depuis les Scripts — le texte actuel de `chapters-data.ts` est de moi, le remplacer résout au passage la réconciliation du « Procès » en attente depuis le 23/07. Les images nommées dans les scripts (`hameau_approche`, `hameau_ruelle`, `hameau_grange`, `colline_cercle`, `tribunal`, `moulin`, `mare`, `borne`, `verger`…) n'existent toujours pas : fallback sur les vues génériques des Landes.

### Session 2026-07-25 (Claude Code, opus) — re-vérification complète des illustrations Drive (v1.18.1)
Demande Patrick : nouvelles photos déposées dans le Drive d'après les scripts (`scene_hameau_dense2_b` = « la principale, celle qui représente vraiment le hameau » ; `scene_hameau_dense_b/_c/_d` = « génériques entre des scènes » ; `scene_maison_du_bailli_c` mis à jour) + « globalement revérifie toutes les images, objet, rencontres et environnement, il y a eu des maj ».
- **Résultat de la re-vérification** (comparaison octet pour octet des 3 dossiers `03_Validé` contre `public/assets`) : **Objets = 0 nouveau, 0 modifié** ; **Rencontres = 9 nouveaux** ; **Environnement = 24 nouveaux** ; **aucune image déjà en jeu n'a changé de contenu**, à une exception : `scene_champ_des_fixes_c` (local) est remplacé par `scene_champ_des_fixes_b` (Drive). Donc les « maj » = des AJOUTS, pas des remplacements silencieux — rien de périmé ne traînait en jeu.
- **14 assets importés et câblés** : hameau vu d'ensemble (`scene_hameau_dense2_b` → `serment-hameau`, l'approche au loin) ; ruelle (`scene_landes_hameau_ruelle_b` → `hameau-entree-2`, `-5`, `hameau-halte-1`) ; grange (`scene_landes_hameau_grange_a` → `hameau-halte-2`, `-3`) ; maison murée (`scene_maison_du_bailli_c` → `chien-du-bailli`, le chien reste sur l'écran-événement `-2`) ; `scene_chemin_creux_c` (chemin-creux + `-2`) ; `scene_champ_des_fixes_b` ; `monstre_epoux_verger_a` (epoux-1/2/3) ; `monstre_fossoyeur_poteaux_a` (champ-des-fixes-2) et `monstre_veuve_cordes_a` (chapelle-des-cordes-2) — ces deux-là suivent la règle du 24/07 « le personnage qui apparaît a son visuel sur l'écran-événement », leur narration EST celle du personnage.
- **Vue de marche contextuelle** (`pickWalkImage` dans scene-data) : la liaison ne tire plus au hasard dans les 4 génériques. Si une des deux directions offertes est le hameau → une des 3 `scene_hameau_dense_*` (le village grossit à l'horizon, exactement l'usage demandé par Patrick) ; le Chemin Creux → `scene_landes_liaison_chemin_creux_a` ; la Palissade Sud → `scene_landes_liaison_sud_c` ; sinon pool `LANDES_WALK` (plateau, fourche, arbres morts + les 4 génériques). Toujours seedé, donc stable à la reprise.
- **~17 assets du Drive identifiés mais VOLONTAIREMENT pas importés** : ils illustrent des scènes/chapitres pas encore écrits (8 `scene_transition_*`, `scene_tour_de_guet_a`, `scene_gibet_vide_c`, `scene_landes_tribunal_b`, `monstre_la_fille_c`, `monstre_ombre_grand_gibet_a`, `monstre_deserteur_tribunal_a`, `monstre_gamin_murets_a`, `monstre_vent_qui_ment_b/_d`). Leurs fileIds + tailles sont conservés dans `scratchpad/drive_manifest.json` — à importer avec la vague 4 (liaisons écrites, chapitres réécrits) où plusieurs d'entre eux trouveront leur place (`scene_transition_*` pour les 18 liaisons, `monstre_la_fille_c`/`scene_gibet_vide_c` pour les chapitres).
- ⚠️ **À signaler à Patrick** : un doublon `monstre_petite_fixee_c copie.png` (13 622 o) traîne dans `03_Validé/1_Rencontres` — à supprimer côté Drive (aucun outil de suppression via le connecteur, et la consigne reste lecture seule sur le Drive).
- Technique de copie inchangée (éprouvée depuis le 24/07) : `download_file_content` par petits lots → `scratchpad/extract_drive.py` lit le base64 par titre dans le transcript `.jsonl`, décode, vérifie signature PNG **et taille exacte** annoncée par le Drive. Zéro retranscription de b64 par le modèle. 17/17 fichiers écrits sans erreur. Les 14 câblés vérifiés 2 couleurs exactes (Charbon/Orange) et 1000×1000.
- `landes.json` (matière de production, pas lu au runtime) synchronisé sur les mêmes 7 ids ; script de contrôle : toutes ses références `illustration` existent sur disque, et les 44 références de `scene-data.ts` aussi.
- `APP_VERSION` 1.18.0 → **1.18.1**, `CACHE_VERSION` v29 → **v30**.

### Session 2026-07-25 (suite, Claude Code, sonnet) — re-vérification Drive 03_Validé (2e passe, zone Landes uniquement)
Demande Patrick : re-pull, comparer les trois dossiers `03_Validé_{Environnement,Rencontres,Objets}` du Drive (mélangent maintenant les assets de la zone donjon obsolète — `scene_donjon_*`, `monstre_loup_squelette`… — et les Landes) contre `public/assets/`, copier ce qui est pertinent pour Les Landes, mapper aux scènes/rencontres/lieux, et rendre un récapitulatif de ce qui est câblé vs orphelin. Contrainte : Drive en lecture seule, dithering jamais retouché.
- `git pull` fait en tout début de session (rien à rebaser, aucune modif locale non commitée avant de commencer).
- **21 fichiers pertinents identifiés et récupérés** (technique de copie inchangée depuis le 24/07 : `download_file_content` par lots de ≤6 → script `extract_drive2.py` lit le b64 par titre dans le transcript `.jsonl`, décode, vérifie signature PNG **et taille exacte** annoncée par le Drive — zéro retranscription de b64 par le modèle). 21/21 écrits sans erreur, tailles exactes confirmées. Dossier Objets confirmé sans changement (0 nouveau/modifié), comme annoncé par Patrick. Les 20 fichiers `donjon_*`/`caverne_*`/etc. de la zone obsolète explicitement exclus (liste dans `scratchpad/drive_manifest2.json`, clé `exclus_donjon`).
- **2 images DÉJÀ EN JEU mises à jour** (mêmes noms de fichier, contenu remplacé — aucun changement de code nécessaire, le refresh est automatique) : `monstre_meute_grise_c.png` (32 950 o, nouvelle version « 6 chiens à contre-jour d'un soleil », remplace l'ancienne 25 522 o, câblée sur `meute-grise-1`/`meute-grise-2`) et `monstre_petite_fixee_c.png` (13 622 o, dédupliquée par Patrick — un seul fichier canonique maintenant côté Drive — remplace l'ancienne 22 629 o ; reste un ORPHELIN côté code, aucune scène « petite fixée » n'existe encore).
- **13 fichiers nouveaux, volontairement PAS câblés** (vérifiés présents sur disque, prêts pour de futures sessions) :
  - `monstre_pendu_mal_fixe_v1_b.png` — relation ambiguë avec `monstre_pendu_mal_fixe_a.png` déjà câblé sur la scène `pendu-mal-fixe` ; pas de signal permettant de trancher variante vs remplacement → laissé de côté plutôt que deviné, à valider par Patrick.
  - `scene_landes_hameau_croix_craie_a.png` / `scene_landes_hameau_puits_b.png` — l'architecture actuelle des points d'intérêt (POI, session 24/07 vague 4) fait ses « plans rapprochés » par crop CSS de l'illustration du LIEU, pas par asset dédié par point — donc pas de slot évident où les poser sans redéfinir cette règle. `_puits_b` en particulier est ambigu : remplace `scene_puits_condamne_c.png` déjà en jeu, ou vue distincte du Puits vu depuis le Hameau ?
  - `monstre_ombre_grand_gibet_a.png`, `monstre_deserteur_tribunal_a.png` — rencontres pas encore écrites dans `scene-data.ts`.
  - `monstre_vent_qui_ment_b.png` / `_d.png` — la mécanique « Vent qui ment » (leurre sensoriel sur les orientations, notée le 21/07) n'est toujours pas implémentée.
  - `monstre_gamin_murets_a.png`, `monstre_la_fille_c.png`, `scene_gibet_vide_c.png`, `scene_landes_tribunal_b.png` — liés aux 4 chapitres du Bailli, dont le texte actuel (`chapters-data.ts`) reste provisoire (écrit par Claude Code, pas par Patrick) ; ce fichier n'a de toute façon pas de champ `illustration` — pas de slot où les poser sans construire la vague 4 (chapitres réécrits depuis les Scripts Notion, en attente depuis le 24/07).
  - 6× `scene_transition_*` (`arrivee_hameau_b`, `crete_cordes_b`, `chemin_murets_b`, `croisee_sud_b`, `croix_craie_c`, `hommes_suivent_b`, `vols_basses_b`) — matière pour le pool des 18 liaisons ÉCRITES de la page Scripts Notion, toujours pas implémenté (le système actuel de liaisons contextuelles, session 24/07 vague 4, génère son texte par variantes plutôt que par les 18 entrées figées).
- `data/zones/landes.json` (matière de production, pas lu au runtime) inchangé cette passe — les 2 fichiers mis à jour y étaient déjà référencés par les mêmes noms, donc déjà à jour automatiquement ; aucun des 13 nouveaux n'y correspond à une entrée `illustration` vide qui appellerait une mise à jour immédiate.
- Build + lint vérifiés propres (`next build` avec `PAGES_BASE_PATH=/Pat/aldenhar`, `npm run lint` — seul le warning police préexistant). `APP_VERSION` 1.18.1 → **1.18.2**, `CACHE_VERSION` v30 → **v31**.
- **Récapitulatif demandé par Patrick** : câblé = 2 refreshs silencieux (meute grise, petite fixée) sur assets déjà en jeu, zéro changement de code. Orphelins = les 13 fichiers ci-dessus, tous conservés sur disque et documentés ici avec la raison précise de chaque blocage (variante ambiguë, pas de slot d'architecture, contenu pas encore écrit, ou mécanique pas encore codée) — à reprendre dès que la vague 4 (chapitres + 18 liaisons) ou une clarification de Patrick lève le blocage.

### Session 2026-07-25 (suite #2, Claude Code, opus) — retours playtest 25/07 (5 points, v1.19.0)
Cinq retours après déploiement. Tout est livré sauf une exception d'écran signalée en fin de liste.
- **1. Bande de dissolution (`bande_dissolution_haut.svg`)** : la version en jeu était **procédurale** (1170×260, générée le 13/07 faute d'un asset source lisible) et **étirée** en 390×42 par `background-size: 100% 100%`. Deux conséquences : cellules de trame déformées, et surtout un profil de densité beaucoup trop lourd (**solide sur 20 % de sa hauteur**, soit ~8 px de noir plein au bas de l'image) — d'où le « dégradé noir » que Patrick voyait. Le vrai composant Figma (**node 2225:544**, qui était sélectionné dans son fichier) a été **reconstruit vecteur par vecteur depuis `get_metadata`** : 3064 pavés de 2×0,95 px, 390×41,27, **plein sur ~4 px (10 %) puis dissolution jusqu'à 1 %**. CSS : plus d'étirement, `background-size: 390px 41px` + `repeat-x` → pleine largeur sur n'importe quel écran sans une seule cellule déformée. Vérifié : bande x=0 w=390 = largeur exacte de l'image, en état normal ET compact (texte long).
  - ⚠️ **Écart assumé à faire trancher** : dans la maquette, la bande est posée à y=226, c'est-à-dire **sous** l'image (bord plein en haut, dents vers le bas) — donc du charbon sur du charbon, invisible. En jeu elle est gardée **retournée et superposée au bas de l'illustration** (bord plein collé au bord bas, trame qui monte dans l'image) : c'est le seul sens où la dissolution se voit, puisqu'elle ne lit que sur l'orange. Si Patrick voulait l'autre sens, c'est un `scaleY` à retirer.
- **2. Géographie du Hameau** — l'incohérence « je me dirige vers le hameau alors que j'y suis déjà » avait une cause simple : les lieux **intérieurs** au village étaient dans le MÊME pool plat que la lande, à côté de la séquence d'arrivée. On pouvait donc être envoyé à la Chapelle sans être entré, puis se voir offrir « Vers la fumée d'un hameau ».
  - Relevé sur la **carte Figma 2112:325** : le cadre pointillé « Le Hameau des Renonçants » va de (810,620) à (1790,1280) et contient **6 lieux** — Chapelle des Cordes, Maison du Bailli, Petit Tribunal, Puits Condamné, Marché Muet, Tour de Guet (cette dernière pas encore écrite). Patrick avait raison sur la Chapelle.
  - Implémenté : `HAMEAU_INTERIOR` + `isHameauInterior()` dans `scene-data.ts`. `pickLiaisonOptions(visited, seed, hameauEntree)` **ferme l'intérieur tant qu'on n'est pas entré** et **retire la séquence d'arrivée une fois entré**. La garantie de chapitre respecte la porte (2 des 4 chapitres se jouent à l'intérieur — les forcer avant rouvrait le bug). Pas de garantie ajoutée sur l'entrée elle-même : 12 destinations extérieures restent tirables pour une cible de 9-11, le hameau tombe donc presque toujours, et le code gérait déjà la sortie de zone sans Halte.
  - **Image de marche + texte dans le village** : `pickWalkImage` prend une vue `scene_hameau_dense_*` dès qu'on **vient** d'un lieu intérieur ou qu'on y va (avant, seule la destination « serment-hameau » déclenchait ça — d'où la vue générique des Landes que Patrick a vue). 3 ambiances de liaison intérieures ajoutées (« tu longes les murets d'une ruelle à l'autre… ») : on ne « marche plus dans la lande » à l'intérieur d'un village.
- **3. Images à produire** : liste complète livrée dans **`data/images-a-produire.md`** — **32 prompts autonomes** (bloc de style inclus, rien à assembler), classés en 4 priorités, chacun avec son nom de fichier et son **raccord** (ce que le plan rapproché doit partager avec l'image large : matière, source de lumière, cadrage).
  - Câblage prêt : nouveau champ **`PointInteret.illustration`**. Rempli → l'image dédiée remplace le crop CSS (et le zoom est neutralisé pour ne pas re-zoomer un asset déjà cadré) ; vide → le crop actuel continue de servir. Donc la liste peut être produite au compte-gouttes sans rien casser.
  - ⚠️ **Les 12 images de lieu ont été REGARDÉES avant d'écrire les prompts** (et ça valait le coup, les raccords étaient faux sans ça) : le Petit Tribunal est un intérieur de **pierre avec une arche rétroéclairée**, pas du bois ; la Chapelle a ses **cordes qui pendent verticalement du plafond au sol** le long des murs ; la Colline est une **ligne** de potences en contre-jour sur une crête (le texte dit « le cercle ») ; la grange n'est éclairée que par **une lanterne posée au sol**. Tout le lot existant est en contre-jour extrême, silhouettes quasi noires sur ciel orange — les prompts le disent explicitement.
  - **3 problèmes trouvés dans les assets en jeu** (priorité 0 du fichier) : (1) **`scene_moulin_sans_ailes_c.png` montre un moulin AVEC ses ailes**, alors que le lieu s'appelle « le Moulin sans Ailes » et que son point d'intérêt (« la croix d'ombres ») repose entièrement sur leur disparition — l'image contredit le texte ; (2) **`palissade-sud` n'a aucune image de lieu** (vue générique) alors que c'est le seuil de l'Acte II ; (3) `borne-frontiere` utilise une image de **transition**, aussi présente dans le pool des vues de marche.
  - **4 plans rapprochés câblés immédiatement, sans rien produire** : l'Hésitant (`monstre_hesitant_b`), la Femme au Seuil (`monstre_femme_au_seuil_b`), le Marcheur à rebours (`monstre_marcheur_a_rebours_d`) et les Époux du Verger (`monstre_epoux_verger_a`) avaient déjà leur portrait validé en assets. Leurs points d'intérêt l'utilisent maintenant — et comme c'est la même image que la rencontre qui suit, le plan rapproché et la rencontre raccordent d'eux-mêmes. Vérifié : l'écran passe bien du plan large du lieu au portrait, sans re-zoom.
- **4. Musique** : la rotation tirait un `Math.random()` à chaque entrée en jeu — 1 chance sur 3 de retomber sur la même piste, et rien n'empêchait deux runs identiques d'affilée. Remplacé par une **file mélangée** (`buildQueue`) qui ne **redémarre jamais sur la piste entendue la fois précédente** (mémorisée dans `localStorage`) et qui se renouvelle en fin de file sans doublon d'enchaînement.
  - ⚠️ **Bug attrapé au test, à ne pas refaire** : une seule clé `pactum-music-last` ne marche PAS — l'accueil joue l'intro juste avant que le jeu demande les Landes, donc la « dernière piste » valait toujours `intro.mp3` et n'évitait jamais de rejouer la même boucle. Mesuré : `landes_1, landes_1, landes_3, landes_3, landes_3, landes_3` sur 6 lancements. La mémoire est donc **par contexte** (`pactum-music-last-{intro|landes}`) → re-mesuré `3, 2, 1, 2, 1, 3` : les 3 boucles, jamais deux fois de suite. Les 4 mp3 sont bien en place et distincts (md5 différents ; `landes_1` et `landes_3` ont la même taille mais pas le même contenu). Déverrouillage autoplay élargi : le premier geste **quel qu'il soit** (`pointerdown`/`touchstart`/`touchend`/`mousedown`/`keydown`/`wheel`, en **capture**) lance le son, au lieu du seul `pointerdown` — donc un simple contact avec l'écran suffit, sans viser un bouton. Un `play()` refusé **ré-arme** proprement (avant, un échec consommait l'écouteur et la run restait muette), et l'écouteur est gardé au niveau module pour ne jamais s'empiler.
  - ⚠️ **Limite de plateforme, pas un bug** : aucun navigateur ne joue un son **avant** un geste utilisateur. Le silence entre l'ouverture de l'app et le premier contact est incompressible hors PWA installée (où l'autoplay est souvent déjà autorisé — `playMusic` tente toujours de jouer d'emblée).
- **5. Trois choix par écran** : les points d'intérêt étaient listés **à côté** des actions du lieu (jusqu'à 6 boutons sur la Borne). Ils passent derrière un seul CTA **« Observer les alentours »** qui ouvre un sous-menu (descriptions + « Ne rien regarder de plus »). Ouvrir/fermer ne consomme ni tour, ni dé, ni changement d'image — c'est de l'affichage pur, et le sous-menu se referme à chaque nouvel écran.
  - Règle complémentaire : quand des descriptions restent ouvertes, les choix **passifs** de la scène (regarder/écouter/ne rien faire — pas de jet, pas de stat) **descendent dans le sous-menu** ; le niveau supérieur ne garde que les actes. Même registre contemplatif, et ça ramène la Borne à 3 CTA pile.
  - **Garde-fou important** : un passif ne descend que s'il RESTE un acte en haut, et **jamais un serment**. Sans ça l'écran du Serment (dont les trois serments sont tous passifs — jurer n'est pas un jet) se retrouvait vidé, avec le choix le plus lourd de la zone caché derrière « Observer ».
  - Mesuré sur les 17 écrans de lieu : **16 sont à 3 CTA ou moins**. **Exception à trancher par Patrick : `hameau-entree-4` (le Serment) reste à 4** — « Observer les alentours » + les 3 serments. Impossible de descendre à 3 sans soit cacher un serment (exclu), soit sortir son unique point d'intérêt (« Demander pourquoi trois aubes », qui est en réalité une **question de dialogue**, pas une description de décor). Proposition si Patrick veut 3 partout : replier cette question dans la liste des serments et supprimer le point d'intérêt de cet écran.
- Vérifié en Playwright (build local) : bande pleine largeur en taille native (normal + compact), Borne à 3 CTA avec sous-menu à 5 entrées dont le passif, porte du Hameau dans les deux sens, vue de marche du village, rotation musicale variée. Lint + build propres. `APP_VERSION` 1.18.2 → **1.19.0**, `CACHE_VERSION` v31 → **v32**.
- ⚠️ **Piège de test à retenir** : cliquer à (195,300) pour accélérer la frappe tombe **dans l'illustration** (haute de 352 px), pas dans la zone de texte — le clic ne saute rien et le test expire en croyant que les CTA n'arrivent jamais. Attendre la fin de frappe avec `waitForFunction` sur `.choices-bar` est plus fiable que de cliquer. (Et rappel du 19/07, retombé dessus : `npx next build` lancé depuis `/home/user/Pat` échoue en « Couldn't find pages or app » et on teste alors l'ANCIEN `out/`.)

### Session 2026-07-25 (suite #3, Claude Code, opus) — 1er lot de plans rapprochés (v1.19.2)
Patrick a déposé **29 fichiers** dans `03_Validé_Environnement` (14h17), nommés d'après `data/images-a-produire.md`. ⚠️ Le pipeline **double le suffixe de variante** : `scene_borne_gravures_a` ressort en `scene_borne_gravures_a_d.png`. Noms conservés TELS QUELS pour que repo et Drive restent diffables.
- **⚠️ COÛT DU TRANSFERT — à savoir avant de relancer un gros lot** : `download_file_content` renvoie le base64 **inline dans le contexte** tant que le fichier fait moins de ~50 Ko de b64 (au-delà, le harnais le décharge dans un fichier de tool-results et le contexte est épargné). Nos PNG font 5-28 Ko → **~5 à 10 k tokens de contexte CHACUN**. 29 fichiers ≈ 200 k tokens : impossible en une session sans tout noyer. Options réelles : (1) Patrick fait un `cp` depuis son Drive synchronisé vers `aldenhar/public/assets/` et pousse (instantané, zéro contexte) ; (2) on tire par petits lots sur plusieurs tours ; (3) sous-agent dédié (interdit ici sans demande explicite). **La solution (1) est la bonne par défaut pour tout lot > ~5 fichiers.**
- **Priorité 0 traitée (3 fichiers tirés, 2 retenus)** :
  - ✅ `scene_borne_frontiere_a_c.png` câblée sur `borne-frontiere` — monolithe noir dominant, soleil bas à sa base. Le tout premier écran du jeu n'utilise plus une image de transition partagée avec le pool des marches.
  - ✅ `scene_palissade_sud_a_a.png` câblée sur `palissade-sud` — le lieu n'avait AUCUNE image propre. Réserve notée : tout le contenu est dans le tiers haut, la moitié basse est noire.
  - ❌ `scene_moulin_sans_ailes_d_d.png` **REJETÉE et supprimée du repo** : la génération a gardé les quatre ailes malgré « sail assembly completely gone ». Piège classique de diffusion — le mot *windmill* appelle les ailes, et on ne retire pas un élément sur commande. Prompt corrigé écrit dans `images-a-produire.md` : décrire **ce qui est là** (« squat round stone tower, no blades, no vanes, bare truncated cap, single broken iron stub ») sans jamais nommer un moulin.
- Les **26 plans rapprochés restants ne sont PAS câblés** : pointer `PointInteret.illustration` vers un fichier absent afficherait une image cassée, ce qui serait pire que le crop CSS actuel. Le câblage se fait dès que les fichiers sont dans `public/assets/`.
- `APP_VERSION` 1.19.1 → **1.19.2**, `CACHE_VERSION` v33 → **v34**.

### Session 2026-07-25 (suite #2, Claude Code) — plans rapprochés des points d'intérêt (v1.20.0)
Lot Leonardo « plans rapprochés » déposé dans le Drive `03_Validé/2_Environnement`. **29 fichiers récupérés via le connecteur Drive** (technique inchangée : `download_file_content` par lots de 1-3 → `scratchpad/extract_drive3.py` lit le b64 par titre dans le transcript `.jsonl`, décode, vérifie signature PNG **et taille exacte** annoncée par le Drive ; 29/29 OK). Manifeste + fileIds dans `scratchpad/drive_manifest3.json`.
- **Ce que ça change** : jusqu'ici un point d'intérêt faisait son « plan rapproché » par **crop CSS** de l'illustration du LIEU (`transform: scale()` + `transformOrigin`, session 24/07 vague 1). Le champ `PointInteret.illustration` (déjà prévu) court-circuite ce crop quand une image dédiée existe — `Scene.tsx` pose `setPoiZoom(poi.illustration ? null : {...})`. **26 des 28 points ont maintenant leur vraie image.**
- **Points câblés** : Borne (`gravures-borne`, `eclat-descelle`) · Chemin Creux (`charrette-embourbee`, `talus-empreintes`) · Colline aux Gibets (`potences-cercle`, `gibet-vide`) · Champ des Fixés (`les-rangees`, `poteaux-vierges`, `tombe-sans-poteau`) · Hameau (`croix-craie`, `examiner-grange`) · Moulin (`croix-ombres`, `lucarne-moulin`, `interieur-moulin`) · Chapelle (`mur-cordes`, `autel-renverse`, `ouvrage-tressage`) · Petit Tribunal (`mur-ordonnance`, `chaire-registre`, `les-bancs`) · Mare (`berge-usee`, `reflet-metal`) · Verger (`fruits-cendre`, `souche-premier-arbre`) · Palissade Sud (`rondins-pointes`, `portillon-verrou`).
- **Vue d'ensemble du Moulin remplacée** : `scene_moulin_sans_ailes_c.png` → `scene_moulin_sans_ailes_d_d.png` ~~(la version SANS ailes demandée en P0)~~ sur `campement` et `campement-2`, plus `landes.json`. ⚠️ **CETTE PARENTHÈSE ÉTAIT FAUSSE** (vérifiée le 10/08 en ouvrant le fichier) : `_d_d` a **quatre ailes complètes en croix**, comme `_c` avant elle. C'est cette ligne de journal, crue sur parole par les relectures suivantes, qui a fait survivre le défaut à quatre signalements — personne ne rouvrait l'image puisque le journal disait le correctif fait. **Règle qui en sort : ne jamais écrire dans le journal qu'un asset montre X sans l'avoir ouvert.**
- **2 points restent volontairement sur le crop CSS**, aucune image dans le lot : `poteau-pendu` (le Bailli pendu, vivant — attendu `scene_colline_poteau_pendu`) et `eau-reflet` (le reflet en retard d'une demi-seconde — attendu `scene_mare_eau`). À câbler dès réception, aucun code à changer.
- ⚠️ **Absent aussi du lot** : `monstre_veilleur_palissade` (préfixe `monstre_`, donc attendu dans `03_Validé/1_Rencontres`, pas Environnement) — le POI `homme-guerite` de la Palissade reste sans portrait dédié.
- Vérifié : les **71** références `assets/…` de `scene-data.ts` pointent toutes sur un fichier présent. Lint + build propres. Playwright (build servi en local, run seedée sur la Borne) : « Observer les alentours » → 4 points + sortie, le tap sur « Les gravures de la pierre » sert bien `assets/scene_borne_gravures_a_d.png` avec `transform: matrix(1,0,0,1,0,0)` (donc **aucun zoom CSS** — c'est bien l'image dédiée, pas un crop), aucune erreur JS.
- `APP_VERSION` 1.19.2 → **1.20.0**, `CACHE_VERSION` v34 → **v35**. Déployé sur gh-pages.
- ⚠️ Piège de test relevé : le script Playwright doit **cliquer REPRENDRE sur l'accueil** avant d'attendre `.choices-bar` — une run seedée en localStorage ouvre l'écran d'accueil, pas le jeu. Et `playwright-core` n'est pas dans `node_modules` du projet : il vit dans `/root/.npm/_npx/<hash>/node_modules/playwright-core`.

### Session 2026-07-26 (Claude Code, opus) — lot Notion 25/07 : Le Savoir + outil de couverture visuelle
Les deux sections du journal implémentées et déployées (v1.21.0 puis v1.22.0, `CACHE_VERSION` v36 puis v37).

**A. Le Savoir — rendre l'exploration payante**
- Socle : `RunState.savoirs: string[]` (vidé à la mort via `resetRun()`→`fresh()`, comme la Besace) + `RunState.fragmentsLus: number[]`. `PointInteret.savoir` pose le flag à l'examen ; `Choice.requiresSavoir` conditionne un choix. Le filtrage se fait **avant** le Fisher-Yates de `Scene.tsx` → une option débloquée prend une position quelconque, sans marqueur ni place réservée. Aucun chiffre, aucune stat.
- **Deux sources ajoutées au-delà de l'examen**, parce qu'apprendre ne passe pas que par une trace : `Scene.savoir` (la narration LIVRE l'info — le Marcheur à rebours dit le versant nord dès sa première réplique ; sans ça le joueur entendrait le conseil sans pouvoir s'en servir) et `Choice.grantsSavoir` (poser la question vaut lire une trace, acquis à la sélection comme le Soupçon).
- Les 7 Savoirs de la table câblés, **vérifiés bouclés dans les deux sens** (script : aucun requis sans source, aucun posé sans usage).
- **Deux écarts assumés avec la table, documentés dans le code** :
  - `savoir_reflet` devait ouvrir une option sur l'**Hésitant** beat 2. Impossible : l'Hésitant n'est joignable que par le POI `homme-immobile` de `borne-frontiere`, et la Borne **n'est pas dans `TRAVERSAL_POOL`** — c'est le premier écran de la run, donc toujours AVANT la Mare. L'option ne se serait jamais ouverte. Reporté sur la **Femme au Seuil** (même rôle : aveu réciproque, EMPATHIE forte ; et elle est atteignable après la Mare).
  - `savoir_grange_comptee` devait faire « gagner un cran » à *Écouter sans bouger*. **Contradiction interne au même journal** : la règle du Savoir interdit d'ajouter de la puissance (« il ajoute une option qui n'existait pas »). Arbitré pour la règle : aucun seuil abaissé, une option nouvelle (« Écouter entre deux rondes ») qui obtient l'information sans dé. Bénéfice identique pour le joueur.
- **Règle de dosage tenue** : les **34** points d'intérêt rendent tous une des quatre monnaies (17 ne rendaient rien avant). 4e monnaie créée : `Chapter.fragments` — 6 beats de lore par chapitre servis un par un par `PointInteret.chapterFragment`, qui épaississent l'histoire du Bailli **sans toucher** aux trois beats garantis (stage inchangé). Filet pour la règle « JAMAIS RIEN » : `LANDES_LORE_FRAGMENTS` (6 fragments de zone, indexés à partir de 1000 dans `fragmentsLus` pour ne pas collisionner) servis quand le chapitre est épuisé.
- 3 objets créés (Craie du Condamné, Jouet de la Petite Fixée, Clé du Portillon) — leurs icônes attendaient dans `assets/` depuis le 24/07.

**B. Outil de couverture visuelle** (`tools/coverage.py` → `data/couverture_visuelle.html`)
- `python3 tools/coverage.py` écrit le rapport ; `--serve [--port N]` ajoute l'édition (une page `file://` ne peut pas écrire sur le disque).
- **Croise 4 sources** : `lib/scene-data.ts` (image réellement affichée), `data/zones/*.json` (production), `data/scene-meta.json`, `public/assets/`. ⚠️ **Lit le .ts, pas seulement les JSON** : `landes.json` n'est pas lu au runtime, s'y fier ferait mentir la colonne « image affichée en jeu » — et les divergences entre les deux sont justement à voir.
- Une carte par scène ET par POI : statut **dédiée / héritée** (avec la scène source) **/ fallback de zone / manquante**, compteurs, filtres statut + catégorie, orphelins, « Copier le prompt Leonardo », journal de l'ordre d'écriture.
- ⚠️ **Piège de détection des orphelins** : ne regarder que les champs `illustration` des scènes produisait des orphelins fantômes (icônes d'objet référencées dans `besace.ts`, icônes génériques dans `GameMenu.tsx`/`Scene.tsx`, vues de marche câblées dans `pickWalkImage`). L'outil scanne donc **tout `lib/` et `components/`**. Après correction : 37 → 28 orphelins, tous documentés comme « en réserve ».
- **État de la zone au 26/07** : 58 scènes · 34 POI · dédiée 79 / héritée 6 / fallback 6 / **manquante 1** (`palissade-sud-2`, l'Appelé vu de dos) · 17 prompts à écrire · 28 orphelins.
- **Édition** : rattacher un asset déjà tramé, ou importer un fichier brut qui passe par `dither_batch.py` avant `assets/` (vérifié : l'import ressort en exactement 2 couleurs #1c1a16/#e0632a, aucun upscale). Chaque écriture met à jour `scene-data.ts` **et** les JSON de zone qui mentionnent la scène, avec journal des fichiers touchés.
- `data/scene-meta.json` : 58 descriptions FR + 41 `prompt_image` (recette v2 assemblée, fond noir pour les portraits / vignette sombre pour les scènes). Les 14 prompts des Scripts repris **verbatim**. **Fichier de production séparé de `lib/scene-data.ts` à dessein** : le jeu ne le charge jamais, ces textes n'ont pas à voyager dans le bundle du joueur. Prompt vide → « prompt à écrire » dans l'outil.
- **Mode debug en jeu** : triple tap sur l'icône de menu → id de scène + id d'image en 9px dans le coin bas gauche. Les taps sur la **croix de fermeture comptent dans la séquence** (elle occupe le même pixel que l'icône, alignement vérifié le 16/07) — sans ça le geste serait infaisable, puisque le 1er tap ouvre le menu. Non persisté.
- ~~La maquette `couverture_visuelle_maquette.html` n'est jamais arrivée dans une session Claude Code~~ — **reçue et confrontée le 26/07, voir plus bas.**
- Vérifié en Playwright : 92 cartes, filtres, 41 boutons de copie / 17 « prompt à écrire », vignettes en 200, rattachement écrivant une seule ligne au bon endroit, import dithéré, triple tap qui bascule l'overlay sans laisser le menu ouvert. Côté jeu : au coude du Chemin Creux, 2 choix sans le Savoir et 3 avec ; l'examen du talus pose le flag ; l'examen des potences sert un fragment et l'inscrit dans `fragmentsLus`. Aucune erreur JS. Lint + build propres.
- ⚠️ Pièges d'environnement rencontrés : `pkill`/`kill %1` **tuent le shell de l'outil Bash** (exit 144) — passer par `run_in_background` et un autre port plutôt que de tuer un serveur ; `npx serve -l PORT` ignore parfois le port demandé (préférer `python3 -m http.server`) ; un serveur `--serve` déjà lancé garde l'ANCIEN module en mémoire, il faut le relancer après chaque édition de `coverage.py`.
- **Reste du 25/07** : rien sur ces deux sections. Toujours en attente par ailleurs — les 18 liaisons écrites et les 4 chapitres réécrits depuis les Scripts (vague 4 du 24/07), `scene_colline_poteau_pendu` / `scene_mare_eau` / `monstre_veilleur_palissade` (3 plans rapprochés absents du lot du 25/07), et la vue de lieu de `palissade-sud-2`.

### Session 2026-07-26 (suite #4, Claude Code, opus) — écrans d'intro, écran d'acte, icônes d'objets (v1.23.0)
Lot du 26/07 : les 5 écrans tuto (Figma 2238:1009), l'écran d'acte (2245:13747), le lien de remise à zéro dans Options, et les icônes d'objets.
- **5 écrans d'intro** (`components/Intro.tsx`) : les 4 clauses du pacte + l'écran d'ouverture, textes repris VERBATIM de la maquette (c'est de la voix du Geôlier, pas de la copie d'UI). Mise en page relevée sur le Figma : illustration 390×390, bande de dissolution 42px, chapeau de clause y=406, titre Instrument Serif orange y=427, corps mono y=479, points + « Touche pour commencer » en pied. Points de progression = **carrés pleins** (jamais une barre — pas de jauge dans PACTUM).
- **Ne se joue qu'au tout PREMIER lancement** : drapeau `introSeen` dans la **mémoire du COMPTE** (`player-memory`), pas dans la run — le tuto énonce les règles du JEU, pas d'une partie, le rejouer à chaque mort serait une punition. Migration : une mémoire d'avant le 26/07 n'a pas le drapeau, on le déduit de `runsStarted > 0` pour ne pas infliger le tuto à un compte qui a déjà joué.
- **Écran d'acte** (`ActeScreen`, même fichier — grammaire visuelle identique) inséré **entre le Seuil et la première scène**, à chaque run neuve. Ciel orange plein, « • LE DOMAINE • » + titre en Instrument Serif **charbon** posés sur l'orange, frise en dessous, charbon jusqu'en bas. Table `ACTES` prête pour l'acte II (visuel annoncé par Patrick, pas encore fourni).
  - ⚠️ **Écart de nommage non tranché** : la maquette dit « **Les Lisières** », la zone s'appelle « **Les Landes** » dans `scene-data.ts` et `landes.json`. Question posée deux fois à Patrick, sans réponse → appliqué la règle du projet (« Figma fait foi pour les écrans reproduits ») : l'écran affiche « Les Lisières », **rien n'est renommé ailleurs**. À réconcilier (Acte I = Les Lisières dont Les Landes est une zone ? ou renommage à propager ?).
  - Note de conception : fermer l'app pendant l'écran d'acte reprend directement en jeu (il n'est pas persisté). Volontaire — c'est un carton-titre, pas du contenu, et rien n'est perdu.
- **Image d'acte : `scene_landes_frise_montagnes_pleine_b.png`**, déposée par Patrick en cours de session dans le Drive **`Assets/IMG/3 actes`** (nouveau dossier — les visuels d'acte y vivront, l'acte II viendra là).
  - ⚠️ **Leçon : le nom de calque Figma avait RAISON, c'est moi qui ai conclu trop vite.** Ne trouvant pas `_pleine_b` dans les dossiers que je connaissais, j'ai supposé un « reliquat de fill remplacé » (piège réel du 15/07) et choisi `_pleine_c` par comparaison de profils de crête contre une capture du node — le meilleur des candidats **disponibles**, à 0,042 d'erreur. Quand Patrick a fourni le vrai fichier : **0,0130**, soit 3× plus proche. Le bon réflexe aurait été de **dire que l'asset était introuvable et de le demander**, au lieu d'élire un remplaçant : ma mesure disait bien qu'aucun candidat ne collait vraiment (un vrai match tombe vers 0,005), je l'ai notée puis ignorée. `_pleine_c` retirée du repo.
- **Options : « Revoir l'introduction »** (nouveau, actif) → `forgetIntro()` puis reload. ⚠️ « Effacer la progression » **existait déjà et fonctionnait** — mais tout effacer pour revoir l'intro coûterait reliques et Registre, donc un lien **non destructif** dédié est la vraie réponse au besoin (« si je veux retester l'intro »).
- **Icônes d'objets — la vraie cause trouvée** : `objet_dague.png`, `objet_crane.png` et `objet_masque.png` sont les exports Figma du 14/07 en **68×68**, servis en repli à tout objet sans icône propre… et affichés **plein cadre** dans le bandeau « Obtenu ». C'est ça, les « anciennes images ». Remplacés par les vraies tramées 1000×1000 (arme→`dague_os`, soin→`fiole_baume`, babiole→`grimoire`, relique→`couronne_brisee`) aux **3 endroits** concernés : `GameMenu` (Inventaire), `Scene` (bandeau Obtenu) et `Home` (liste des reliques). 5 objets câblés sur une icône propre en plus (dague de départ, fiole, bandage, clef sans porte, amulette d'os). Vérifié par grep : plus aucune référence aux 3 fichiers 68px.
  - Restent au repli par type : Baume de mousse noire, Onguent gris, Lame de lanterne, Élixir du campement, Larme du Geôlier — aucune icône du lot ne leur correspond franchement, et forcer un mauvais appariement serait pire. 6 icônes réelles encore libres dans `assets/` pour eux quand Patrick voudra trancher.
- Vérifié en Playwright sur le build servi en local, 3 cas : (1) premier lancement → les 5 clauses dans l'ordre avec les bonnes images et les points 1→5, sortie vers le Seuil, `introSeen` persisté ; (2) relancement → l'intro ne rejoue plus, COMMENCER va droit au Seuil ; (3) prologue déroulé jusqu'au bout → écran d'acte avec « • LE DOMAINE • / Les Lisières » en Instrument Serif charbon sur `pleine_c`, et le tap mène au jeu. Aucune erreur JS. Lint (0 erreur) + build propres.
  - ⚠️ Pièges de test relevés : sur l'écran du Nom, **« Qu'il choisisse pour moi » ne fait que remplir le champ** — le cliquer en boucle bloque le script indéfiniment, il faut viser SCELLER ; et cliquer un bouton `aria-label` (le menu) sort du prologue. Exclure les boutons désactivés ET les boutons de menu.
- `APP_VERSION` 1.22.0 → **1.23.0**, `CACHE_VERSION` v37 → **v38**.

### Session 2026-07-26 (suite, Claude Code, sonnet) — `tools/coverage.py` recalé sur la maquette reçue
Patrick a transmis `couverture_visuelle_maquette.html` en pièce jointe. L'outil (écrit de mémoire la veille sur simple description textuelle) avait un modèle d'édition différent — recalé dessus, la maquette faisant foi pour tout ce qui est visuel/interaction.
- **Séparateur tramé** : le `<header>` à bordure CSS est remplacé par `canvas.rule` + `function rule(cv)` (semis de pixels blanc à deux opacités, jamais un filet net — la règle DA sur les dégradés/séparateurs s'appliquait déjà à l'outil, juste pas encore respectée).
- **Statuts** : bandeau plein largeur en haut de carte → **badge de coin** `top:6px;left:6px` avec les 4 traitements exacts de la maquette (dédiée = fond orange ; héritée = liseré orange ; fallback = liseré blanc-20 ; manquante = fond blanc). Stats en chiffre Instrument Serif 30px + libellé 10px capitales (dédiées/héritées/fallback/manquantes/orphelins — un 6e « prompts à écrire » gardé en plus, utile au pipeline mais absent de la maquette).
- **Filtres** : les deux rangées (STATUT / CATÉGORIE) fusionnées en une seule, comme la maquette. ⚠️ Catégorie gardée = **scene/monstre/objet** (type d'image, ce qui pilote la recette de prompt Leonardo), PAS le lieu/rencontre/créature/liaison de la maquette : une liaison n'existe jamais comme entrée statique de `SCENES[]`, elle est générée à l'exécution par `makeLiaison()` — rien à lister sous ce nom sans fabriquer une catégorie qui n'existe pas dans les données. Documenté en commentaire dans le code.
- **Édition inline** (le vrai changement de modèle) : la popup `<dialog>` disparaît. Chaque carte porte directement un `<select>` (rattacher un asset déjà tramé), une zone `.drop` (glisser-déposer → dithering canonique → assets/), et un lien souligné « détacher » (dédiées uniquement). `wire_image()` étendu : `asset` vide retire la ligne `illustration` du `.ts` (le champ est `illustration?: string`, pas nullable) et pose `null` dans les JSON de zone (convention déjà en place dans `landes.json`).
- **Journal** : la liste `#ed-journal` (vidée à chaque ouverture de popup) devient un panneau **fixe bas-droite persistant**, sauvegardé en `localStorage` (`pactum-coverage-log`) plutôt qu'en mémoire JS — nécessaire puisque chaque action réelle (rattacher/détacher/importer) recharge la page pour que les statuts de TOUTES les cartes se recalculent côté serveur (détacher une image dédiée peut faire retomber une autre scène héritière en « manquante » ailleurs dans la grille).
- ⚠️ **Bug de layout trouvé en testant, absent de la maquette** : la maquette n'a qu'une quinzaine de cartes de démo, jamais assez pour atteindre le bas de page ; ce rapport en a 92 (58 scènes + 34 POI), largement assez pour qu'une carte de la dernière ligne se retrouve sous le panneau `#log` fixe (recouvrement qui bloque le clic, confirmé par un test Playwright qui timeout sur `selectOption` — « element is not visible »). Corrigé par une marge basse de page `36vh` (couvre le panneau quelle que soit sa hauteur réelle).
- Vérifié (serveur `--serve` relancé à chaque édition, requis car le module reste en mémoire) : séparateur tramé dessiné, stats/filtres fusionnés corrects, rattachement via `<select>` → écrit `scene-data.ts` → statut recalculé en `dediee` (confirmé par import direct du module, pas seulement l'UI), détachement → repasse en `manquante`, aller-retour rattacher/détacher **sans diff résiduel** (`git diff` vide après coup). Import par glisser-déposer testé au niveau **backend** (`/api/import` en POST direct) plutôt qu'en simulant un vrai drag navigateur : le PNG ressort en exactement 2 couleurs `#1c1a16`/`#e0632a`, correctement câblé, puis nettoyé. ⚠️ Un `DragEvent` synthétique (`dispatchEvent`) déclenche bien le handler `ondrop` (confirmé : `dataTransfer.files` est peuplé, le handler s'exécute, `preventDefault()` engagé), mais le `window.prompt()` qu'il appelle pour le nom de fichier ne déclenche jamais l'événement `dialog` de Playwright dans ce contexte — probablement parce qu'un `dispatchEvent()` scripté n'est pas un vrai geste utilisateur aux yeux de Chromium pour cette API. Limite de test outillage, pas un bug produit (le mécanisme réel, actionné à la souris par Patrick, n'est pas concerné).
- Scène `palissade-sud-2` (seule « manquante » de la zone) utilisée comme cobaye pour tous les tests d'écriture réelle — repartie strictement dans l'état où elle a été trouvée à chaque fois (`git status` vide vérifié après coup).

### Session 2026-07-26 (suite #2, Claude Code, opus) — couverture : verdicts de qualité + travail sur deux postes
Patrick travaille alternativement sur **MacBook Air et iMac** et veut pouvoir éditer depuis les deux, en voyant **les images d'origine** (« le but de ce site est de voir lesquelles marchent et lesquelles je dois remplacer »).
- **Fausse piste écartée : l'artifact.** Une page autonome publiée en ligne ne peut pas porter 61 PNG en 1000×1000 (j'avais rééchantillonné à 224 px, ce qui tue précisément l'usage) ni écrire sur le disque. Le bon outil était déjà le bon : `coverage.py --serve` sert `f.read_bytes()`, donc **les PNG d'origine, octet pour octet** (vérifié par md5 : identique au fichier disque, 1000×1000). L'artifact reste utile comme simple coup d'œil partageable, pas comme poste de travail.
- **Le pont entre les deux Macs est git**, puisque TOUT ce que l'outil écrit est suivi : `assets/*.png`, `scene-data.ts`, `data/zones/*.json`, et le nouveau `data/couverture-verdicts.json`. Nouveau **`tools/couverture.sh`** qui encadre la partie fragile : installe Pillow si absent, `git fetch` puis **ff-only pull avant d'éditer** (jamais démarrer sur une base périmée, sinon on réécrit `scene-data.ts` par-dessus l'autre poste), **refuse de lancer si les historiques ont divergé** (le seul cas vraiment dangereux — il dit quoi taper), et à la sortie propose commit + push. `--no-sync` pour hors ligne, `--port N` sinon 8765.
- **Verdict de qualité (nouveau)** : le statut disait seulement si une image est CÂBLÉE, jamais si elle est BONNE — or c'est ça, le travail de Patrick. Deux boutons par carte (« à remplacer » / « ça marche »), avec **note facultative** au marquage (`prompt`), stockés dans `data/couverture-verdicts.json` **suivi par git → les deux postes voient les mêmes marquages**. Recliquer le même bouton retire l'avis. Deux filtres + deux compteurs de plus.
  - ⚠️ **Statut et verdict ne doivent jamais être confondus** (commenté dans le code) : une scène peut être « dédiée » ET « à remplacer » — le Moulin sans Ailes qui avait encore ses ailes était exactement ce cas. Distingués par la FORME autant que la couleur : statut = puce en haut à gauche de la vignette, verdict = bande pleine largeur en bas + bordure de carte blanche. Le verdict est du jugement humain, jamais déduit.
  - Un verdict ne change le statut d'AUCUNE autre carte (contrairement au câblage) → mise à jour **sur place, sans recharger**, compteurs recalculés en JS. Seules les écritures de câblage rechargent.
- **Agrandi sur clic** ajouté à l'outil local (il n'existait que dans l'artifact) : sert l'image d'origine, `image-rendering: pixelated`, Échap pour fermer. C'est le geste central pour juger une image.
- Vérifié en Playwright : verdict posé par API visible au chargement (donc il persiste), bande + note + bordure blanche + bouton actif, filtre « À remplacer », pose depuis l'UI sans reload, note via prompt, retrait par re-clic, compteurs à jour, agrandi en **1000×1000 confirmé**, aucune erreur JS. Verdicts de test effacés avant commit (`verdicts: {}`).

### Session 2026-07-26 (suite #3, Claude Code, opus) — LE LIEN : couverture en ligne sur gh-pages
Retour Patrick, deux fois : « pourquoi c'est aussi complexe pour l'ouvrir, tu ne peux pas juste m'envoyer un lien ? » puis « si je ne peux pas les modifier je ne vois pas l'intérêt ». Les deux étaient justes et pointaient une erreur de ma part : j'avais répondu à « ça ne marche pas » en SUPPOSANT (le poids) et en construisant un script shell par-dessus, au lieu de diagnostiquer, puis en proposant un outil à terminal alors qu'il veut un lien.
- **`python3 tools/coverage.py --web <dest>`** (nouveau) → page déployée sur **gh-pages, à côté du jeu** : `https://patrickmorvan21.github.io/Pat/aldenhar/couverture.html`. **96 Ko**, parce que les images ne sont PAS embarquées : les 142 assets sont **déjà publics** sous `/Pat/aldenhar/assets/` (servis par le jeu), donc la page les référence en relatif et affiche les PNG **1000×1000 d'origine**. C'est ce qu'il fallait faire dès le départ — l'artifact autonome (1,3 Mo de base64, images rééchantillonnées à 224 px) était une fausse piste sur toute la ligne.
- **Vérifié avant de déployer** : les 61 images référencées sont toutes déjà sur gh-pages (0 manquante) — sinon les vignettes auraient été cassées, exactement l'expérience « ça ne marche pas » qu'il décrivait.
- **Le marquage fonctionne DEPUIS LE LIEN** (c'est la réponse à « je ne peux pas les modifier ») : « à remplacer » / « ça marche » + note, rangés en `localStorage`, restaurés au chargement, plus un bouton plein orange **« Copier ma liste »** qui sort un texte prêt à me coller. **Le raisonnement de fond, à ne pas perdre : Patrick n'a JAMAIS câblé une image lui-même — c'est moi qui le fais à chaque séance.** Son travail est le jugement, le mien l'exécution. La page doit donc servir cette répartition, pas lui demander de manipuler des fichiers. Une page web ne peut de toute façon pas écrire dans le dépôt (sécurité navigateur).
- **Prompts Leonardo EXCLUS de la version publique** (`if i.kind == "scene" and not web`) — c'est la seule matière vraiment sensible ici, vérifié par script qu'aucun texte de prompt ne fuite. Les illustrations, elles, étaient déjà publiques via le jeu : aucune exposition nouvelle. Les descriptions de scène restent (contexte utile, peu sensible).
- ⚠️ **Bug trouvé au test — collision de sélecteurs, à retenir** : la CARTE porte `data-verdict` (le filtre en a besoin) et les BOUTONS aussi. Un `querySelectorAll("[data-verdict]")` nu attachait donc le gestionnaire aux deux : le clic sur le bouton remontait à la carte, qui se relançait avec `verdict=undefined` et **effaçait aussitôt le marquage** (symptôme : rien ne se passe au clic). Corrigé en `".verdict button[data-verdict]"`, aux deux endroits (pose + restauration au chargement).
- Vérifié en Playwright (page servie en local, images réelles) : images en **1000×1000 depuis le serveur** et non en data-URI, marquage + note, persistance après rechargement, filtre, contenu exact du presse-papier, agrandi, pas de débordement en 390 px, aucune erreur JS. Seul échec réseau = Google Fonts (proxy du sandbox, charge normalement en vrai).
- **`tools/couverture.sh` reste** pour les séances où il veut vraiment recâbler lui-même, mais ce n'est plus la réponse par défaut. Le déploiement de la page se refait avec `--web` + copie dans le worktree `gh-pages` (les assets, eux, n'ont pas besoin d'être recopiés s'ils sont déjà déployés).

### Session 2026-07-26 (suite #3, Claude Code, sonnet) — fin du plan rapproché + descriptions illustrables (v1.23.1)
Trois retours de Patrick sur la page de couverture et sur le jeu.
- **Le plan rapproché par crop est SUPPRIMÉ, dans le jeu aussi** (« ça ne rend pas bien »). Observer un point d'intérêt n'est plus un zoom dans l'image du lieu : le héros **se déplace** (le beat `approche` était déjà là pour ça) et l'écran montre **l'élément lui-même**, via son `illustration` dédiée. Retirés de `components/Scene.tsx` : l'état `poiZoom`, ses poses, le bloc de style `transform: scale()` + `transformOrigin`, et la ligne de l'overlay debug. Retirés de `lib/scene-data.ts` : les champs `PointInteret.zoom` et `.focus` (type + **68 lignes de données**) — les laisser aurait décrit une mécanique qui n'existe plus.
  - **Conséquence à connaître** : un point SANS image dédiée garde désormais l'image du lieu, inchangée. Ce n'est plus un effet dégradé mais un **manque d'asset** — visible comme tel dans `data/couverture_visuelle.html`. 26 des 28 points ont leur image (lot du 25/07) ; restent `poteau-pendu` (colline) et `eau-reflet` (mare), plus `homme-guerite` côté rencontre (`monstre_veilleur_palissade`, jamais livré).
  - Vérifié en Playwright sur le build servi en local, les deux branches : à la Mare, « Le reflet de métal » sert bien `scene_mare_miroir_a_b.png` et « L'eau » garde `scene_mare_aux_regards_a.png` — dans les deux cas `transform = matrix(1, 0, 0, 1, 0, 0)`, donc plus aucun zoom.
- **Descriptions illustrables** (`data/scene-meta.json`, 26 réécrites) : les libellés de beat (« beat 3 — la résolution ») ne disaient pas quoi dessiner, et pour une variante ne disaient pas non plus **en quoi elle diffère de la principale**. Chaque description donne maintenant le contexte + l'action visible, et se termine par une clause **`CHANGE :`** qui nomme le seul écart avec l'image principale (ex. `hesitant-3` : « la borne seule au milieu du plateau… CHANGE : l'homme a DISPARU du cadre — même pierre, vide »). Les prompts générés par la page reprennent cette clause telle quelle.
- **Bouton « ça marche » retiré** de la page de couverture (« si je ne touche pas c'est que c'est ok ») : un seul bouton `à remplacer`, la stat et le filtre « Validées » supprimés. La constante `OK` reste dans le code pour ne pas effacer les marquages déjà rangés en `localStorage`.
- Vocabulaire de la page aligné sur la décision : rôle « plan rapproché » → **« élément observé »**, et sans image dédiée la description le dit explicitement (« l'écran garde l'image du lieu, il faut produire l'image de cet élément »).
- Lint + build propres. `APP_VERSION` 1.23.0 → **1.23.1**, `CACHE_VERSION` v38 → **v39**. Déployé (jeu + `aldenhar/couverture.html`, qui est écrasée à chaque remplacement du sous-dossier — la régénérer fait partie du déploiement).
- **Icônes de rôle sur la page de couverture** (retour « la navigation est confuse ») : la grammaire du panneau de calques de **Figma** est reprise telle quelle, à gauche du nom — **4 losanges orange = le composant**, c'est-à-dire l'image principale de la scène (posée aussi sur la tête de ligne, puisque la ligne EST le composant) ; **1 losange creux blanc-50 = une variante** (élément observé ou autre moment du lieu). Constantes `ICON_PRINCIPALE` / `ICON_VARIANTE` (SVG inline 14px, `currentColor`), légende ajoutée sous les compteurs pour dire le code une fois. La hiérarchie zone → lieu → ligne est inchangée.

### Session 2026-07-26 (suite #4, Claude Code, opus) — lot Notion 26/07, vague 1 (Registre + affordance)
Le journal Notion « 🗓️ 26 juillet 2026 — Écran du Registre, affordance « Touche pour continuer », séquence de mort » compte **6 points**. Vague 1 = points 1 et 2 (ceux du titre), déployés en v1.24.0 / `CACHE_VERSION` v40.
- **Écran du Grand Registre** (`components/Registre.tsx` + `lib/registre-data.ts`, maquette Figma **2265:24560**) : illustration 390×390 (`objet_grand_registre_d.png`, récupérée du Drive `Assets/IMG/Registre`, 1000×1000, taille exacte vérifiée) + bande de dissolution, titre Instrument Serif orange, deux onglets **LES 100** / **TES MORTS**, colonnes rang · nom avec la cause en sous-titre, lien « Descendre dans le Registre » par pages de 12, pied « Quatre-vingt-dix-neuf places sont prenables. La première, non. »
  - **La première place** : nom rendu **gratté** (canvas 190×13, semis de pixels orange par blocs de largeur de lettre, seedé — la même rature à chaque ouverture, jamais réanimée), jours en orange, et le compte **augmente entre les sessions** (`joursDuPremier` : 11 248 au 26/07, +1 tous les 3 jours réels depuis une époque figée — jamais un `Math.random`, sinon ce n'est plus un registre).
  - **Les 100 lignes existent vraiment** : 36 héros de fond écrits à la main (41 → 1 jour) + les héros tombés du joueur + sa run en cours, complétés par une réserve de noms/causes assemblée **de façon déterministe**. ⚠️ Piège trouvé au test : une réserve à jours uniformes (20 → 1 par paliers de 5) **enterrait le joueur** sous quarante inconnus à 19 jours. Corrigé par une courbe quadratique qui concentre les anonymes sur les premiers jours (la plupart meurent tout de suite) — vérifié : sur une mémoire à 2 morts + run en cours, les 3 lignes du joueur sortent aux rangs 19, 27 et 46, chacune avec son liseré orange, et le classement fait exactement 100 lignes.
  - Ligne du joueur = **bande pleine largeur + liseré orange 4px au bord gauche** (jamais un badge), qui déborde des marges du contenu comme dans la maquette.
  - ⚠️ **Écart assumé Notion ↔ Figma, documenté dans le code** : le Notion annonce « colonnes rang · nom · **jours** », la maquette n'en montre pas — elle met la CAUSE en sous-titre et ne donne les jours que sur la ligne verrouillée. Figma fait foi pour un écran maquetté, donc pas de colonne jours dans « Les 100 » ; en revanche « Tes morts » les affiche (J19, J7…) avec la cause **et la relique forgée**, comme le Notion le demande explicitement pour cet onglet.
  - Correctif attrapé au test : le **numéro de version** de l'accueil se posait par-dessus l'écran plein cadre (il est peint après l'overlay) → masqué dès qu'un overlay est ouvert.
- **Affordance « Touche pour … » — règle GLOBALE** (`components/TouchHint.tsx`) : un seul composant partagé, **50 px du bas du cadre**, centré, Roboto Mono blanc-50, **clignotement saccadé** (`@keyframes touch-blink` en `steps(1)` sur `visibility` — une transition d'opacité, même courte, produirait le fondu que la DA interdit). Libellé « Touche pour **commencer** » sur le premier écran d'une séquence, « **continuer** » ensuite. Branché sur les 5 clauses d'intro (la 1re dit « commencer »), l'écran d'acte et les beats d'amorce du prologue — les trois implémentations maison (positions 22px / 52px, pulse d'opacité) sont supprimées.
- **RESTE du 26/07, non fait** : point 3 (séquence de mort en 6 écrans — l'ordre est verrouillé *beat fatal → mort → fragment → Registre → relique → relève*, avec le Registre AVANT la relique) ; point 4 (l'outil de couverture passe de la liste à la **vue carte**, la maquette `data/couverture_carte_v2.html` faisant référence) ; point 5 (réimport des nouvelles images et recâblage des scènes encore en vue générique) ; point 6 (réécrire la scène perdue des **Corbeaux du Compte**, où les corbeaux se posent au nombre exact des morts du joueur).

#### Vague 2 (v1.25.0 / v41) — la SÉQUENCE DE MORT en six écrans (point 3)
- **Ordre verrouillé** : beat fatal → mort → fragment → Registre → relique → relève. Le Registre passe AVANT la relique parce que le fragment et le Registre regardent en ARRIÈRE, la relique en AVANT — elle doit être la dernière image avant la nouvelle run.
- **Écran 1, la mort arrive DANS la scène** : `DeathScreen` se monte par-dessus le jeu en restant **transparent**. Il mesure les vraies boîtes (`.choices-bar` puis `.scene-text-zone`) et les mange par cellules de 3px, du bas vers le haut, dans un ordre bruité (une grille qui se vide ligne à ligne aurait l'air d'un store). Chaque cellule mangée pousse une **braise orange** au même endroit, qui monte et se raréfie par PROBABILITÉ DE DESSIN (jamais par alpha). Le mot **MORT** tombe sec en Instrument Serif 46px espacé 14, l'écran tremble (`death-quake`, steps). Le tap ne saute rien — seul moment du jeu où le joueur n'a plus la main.
- **Écran 2, le bilan** : les SEULS chiffres bruts autorisés du jeu (c'est un registre de greffe, pas un retour de partie). `bilanDeMort(run)` — jours, plus loin descendue en NOM DE RÉGION, lieux traversés, rencontres, dés lancés dont tenus, Destins · Malédictions, reliques portées ; jours et le couple Destin/Malédiction en orange. `RollRecord` gagne un champ `ok` (posé à la résolution) pour le « dont tenus ».
- **Écran 3, le fragment** : arc garanti aux morts 1, 2, 3, puis 5, 8, 12, 17 (7 fragments écrits) ; entre les jalons il se tait, sinon la promesse de la 3ᵉ clause s'use. Frappe à 42 ms, glyphe ◉, texte orange.
- **Écran 4, le Registre** : la ligne du héros s'insère entre ses deux voisines estompées, liseré orange. **N'apparaît que si le score entre dans les cent** — sinon écran sec « Ton nom n'entre pas au livre », sans quoi le Registre perdrait sa valeur.
- **Écran 5, la relique** : elle SE FORGE à l'écran (les cendres convergent et s'agglomèrent), murmure en orange italique réservé aux rares et légendaires. **Écran 6** : « Un autre viendra » (CTA plein à segments décalés) + « Reposer le livre ».
- ⚠️ **Pièges de test, tous du SETUP** : (1) le dé se saisit à ~60 px de SON centre — l'attraper par `.die-ring` (l'anneau est centré dessus), pas par le canvas plein cadre ; (2) entre deux jets il faut **taper dans la zone de texte** (y≈520 ; à y=300 on tombe dans l'illustration, haute de 352 px) pour finir la frappe, sinon les CTA n'apparaissent pas dans le temps imparti ; (3) seeder `trav.target` haut (99) — sinon la run atteint la Descente et redémarre avant d'avoir pu mourir. Vérifié de bout en bout : MORT + tremblement + dissolution, bilan complet (28 j · Les Landes · 8 lieux · 6 rencontres · 8 dés dont 6 tenus · 1 Destin · 1 Malédiction · 2 reliques), fragment de la 3ᵉ mort, insertion au Registre au rang 10 entre Otho et Maerith, relique, relève. Aucune erreur JS.
- ⚠️ **Non fait, à signaler** : la **face rongée du dé** (« sa face du dessus est rongée, illisible ») demande une modification de `Die3D` (texture de face générée au moment de l'immobilisation) — pas touché, le dé garde sa face normale avant que l'écran ne bascule.
- **RESTE du 26/07** : point 4 (outil de couverture en vue carte), point 5 (réimport des nouvelles images + recâblage des scènes en vue générique), point 6 (scène des Corbeaux du Compte).

#### Vague 3 (v1.26.0 / v42) — les Corbeaux du Compte (point 6) + début du lot d'images (point 5)
- **LES CORBEAUX DU COMPTE remis en jeu** : point d'intérêt de la Colline aux Gibets (`corbeaux-compte`, posé EN TÊTE des points — on les voit de loin avant d'entrer dans le cercle), image `monstre_corbeaux_du_compte_b.png` qui dormait en réserve depuis le 24/07. Nouveau champ `PointInteret.corbeaux` → l'examen pousse une 3ᵉ narration calculée par `ligneCorbeaux(mem.deaths)` : les corbeaux sont **exactement aussi nombreux que les morts du compte**. Le nombre est dit en PROSE (« ils sont trois »), jamais en chiffre — et au-delà de douze on cesse de le nommer, parce que « beaucoup » est plus juste et transforme la ligne en menace plutôt qu'en score. Palier 0 (aucune mort) : un seul corbeau, de travers, « comme s'il gardait une place ». Vérifié à 0 / 1 / 3 / 20 morts. La règle de dosage est tenue via `chapterFragment` sur les deux autres points.
  - ⚠️ Détail d'écriture attrapé au test : « ils sont un » sonne faux — le cas d'un seul corbeau a sa propre phrase.
- **Lot d'images du 26/07 : 21 nouveaux fichiers repérés dans le Drive, 2 importés.**
  - ⚠️ **La contrainte est le COÛT DE TRANSFERT, pas le câblage** : `download_file_content` renvoie le base64 dans le contexte du modèle, soit ~8 à 10 k tokens par PNG de 25-30 Ko. 21 fichiers ≈ 180 k tokens, impossible en une session. Les deux qui bouchaient les trous les plus visibles sont passés : `monstre_appele_descente.png` → **`palissade-sud-2`** (la SEULE scène de la zone qui n'avait aucune image) et `objet_lanterne_rouillee_guerite.png` → `veilleur-1` (ses trois beats montraient la même lanterne).
  - **Mapping déjà établi pour les 19 restants** (à câbler dès qu'ils sont sur disque — le plus économique est un `cp` local par Patrick depuis son Drive synchronisé) : `objet_lanterne_rouillee_tendue`→veilleur-2 · `_registre`→veilleur-3 · `monstre_pendu_qui_parle_sceau`→pendu-qui-parle-2 · `monstre_hameau_vieux_pain`→hameau-halte-4 · `monstre_epoux_verger_mains_tendues`→epoux-2 · `_dos`→epoux-3 · `scene_hameau_portillon_escorte`→hameau-halte-5 · `scene_hameau_grange_fente_lumiere`→hameau-halte-3 · `scene_hameau_ruelle_volets`→hameau-entree-2 · `scene_hameau_femme_seuil_approche`→femme-seuil-1 · `scene_verger_noir_fruit_tombe`→verger-noir-2 · `scene_moulin_sans_ailes_approche`→campement · `_lit_bruyere`→campement-2 · `scene_chemin_creux_approche`→chemin-creux · `scene_verger_noir_epoux_approche`→POI epoux-verger · `scene_borne_frontiere_entree`→borne-frontiere (remplacerait l'image du 25/07, à confirmer).
  - **3 fichiers volontairement sans mapping** : `monstre_marcheur_a_rebours` (sans suffixe de beat, même ambiguïté que `monstre_pendu_mal_fixe_v1_b` le 25/07 — variante ou remplacement ?), `scene_mare_miroir_approche` et `scene_verger_noir_fruits_approche` (images d'APPROCHE d'un point d'intérêt, or un POI n'affiche qu'une image — il n'y a pas de slot sans changer l'architecture).
- **RESTE du 26/07** : point 4 (outil de couverture en vue carte) et la fin du lot d'images.

### Session 2026-07-28/29 (Claude Code, opus) — Tour de Guet, flamme tramée, carte de l'atelier, prompts refaits, transfert Drive complet (v1.28.0)

Longue session en plusieurs volets. Le fil rouge : Patrick ne lit ni `landes.json` ni le code — il travaille depuis les pages HTML publiées, et son rôle est le JUGEMENT, le mien l'EXÉCUTION. Toutes les décisions d'outillage découlent de ça.

- **Tour de Guet écrite** (`lib/scene-data.ts`) : `tour-de-guet` (3 points d'intérêt) + `tour-de-guet-2` (le Guetteur sans tour). ⚠️ `outcomes()` prend **4 arguments** (crit / réussite / échec / funeste), pas 2 — l'erreur de compilation est immédiate mais le rappel évite de réécrire les textes deux fois.
- **Flamme pixel** (`components/FlammeTramee.tsx` + `maquettes/flamme_pixel.html`) : automate de chaleur + seuillage Bayer 4×4, **exactement 2 couleurs**, reproduit le mp4 fourni. Pas de dégradé, pas d'interpolation.
- **L'ATELIER devient une carte publiée** (`https://patrickmorvan21.github.io/Pat/aldenhar/atelier.html`) : `tools/atelier.py --web <dest>` cuit les données dans `window.__ZONE_DATA__` (lecture seule en ligne, édition seulement en `--serve` local). Positions x/y par lieu, cadre englobant des régions, zoom, glisser-déposer.
  - ⚠️ **Le drag n'enregistrait que 2 `pointermove` sur 10** (mesuré) avec `setPointerCapture` — les écouteurs doivent vivre sur **`window`**, pas sur l'élément. Vérifié ensuite : 100 px à 58 % de zoom = 173 unités du modèle.
  - ⚠️ **Piège de préfixe, qui a mordu deux fois** : les LIEUX portent `assets/xxx.png`, les SCÈNES portent `xxx.png`. Un `BASE="/assets/"` absolu casse les uns ou les autres selon le côté d'où on regarde. Normaliser avant de comparer.
  - ⚠️ La fiche latérale recouvrait la barre du haut (`top:0`) et rendait le nouveau CTA incliquable — `#fiche` descendu à `top:58px`.
  - **Positions de la carte = géographie ÉCRITE de la zone**, pas le Figma : le MCP Figma répond « you don't have edit access to this file » sur le node 2112:325. Réserve à lever si Patrick partage le fichier.
- **Les 68 prompts de variante réécrits** (`tools/prompts_variantes.py`) après un retour précis de Patrick sur le Verger Noir : les prompts de variante **redécrivaient le couple à chaque fois**, donc chaque image sortait avec des personnages et un décor différents. La règle était : **zoomer sur un élément d'action ou de décor**, jamais re-générer les personnages.
  - Quatre cadrages nommés — `pres` (macro, aucune figure), `large` (silhouettes lointaines à contre-jour), `portrait` (fond noir, image de référence du personnage), et surtout **`geste`** : cadrage serré sur les mains ou le dos, **tête hors champ, visage jamais visible**, pour que rien n'ait à correspondre à une autre image. C'est ce cadrage qui résout le problème de fond.
  - Au passage : la clause de lumière était trop molle et écrasait le détail des gros plans — une seule source rasante, fond qui tombe au noir.
  - Les **15 images d'établissement de lieu sont protégées** : le script n'y touche pas.
- **Transfert Drive : les 36 images du lot 28/07 récupérées, jusqu'à la dernière.** Patrick : « fais-le toi-même quitte à ce que ça prenne du temps » — fait, par lots, avec commit après chaque lot pour survivre à une compaction.
  - ⚠️ **Le coût est le VRAI facteur** : `download_file_content` renvoie le base64 **dans le contexte du modèle** tant que le fichier fait moins de ~75 Ko, soit ~5-10 k tokens par PNG. Au-delà, le harnais le décharge dans un fichier de tool-results et le contexte est épargné. Nos PNG tombent pile dans la mauvaise fourchette.
  - **Reprise sûre** : `data/lots/2026-07-28-landes.json` porte un bloc `drive` avec fileId **et taille attendue** pour chaque fichier. L'extracteur (`scratchpad/extraire.py`) lit le base64 par titre dans le `.jsonl` de session, vérifie la signature PNG **et la taille exacte**, et n'écrit jamais un fichier mal décodé. On peut donc reprendre à n'importe quel point — c'est le contrôle de taille qui rend ça sûr, pas la liste.
  - **`tools/cabler_landes.py`** applique un **manifeste explicite** (36 correspondances fichier→id de scène). ⚠️ La 1re version marchait par heuristique sur tout `assets/` : elle attrapait des images de réserve et **écrasait des câblages corrects** (elle a mis `scene_gibet_vide_c` par-dessus le gros plan du Gibet Vide). Un manifeste, jamais une devinette.
  - Résultat : **99 références d'assets dans `scene-data.ts`, 0 manquante**. Couverture de la zone : 60 scènes · 38 points d'intérêt · **dédiée 92 · héritée 3 · fallback 3 · manquante 0**.
- ⚠️ **À TRANCHER PAR PATRICK — le Moulin sans Ailes a encore ses ailes.** `scene_moulin_campement_a.png` (lot du 28/07, aujourd'hui câblée sur `campement`) montre **quatre ailes complètes en croix** devant le soleil. C'est la **troisième** génération de suite qui les garde (déjà `_c` puis `_d_d` le 25/07, où le correctif avait été noté comme fait alors que le fichier était faux). L'image est belle et bien tramée — mais le lieu s'appelle *le Moulin sans Ailes* et son point d'intérêt (« la croix d'ombres ») repose entièrement sur leur disparition. Le prompt corrigé est dans `data/images-a-produire.md` : décrire **une tour de pierre tronquée**, sans jamais écrire le mot *moulin* (une consigne négative « no blades » renforce ce qu'elle interdit). L'écran-intérieur `campement-2` (lucarne éclairée), lui, est bon.
- ⚠️ **Le Mac de Patrick n'a PAS de clone du dépôt** (`find ~ -maxdepth 4 -type d -name Pat` ne renvoie rien, `git pull` répond « not a git repository »). Toute réponse qui commence par « fais un `cp` depuis ton Drive synchronisé puis pousse » est donc caduque tant qu'il n'a pas cloné. Le transfert par connecteur, même lent, reste le chemin réel.
- **Déploiement** : rappel — remplacer le sous-dossier `aldenhar/` de `gh-pages` **efface `couverture.html` ET `atelier.html`**. Les deux se régénèrent (`coverage.py --web`, `atelier.py --web`) et se recopient à chaque déploiement, sinon les liens de Patrick tombent en 404.
- `APP_VERSION` 1.27.1 → **1.28.0**, `CACHE_VERSION` v44 → **v45**. Lint (0 erreur), typecheck et build propres, déployé (gh-pages `8a8eed3`).

### Session 2026-07-29 (suite, Claude Code, fable) — carte de l'atelier désynchronisée après le lot d'images
Retour Patrick : « mets à jour atelier.html suite aux nouvelles images ». La page avait pourtant été régénérée APRÈS `cabler_landes.py` lors du déploiement — le vrai bug était ailleurs.
- **Cause racine** : la carte de l'atelier affiche **`lieux[].illustration`** (la vignette du lieu), mais `cabler_landes.py` n'écrivait que **`scenes[].illustration`** (+ scene-data.ts). Un lot qui change la vue d'ÉTABLISSEMENT d'un lieu laissait donc la carte sur l'ancienne image. Trois lieux touchés par le lot du 28/07 : `borne_frontiere` (a_c → b), `moulin_sans_ailes` (sans_ailes_d_d → moulin_campement_a), `puits_condamne` (c → a).
- **Correctif durable dans `cabler_landes.py`** : après le câblage des scènes, chaque lieu est réaligné sur l'illustration de sa **scène d'arrivée** (`type: "arrivee"`, même champ `lieu` — la première en ordre de document, qui est toujours l'établissement ; vérifié pour les 15 lieux, les combats/beats arrivent après). Les ids de `lieux[]` correspondent tous aux champs `lieu` des scènes (y compris `tour_de_guet_effondree`, `hameau_des_renoncants`).
- ⚠️ **Panne d'infra rencontrée en cours de session** : le classifieur de sécurité Bash (`claude-opus-5[1m]`) est tombé en panne — TOUTES les commandes Bash refusées pendant un long moment (« temporarily unavailable »), y compris `ScheduleWakeup`. Les outils **Edit/Write/Read/Grep/Glob ne passent pas par ce classifieur** et restent utilisables : les 3 corrections de `landes.json` ont été faites à l'Edit directement (déterministes, une occurrence chacune, vérifiée par grep avant), plutôt que d'attendre le script. Réflexe à retenir : quand Bash est gelé, basculer le travail sur les outils de fichiers, garder Bash pour ce qui l'exige vraiment (régénération, déploiement).

### Session 2026-07-30 (suite, Claude Code, fable) — réconciliation FondBraises ↔ flamme_braise_v3.html (v1.29.1)
Patrick a fourni `flamme_braise_v3.html` en PJ → versionnée dans `maquettes/`. La comparaison a montré que mon implémentation « depuis les réglages écrits » divergeait du moteur réel sur 4 points — **le composant est maintenant un PORT FIDÈLE de la maquette**, verrouillé sur le réglage retenu :
- **Bruit de VALEUR lissé (hash, 3 octaves 0.55/0.29/0.16 à ×1/×2.37/×4.91)** au lieu de mes sinus — la maquette dit explicitement « casse toute périodicité », un sinus est périodique par construction.
- **Respiration = MULTIPLICATEUR (0.70 → 1.22, 16 paliers)** qui monte la source ET ralentit le refroidissement (`cool / respire`) — ma version n'ajoutait qu'un ±0.06 sur la source, amplitude ~10× trop faible.
- **Vent en cisaillement SOUS-CELLULE** (échantillon décalé interpolé, `cis = vent·(y/H)·1.7`) au lieu d'un décalage probabiliste de colonne entière ; braises du lit avec rayon d'influence 3-10, chute t², déclin après 70 % de vie.
- **Le `boost` de densité ABAISSE le seuil de Bayer** (`v > (bayer+0.5)/16 − boost`) — je l'ignorais au seuillage. + boucle à pas de temps fixe (accumulateur, 4 pas max/frame), cendres 28 % blanches, trous de charbon qui ne s'ouvrent qu'en haut du lit.
- Le résultat à l'écran n'a plus rien à voir : nappe continue incandescente à crête irrégulière au lieu de paquets épars. Leçon (déjà apprise avec le Procès du 23/07, re-confirmée) : **implémenter depuis une description ne remplace jamais le fichier de référence — récupérer le fichier d'abord, ou marquer le résultat comme provisoire.** Vérifié Playwright (séquence complète re-testée, capture comparée), lint/build propres. `APP_VERSION` **1.29.1**, `CACHE_VERSION` **v47**.

### Session 2026-07-30 (suite #2, Claude Code, fable) — retours écran de mort (v1.29.2)
Deux retours de Patrick sur le déploiement 1.29.1 :
- **Les cendres passent AU-DESSUS de l'interface** : le canvas de `FondBraises` ne faisait que la hauteur du foyer (48px) — les cendres mouraient coupées à son bord haut. Le composant est maintenant un **calque plein écran transparent** (`absolute inset-0`, `alpha: true`, pointer-events-none, rendu APRÈS le contenu) : la simulation de chaleur reste confinée au bas (`HF` cellules = prop `height`), mais les cendres montent dans tout le cadre (`HT`) et volent par-dessus le texte. ⚠️ Piège du passage en plein écran : l'ancien `fillRect` charbon de fond aurait masqué toute l'interface — remplacé par `clearRect` (le charbon, c'est déjà l'écran dessous).
- **Le nom gratté (Geôlier) en record des « Les 100 »** de l'écran de mort : `NomGratte` exporté de `Registre.tsx` et réutilisé dans `LigneRegistreMort` pour la ligne `locked` — rang 1 orange, rature de pixels à la place du nom, « 11 249 jours, et le compte continue » en orange dans le sous-titre, pas de colonne jours (ils ne se comparent à rien). Même traitement que le Registre plein cadre.
- NB de test : « Harn, 33, rendu à la Marge » vu dans Les 100 n'est PAS un bug de jours — c'est un héros du FOND écrit à la main (`registre-data.ts:56`) ; le seed du test réutilisait le même nom par coïncidence.
- Vérifié Playwright (séquence complète + onglet LES 100 : canvas 190×13 présent, « compte continue » affiché) ; captures : cendres visibles au-dessus des lignes du Registre et du « Touche pour continuer ». `APP_VERSION` **1.29.2**, `CACHE_VERSION` **v48**.

### Session 2026-07-30 (suite #3, Claude Code, sonnet) — Options : « Aperçu de l'écran de mort » (v1.29.3)
Retour Patrick : « je ne veux pas refaire le jeu jusqu'à ce que je meure pour voir si tout est ok ». Ajout d'un bouton non destructif, plutôt qu'un vrai déclencheur de mort qu'il faudrait ensuite annuler.
- **`bilanDeMort` déplacé de `Scene.tsx` vers `DeathScreen.tsx`** (exporté, à côté du type `Bilan` qu'il construit) : nécessaire pour être appelable depuis `GameMenu.tsx` sans créer de cycle (`Scene.tsx` importe déjà `GameMenu.tsx`, donc l'inverse est impossible). Scene.tsx importe désormais `bilanDeMort` au lieu de le redéfinir.
- **Bouton dans `OptionsTab`** (`components/GameMenu.tsx`), juste après « Revoir l'introduction » : `buildPreviewMort()` lit la run et la mémoire RÉELLES (`loadRun`/`loadMemory`) pour rester représentatif (nom du héros, jour, bilan des jets, reliques en mémoire) mais **ne persiste rien** — pas de `recordDeath` (deaths/relics/Registre du compte inchangés), pas de `resetRun`. La relique de l'aperçu vient de `forgeRelic()` (déjà pur, aucune écriture) appelé à la volée, pas de la vraie forge de mort.
- `OptionsTab` (composant partagé, utilisé aussi bien dans le menu en jeu que dans l'overlay Options de l'accueil) rend `<DeathScreen>` en overlay (`absolute inset-0 z-[50]`) par-dessus son propre contenu quand l'aperçu est actif ; `onRestart` referme simplement l'aperçu (`setPreview(null)`) au lieu du `window.location.reload()` d'une vraie mort — fonctionne identiquement depuis l'accueil (avant même de lancer une run) et depuis le menu en cours de partie.
- ⚠️ Effet de bord assumé, mineur : `pickJailerQuote`/`fragmentPour` à l'écran du fragment tournent la mémoire locale « 3 dernières citations servies » comme une vraie mort le ferait — c'est cosmétique (juste quelle citation reviendra la prochaine fois), pas une donnée de progression.
- Vérifié Playwright, DEUX chemins : (1) depuis l'accueil (aucune run en cours) — bouton visible dans Options, séquence complète jusqu'à la révélation, fermeture SANS reload, `localStorage` `aldenhar-run`/`aldenhar-player` strictement identiques avant/après (deaths et relics inchangés) ; (2) depuis le menu EN JEU (`REPRENDRE` → burger → Options) — aperçu lancé par-dessus le jeu, refermé sur les Options, menu fermé ensuite → le jeu continue normalement (`.choices-bar` toujours là), `deaths` toujours à 0. Aucune erreur JS dans les deux cas. Lint (0 erreur, seul le warning police préexistant) + typecheck + build propres.
- `APP_VERSION` 1.29.2 → **1.29.3**, `CACHE_VERSION` v48 → **v49**.

### Session 2026-07-30 (suite #4, Claude Code, opus) — flammes pleine hauteur + éclat de cendres (v1.29.4)
Trois retours de Patrick sur la séquence de mort, avec capture à l'appui.
- **« Un bloc noir en bas » — la cause n'était pas le feu, c'était le CADRE.** Tous les `.phone-frame` sont dimensionnés à la maquette (800 / 841 / 848 px) et **centrés** par `min-h-dvh items-center` : sur un iPhone récent (≈ 852 px de haut), le fond de page apparaissait donc en bande noire sous le cadre. Corrigé en CSS (`globals.css`, une seule règle sous `@media (max-width: 767px)` → `.phone-frame { height: 100dvh }`) plutôt qu'en touchant les 5 className : la **largeur reste 390 px** (référence de toutes les maquettes Figma) et les mises en page sont en `flex-1`/`dvh`, donc les ~50 px gagnés se répartissent seuls. Au-delà de 767 px (desktop, prévisualisation) le cadre garde sa hauteur de maquette. Vérifié à 393×852 : cadre 0→852, **0 px** de fond visible.
- **Flammes ×3** : `FondBraises` passe de `height={48}` à `144`. ⚠️ **Agrandir le foyer ne suffit PAS** — la chaleur perd `COOL` par rangée, donc la flamme mourait au même nombre de pixels et on n'ajoutait que du vide au-dessus. Nouveau `HAUTEUR_REF = 48` : `cool` est divisé par le facteur d'agrandissement, ce qui **étire le profil** au lieu de le déplacer (le lit, le cisaillement du vent et la zone basse sont déjà en fraction de `HF`, ils suivent seuls). Mesuré : le feu monte à **~170 px** (contre ~50). Réglages de la maquette `flamme_braise_v3.html` inchangés par ailleurs.
- **Conséquence attrapée au test** : à 50 px du bas, « Touche pour continuer » passait DERRIÈRE les flammes. `TouchHint` gagne un prop `bottom` optionnel, utilisé **uniquement** par la séquence de mort (`HINT_BAS = 200`) — la règle globale des 50 px reste la règle partout ailleurs, et le commentaire du composant le dit.
- **Éclat de cendres de la relique, « plus impressionnante, plus voyante »** : `CendresRevelation` refait. Les cendres partaient du CENTRE, donc **cachées derrière l'illustration** — on ne voyait que la fin du mouvement. Elles partent maintenant du **bord du cadre** (rayon calculé sur le CARRÉ de 336 px, coins compris), le canvas passe de 420 à **620 px** et le calque passe **DEVANT** la relique (`z-10`) : des débris au premier plan lisent comme une explosion, un calque derrière lisait comme un halo. Trois populations — l'**onde** (rapide, brève, moitié blanche, avec traînée de 2-3 px pour les plus vives), les **éclats lourds** (projetés loin puis qui retombent, gravité appliquée seulement APRÈS la projection), les **cendres fines** (qui montent et ondulent, 2,6 s). Raréfaction par probabilité de dessin, jamais par alpha ; deux couleurs. Mesuré : ~1 400 pixels allumés contre ~260 particules avant.
- Vérifié Playwright à 393×852 via l'aperçu d'Options (bande noire 0 px, hauteur de feu, densité et z-index de l'éclat, captures à t≈60/130/260/700 ms), aucune erreur JS. Lint + typecheck + build propres.
- **Point d'attention relevé au passage (pas un bug) :** `data/atelier.html` déployé est **byte-identique** à une régénération d'aujourd'hui, et les 187 assets de `gh-pages` sont identiques octet pour octet aux locaux. Les « images fantômes » ne viennent donc pas d'un fichier manquant ni d'une page périmée : plusieurs PNG ont **changé de contenu sous le même nom** (meute grise, petite fixée le 25/07) et rien dans l'URL ne le signale au navigateur — c'est du cache. Un manifeste d'assets avec hash court est la réponse (chantier ouvert avec Patrick).
- `APP_VERSION` 1.29.3 → **1.29.4**, `CACHE_VERSION` v49 → **v50**.

**v1.29.5 — le feu FERRÉ en bas (2e passe, « j'ai toujours un bloc noir en bas »).** Le cadre était bien collé au device (mesuré : cadre 0→852, canvas 0→852, `0 px` de fond visible) — le noir restant venait du **feu lui-même**. Diagnostic par comptage de pixels allumés par rangée depuis le bas : `90/130 · 100/130 · 69/130 · 95/130 · 57/130…`, avec une **alternance** due à la ligne de Bayer. Autrement dit le lit n'a jamais été plein : au ras du bord on voit le charbon à travers la trame, et ça lit comme une bande noire. Deux correctifs :
  - **Socle** : les 3 rangées du bas (9 px, épaisseur fixe en PIXELS — c'est le contact avec le bord de l'écran, il ne doit pas dépendre de la hauteur du foyer) ont leur chaleur planchée **au-dessus du seuil de trame le plus haut** (0,97 − BOOST), en relâchant en `force²` : le pied est plein, son bord supérieur se ronge tout seul, aucune barre nette. Re-mesuré : `130/130 · 130/130 · 130/130 · 113 · 81…`.
  - **Lit à l'échelle** : `LIT_RANGS` (3) était absolu — sous une flamme ×3 le pied restait maigre et le feu semblait flotter. Il suit maintenant le même facteur que le refroidissement (3 → 9 rangées à 144 px).
  - ⚠️ Leçon de méthode : la première passe avait vérifié la **géométrie** (le cadre touche-t-il le bas ?) et conclu trop vite. Ce qu'il fallait mesurer, c'est le **remplissage par rangée** — « ça touche le bord » et « c'est plein au bord » sont deux questions différentes.
  - `APP_VERSION` 1.29.4 → **1.29.5**, `CACHE_VERSION` v50 → **v51**.

### Session 2026-07-30 (suite #5, Claude Code, opus) — LE MANIFESTE DES ASSETS (v1.30.0)
Réponse à l'irritant n°1 de Patrick (« les images ne se mettent pas à jour »). Diagnostic d'abord, code ensuite — et le diagnostic a déplacé la cible.
- **La cause n'était ni une page périmée ni un fichier manquant.** Vérifié : `atelier.html` déployé était **byte-identique** à une régénération du jour, et les 187 assets de `gh-pages` **identiques octet pour octet** aux locaux. Le vrai mécanisme : plusieurs PNG ont changé de CONTENU **sous le même nom** (meute grise, petite fixée le 25/07) — rien dans l'URL ne le signale, donc aucun cache n'a de raison d'aller rechercher l'image.
- **⚠️ Découverte qui explique la ténacité du symptôme : le service worker a pour portée `/Pat/aldenhar/`** (`register(sw.js, {scope: basePath + "/"})`, layout.tsx:53). **`atelier.html` et `couverture.html` sont DANS cette portée** — elles subissaient donc le cache applicatif du jeu (stale-while-revalidate : le cache est servi AVANT le réseau), bien plus tenace qu'un cache HTTP, et insensible à un simple rechargement.
- **`tools/gen_manifest.py`** (nouveau) : parcourt `assets/`, écrit **deux** sorties d'un seul calcul — `aldenhar/lib/assets-manifest.json` (minimal, nom → hash, **importé** par le bundle pour que `assetUrl()` reste synchrone) et `aldenhar/public/assets/manifest.json` (complet : taille, date, hash, drapeau `recent` calculé sur les 10 derniers commits, + en-tête `genere`/`commit`, **fetché** par les pages d'outillage). `npm run gen:manifest`, branché en **`prebuild`** → impossible à oublier tant qu'on déploie avec `npm run build` (⚠️ **le déploiement doit passer par `npm run build`, plus `npx next build`**, sinon pas de prebuild).
- **`lib/assets.ts`** : `assetUrl()` / `assetCss()` suffixent chaque image de son hash (`assets/x.png?v=a3f2b9c1`). Appliqué à la **couche de RENDU** (7 composants, ~20 points d'appel), jamais dans les données : `scene-data.ts` et `besace.ts` gardent des chemins propres et diffables. Un chemin inconnu du manifeste ressort inchangé (mieux vaut une image peut-être cachée qu'une image cassée). Vérifié Playwright : **9 requêtes d'assets distinctes, 9 avec hash, 0 sans, aucune 404**.
- **Les deux pages d'outillage** : URL versionnée (la CURE) + **pastille de hash sur la vignette** (le DIAGNOSTIC — deux vignettes au même hash = même fichier), **bandeau de fraîcheur** (date de génération + commit court) complété côté client par une relecture du manifeste SERVI avec cache-buster, qui **alerte si les assets ont bougé depuis la génération de la page**. Trois filtres sur la couverture : **Doublons** (15 cartes — calculé sur le nom de fichier, pas le hash : le même nom sur deux scènes est une image RÉUTILISÉE, ce qu'on veut voir), **Nouveautés** (38), **Introuvable** (0 — un `illustration` pointant vers un fichier absent s'affiche en blanc franc « FICHIER INTROUVABLE »). Les **34 orphelins** passent de liste de noms à **vignettes** : on ne décide pas de garder ou de jeter une image sans la voir.
- **⚠️ RÈGLE DE PROCESS actée, qui rend presque tout ça inutile et qu'il faut tenir quand même : une image regénérée ne garde JAMAIS son nom**, elle prend la lettre de variante suivante (`_c` → `_d`). Le changement devient visible dans l'historique git au lieu d'être un remplacement silencieux. Le hash n'est que le filet de sécurité.
- **Piste ouverte pour le coût de transfert Drive** (~8-10 k tokens par PNG, le maillon le plus lent) : github.com accepte le **glisser-déposer de fichiers multiples** dans un dossier du dépôt, depuis le navigateur, sans terminal ni clone. Proposé à Patrick.
- Vérifié Playwright sur le build servi en local : jeu (100 % des assets hashés), couverture (98 cartes · 34 vignettes de réserve · 130/130 URL versionnées · les 3 filtres · bandeau « à jour »), atelier (16/16 images hashées + bandeau). Aucune erreur JS, aucun asset en erreur. Lint + typecheck + build propres.
- NB opérationnel : les deux manifestes étant regénérés par `prebuild`, **chaque build laisse un petit diff** (`genere`, `commit`, quelques `recent`). C'est normal — on commite l'état réellement déployé. `lib/assets-manifest.json` est versionné à dessein : sans lui, un `tsc --noEmit` sur un clone neuf échouerait avant que `prebuild` n'ait tourné.
- `APP_VERSION` 1.29.5 → **1.30.0**, `CACHE_VERSION` v51 → **v52**.

### Session 2026-07-30 (suite #6, Claude Code, opus) — PACTUM STUDIO : l'outillage devient l'éditeur
Spec de Patrick (maquette fournie) : l'outillage ne doit plus être un ensemble de pages d'audit mais **son éditeur de game design** — carte explorable d'abord, listes jamais. Philosophie posée par lui : « Pactum Studio est mon IDE, Claude reste mon moteur d'exécution » — le Studio n'écrit JAMAIS dans git.
- **Le blocage à lever d'abord** : la mécanique (choix, stats, seuils, issues) n'existe QUE dans `scene-data.ts`. `landes.json` n'en porte rien. Patrick ne lisant pas le code, **il ne voyait pas la moitié de son propre jeu**.
- **`tools/studio_data.py`** (nouveau) : lit le `.ts` — la source que le moteur exécute — et exporte `data/studio-data.json` (185 Ko) : **60 scènes · 38 points d'intérêt · 142 choix** (61 risqués avec stat + seuil + les 4 issues en prose, 52 passifs avec conséquence, 5 verrouillés, 5 orientations, 19 continuations), 42 liens explicites, la réserve d'images, croisé avec les JSON de zone (noms, lieux, coordonnées), `scene-meta.json` (descriptions, prompts) et `assets/manifest.json` (hash, poids, récence). ⚠️ Réutilise la machine à états qui **traite les commentaires** : les apostrophes des commentaires FR (« l'entrée ») cassent tout parseur naïf — piège qui avait déjà fait mentir un comptage.
- **`data/pactum-studio.html`** : carte zoomable/déplaçable à **3 niveaux** (lieux → + scènes → + points d'intérêt, mesuré 15 → 60 → 98 nœuds), catégories en couleur, 3 types de liens, cadre de région pour le Hameau, minimap, barre de statistiques, recherche (scènes, choix, points), vues **Liste** et **Réserve**. Panneau latéral à 4 onglets : Détails (narration + 3 choix), **Choix** (stat, seuil, les 4 issues, effets : objet, savoir, soupçon, serment, dette), Points d'intérêt, Métadonnées (hash, poids, description, prompt).
- **Édition** : mode dédié, les modifications vont dans un **journal `localStorage`** — jamais dans le dépôt. « Copier pour Claude » sort un rapport structuré (cible exacte, avant/après, contexte, + rappel des scripts à relancer) que Patrick me recolle. C'est le seul pont Studio → dépôt.
- **⚠️ Décision de véracité, à ne pas « corriger » plus tard** : le niveau 1 ne trace **aucun chemin entre les lieux**, parce qu'il n'en existe pas — la traversée TIRE sa destination à chaque marche. En dessiner donnerait une carte qui ment. Seul le **Hameau des Renonçants** est encadré : c'est la seule enclave géographique réelle (`HAMEAU_INTERIOR`, exporté pour ça). La légende le dit explicitement.
- Vérifié Playwright (1536×1024) : chargement, 3 niveaux de zoom, panneau sur une rencontre (INSTINCT 11 · RISQUÉ + les 4 issues en prose), mode édition → journal → compteur → localStorage, vues Liste (60) et Réserve (48), recherche. Aucune erreur JS, aucun asset en erreur.
- Déployé : `https://patrickmorvan21.github.io/Pat/aldenhar/studio.html` (+ `studio-data.json` à côté). ⚠️ **Le déploiement doit copier les deux** ; comme `couverture.html` et `atelier.html`, ils sont effacés quand on remplace le sous-dossier `aldenhar/`.
- **Reste à faire** : la disposition automatique tasse les nœuds dans les lieux denses (le Hameau) — positions à main levée à prévoir, comme dans l'atelier ; les vues Liste/Réserve n'ont pas encore de filtres ; le Journal (4e onglet de la maquette) et l'Export ne sont pas construits.

### Session 2026-07-30 (suite #7, Claude Code, opus) — Studio : refonte de la taxonomie et du zoom
Retour de Patrick, capture à l'appui : « la carte mélange les lieux, les scènes, les beats, les rencontres, les points d'intérêt, les objets, les rôles et les états comme s'il s'agissait de catégories équivalentes ». Juste — c'était un seul axe pour quatre choses. Refonte, pas correctif CSS.
- **QUATRE DIMENSIONS séparées, à ne jamais refusionner** : **NATURE** (lieu / scène / rencontre / point) portée par la TAILLE, la FORME et l'icône — la couleur n'est plus le seul moyen de distinguer ; **RÔLE** (entrée · sortie · pivot · optionnel · verrouillé) = un badge posé sur le nœud ; **ÉTAT de production** = une pastille en haut à droite, **jamais la bordure** ; **ALERTES d'audit** = des icônes distinctes en bas à droite. « Scène principale » est **supprimée** : c'était une catégorie concurrente d'un lieu, alors qu'un lieu CONTIENT des scènes. L'orange vif ne sert plus qu'à la sélection (vérifié : une seule bordure orange à l'écran).
- **Rôles et alertes calculés, jamais devinés** (`studio_data.py`) : rôles depuis `ENTRY_SCENE`, les choix verrouillés, les POI `leadsTo` (optionnel) et le nombre de sorties (pivot). ⚠️ **Découverte importante : il n'y a AUCUN vrai orphelin.** Quatre scènes n'ont pas de lien entrant mais sont parfaitement jouables — elles arrivent par la traversée, la séquence du Hameau ou le procès au Soupçon maximum. D'où un champ **`acces`** (« lien · traversée · séquence du Hameau · procès ») qui transforme une fausse alerte en information ; l'alerte « orpheline » n'est levée que si l'accès est réellement nul.
- **Hiérarchie** : le lieu devient dominant (286×124, ~2,5× une scène) avec vignette, compteurs (scènes · rencontres · points) et mode d'accès. Scènes et points sont INTERNES et n'apparaissent qu'une fois le lieu ouvert.
- **Zoom sémantique** : niveau 1 = les lieux seuls ; **ouvrir un lieu n'ouvre que lui** (les autres restent repliés) ; sélectionner une scène atténue tout ce qui ne la touche pas. Mode audit « tout déplier » en option (111 nœuds), jamais par défaut.
- **Les choix deviennent des transitions** : survoler un lien donne le libellé, le type, la stat, la difficulté, la destination et les issues.
- **Disposition — trois forces qui se contredisent, réglées par la MESURE** : la géographie dessinée à la main est normalisée, puis une phase de **cohésion** regroupe les lieux d'une région, puis une phase de **séparation** (qui a le dernier mot) garantit zéro chevauchement ; un balayage du facteur d'échelle retient la disposition **sans aucun chevauchement** dont le zoom d'ajustement approche le plus 1. Résultat mesuré : **noms lisibles à 11,4 px effectifs au chargement, 0 chevauchement** (contre 6,2 px et 32 chevauchements dans les états intermédiaires).
  - ⚠️ Trois bugs de mise en page que seule la mesure a révélés : (1) je séparais avec une hauteur de carte de 104 px alors qu'un nom sur deux lignes en fait **120** ; (2) la boucle sortait dès que la séparation ne trouvait rien — **y compris pendant la phase de cohésion**, qui venait de rapprocher les cartes ; (3) en supprimant l'exclusion j'avais supprimé la séparation avec, les deux blocs étant adjacents.
- **Le cadre de région ne ment jamais** : un rectangle autour de six lieux en englobe fatalement d'autres selon la disposition. Il n'est donc dessiné que s'il ne contient **aucun intrus** ; sinon chaque lieu membre porte un badge « Hameau des Renonçants ». Sans ce garde-fou, le cadre affirmait que la Borne Frontière était dans le Hameau.
- **Bruit réduit** : liens principaux visibles, secondaires très atténués, conditionnels masqués tant que rien n'est sélectionné. Les **statistiques passent dans la colonne de gauche** — elles ne peuvent plus recouvrir un nœud, quel que soit le zoom. Noms sur deux lignes maximum, jamais tronqués.
- ⚠️ Deux pièges CSS/UX corrigés : `#corps` a une transition de 180 ms sur ses colonnes — mesurer `#centre` juste après avoir ouvert le panneau donne l'ANCIENNE largeur et divise le zoom par deux (d'où `apresTransition`) ; et le panneau du lieu déplié (`z-index:4`) passait au-dessus des nœuds sans z-index, qui devenaient illisibles.
- Vérifié Playwright (1536×1000) sur les six critères de réussite : 15 lieux seuls à l'ouverture · 0 chevauchement · noms à 11,4 px · ouvrir un lieu n'en déplie qu'un · niveau 3 avec atténuation · légende en 4 groupes · une seule bordure orange. Aucune erreur JS, aucun asset en erreur.
- Déployé sur `studio.html` (+ `studio-data.json`).

#### Retours 30/07 (suite) — cadre du Hameau, séquences en colonnes, chevauchements
- **Le Hameau est un CARRÉ, toujours** : sa surface est désormais **réservée dès le placement** — la région est posée comme un bloc unique (grille 2 colonnes de ses membres + marge), les autres lieux se répartissent autour, jamais dedans. Le cadre contient donc exactement ses 6 membres et ne peut plus être supprimé faute d'intrus. C'est la bonne structure : dessiner un rectangle APRÈS coup autour de membres dispersés est perdu d'avance.
- **Un lieu n'est pas un tas de scènes** : le Hameau en compte 15, réparties en **5 séquences** lues dans `suite`/chainNext (l'entrée · la Femme au Seuil · la halte · la nuit dehors · le procès). Le déplié est maintenant **une colonne par séquence**, scènes dans l'ordre du jeu, points d'intérêt collés à droite de leur scène. Une queue partagée (`femme-seuil-3 → hameau-entree-3`) n'est placée qu'une fois — la flèche montre la jonction. Mesuré : 5 colonnes de 5/3/5/1/1, 12 flèches.
- **Des FLÈCHES, pas des traits** (`<marker>` SVG, trois variantes principal/secondaire/sélection) : « quelle conséquence suit mon action » se lit dans le SENS du lien. Et les liens d'une même colonne sont tracés **droit de bord à bord** — une bézier entre deux centres se cachait sous les cartes.
- **Chevauchements du lieu déplié** : ses colonnes débordaient sur les lieux voisins. Les autres lieux s'écartent de la boîte dépliée, temporairement (rien n'est mémorisé, refermer rend la carte à sa disposition). ⚠️ Un seul passage ne suffit pas — les lieux repoussés se percutent entre eux (mesuré : 7 chevauchements sur le Hameau ouvert) : on alterne « sortir de la boîte » et « ne pas se marcher dessus » jusqu'à stabilité.
- Vérifié Playwright : **0 chevauchement** au niveau 1, Hameau ouvert et Puits Condamné ouvert ; cadre de région contenant exactement les 6 lieux du Hameau ; zoom 0,95 et noms à **13,7 px** effectifs au chargement.
- **⚠️ Correctif : le Hameau lui-même n'était pas dans son cadre.** `HAMEAU_INTERIOR` est une liste de **GAMEPLAY** — les lieux que la traversée ne peut pas tirer tant qu'on n'est pas entré au village. Le Hameau n'y figure pas parce qu'il EST la porte d'entrée : il doit rester tirable avant. Je l'ai prise pour une liste géographique, et le lieu « Le Hameau des Renonçants » (15 scènes) se retrouvait hors de son propre cadre. L'export calcule maintenant `regions[].lieuxEnPlus` : le lieu qui porte la séquence d'entrée ou de halte (`hameauEntree` / `hameauHalte`) est rattaché à la région. Le cadre contient les **7** lieux du village. Leçon générale : une liste du moteur encode une règle de JEU, pas une vérité de carte — vérifier ce qu'elle veut dire avant de la réutiliser pour de l'affichage.

### Session 2026-08-07 (Claude Code, fable, session distante) — lot d'images 7/08 + Témoin ruelle v5_d (v1.45.0)
Patrick : « je viens de mettre à jour les photos dans le Drive, met à jour le jeu », puis « je viens de mettre de nouvelles photos », puis remplacement explicite du Témoin de la ruelle (`v5_c` → `v5_d`). Tout importé, câblé, testé, déployé (gh-pages `47655c2`, Pages build **success**).
- **30 images importées depuis `03_Validé`** (21 gagnantes + 9 résolutions de départage) + `monstre_temoin_ruelle_v5_d` en toute fin. Technique inchangée : `download_file_content` par petits lots → `scratchpad/extraire0708.py` lit le b64 par titre dans le `.jsonl`, vérifie signature PNG + taille exacte du manifeste (`data/lots/2026-08-07-nouveaux-visuels.json`). ⚠️ **Gros fichier (>limite de tokens) = le harnais décharge le résultat dans `tool-results/*.txt`** — le v5_d (119 808 o) a été extrait DIRECTEMENT de ce fichier JSON en python, zéro coût de contexte : c'est le meilleur chemin pour tout fichier volumineux.
- **⚠️ Découverte : la curation de Patrick passe par la SUPPRESSION Drive.** Les fileIds des variantes à départager (a/b/c) renvoyaient 404 — il avait effacé les rejetées ; la survivante par sujet = son choix. Départage résolu sans lui demander : fille_moulin_ouvrage=c, fossoyeur_trou_1=c, fossoyeur_trou_3=a, murets_vers_sud=c, gamin_caillou=c, gamin_depart=c, troupeau_compte=b, moulin_crete_interrompue=b, colline_pied_grand_gibet=b, **meute_grise_2 entièrement supprimée = sujet rejeté** (on garde `monstre_meute_grise_c.png`). Consigné dans le manifeste du lot (`resolution_departage_7aout`).
- **Câblage** : 21 repointages + 7 insertions de POI dans `scene-data.ts`, 2 icônes Besace réelles (lunette-guet, clochette-meneuse — placeholders retirés), 2 reliques (`relique_vertebre_gravee`/`relique_corde_seche` — chemins DÉRIVÉS au runtime `assets/relique_{id}.png`, donc sauvées SANS suffixe de variante). 6 anciennes lignes placeholder supprimées. Toutes les images VUES avant câblage (règle du moulin ailé) ; Grand Témoin (v3_a/v4_c/v5_d) respecte le garde-fou §8 (ni visage ni membres).
- ⚠️ **Pièges de la passe** : (1) **TS1117 propriétés dupliquées** — 6 POI avaient DÉJÀ une ligne `illustration` (placeholder) ~15 lignes plus bas que ma fenêtre de pré-vérification (10-12 lignes, trop courte) → retirer l'ancienne ligne, la nouvelle image dédiée gagne ; (2) **Studio « 19 introuvables »** = manifeste d'assets périmé, `npm run gen:manifest` (depuis `aldenhar/`) le règle → 0 ; (3) `build:pages` ÉCHOUE sur `check_coherence` si `landes.json` diverge → `python3 ../tools/check_coherence.py --fix` (4 champs réalignés) ; (4) **repo GitHub API = `pat` MINUSCULE** (`Pat` → 404 sur les workflows) ; (5) panne du classifieur Bash en cours de route — les outils de fichiers et le MCP Drive continuent, reprendre les écritures après.
- Vérifié Playwright (7/7) : 6 scènes seedées servent leurs nouveaux fichiers + le POI compter-troupeau sert `monstre_troupeau_compte_b` à l'examen. Couverture après lot : **dédiée 132 · héritée 3 · fallback 0 · manquante 0** · 46 prompts à écrire · 68 orphelins. Lint/typecheck/build propres.
- **À signaler à Patrick (ménage Drive, lecture seule côté session)** : `monstre_temoin_ruelle_v5_c.png` traîne encore dans `03_Validé_Rencontres`, remplacé par v5_d (retiré du repo).
- **Volontairement PAS importé** : les lots de 12h06/12h15 dans `01_En attente` (temoin_ruelle_v6_b, grand_temoin_v5_a/b/c, grange_toit_v3_a/b/c) — pas validés ; à importer quand ils passeront dans `03_Validé`. L'instruction explicite de Patrick désignait v5_d (déjà dans `03_Validé`), appliquée.
- **En attente par ailleurs** : maquette de la page de personnalisation des reliques (« tu ne peux porter que 3 reliques », Figma 1925-524) ; arbitrage moulin (le `campement` montre toujours le moulin AILÉ alors que `scene_moulin_sans_ailes_d_d.png` existe — un seul champ à repointer sur sa décision).
- `APP_VERSION` 1.44.0 → **1.45.0**, `CACHE_VERSION` v73 → **v74**. Déployé (jeu + couverture.html + atelier.html + studio.html + studio-data.json).

#### Témoin ruelle v6_b « mise en Validé » par procuration (v1.45.1)
Patrick (« Peux-tu mettre monstre témoin dans validé et la mettre dans le jeu ? Je ne peux pas le faire actuellement ») : la v6_b attendait dans `01_En attente_Rencontres`. **Découverte d'outillage : le connecteur Drive SAIT copier** (`copy_file` + `parentId`) — la « lecture seule » ne vaut que pour déplacer/supprimer. `monstre_temoin_ruelle_v6_b.png` copiée dans `03_Validé_Rencontres` (15 619 o, taille identique), importée (extraction transcript, PNG 1000×1000, 2 couleurs exactes), regardée (corbeau en silhouette devant la ruelle éclairée — garde-fou §8 OK), câblée sur `temoin-ruelle` à la place de v5_d (fichier retiré du repo). Vérifié Playwright (la scène sert `v6_b?v=6e0b95d8`, 0 erreur). Ménage Drive côté Patrick : l'ORIGINAL v6_b reste dans `01_En attente` (copie, pas déplacement) + `monstre_temoin_ruelle_v5_c/_v5_d` périmées dans `03_Validé`. `APP_VERSION` **1.45.1**, `CACHE_VERSION` **v75**. Déployé (gh-pages `ef60447`, Pages success).

#### La porte de l'intro ANIMÉE — vidéo → sprite tramé (v1.46.0)
Patrick a créé `Assets/VIDEO/Proglogue` sur le Drive (2 mp4 Midjourney de la porte, 12,8/14,6 Mo) : « à la place d'une image mets une vidéo de la porte "une seule vie" — pas juste poser la vidéo, la transformer en CSS comme la dernière fois ».
- **Transfert** : le connecteur Drive refuse au-delà de **10 Mo** (nouveau plafond, mieux que l'ancien ~1 Mo « session expired ») et `drive.google.com` reste bloqué par le proxy (403) → Patrick a envoyé le mp4 en PJ, arrivé **directement sur disque** (`/root/.claude/uploads/<session>/...`) — plus besoin d'extraction transcript pour les PJ désormais. ffmpeg ABSENT de la machine : `pip install imageio-ffmpeg` fournit un binaire statique fonctionnel (`imageio_ffmpeg.get_ffmpeg_exe()`).
- **Pipeline** : 5,08 s · 1080×1080 · 24 fps → 16 frames équiréparties 390×390 → **étalonnage source** (plancher noir 0,06 + gamma 0,42 — les frames vidéo sont bien plus sombres que les sources Leonardo : le pipeline canonique brut rendait 0,2 % d'orange, un carré noir ; l'étalonnage se fait EN AMONT, les paramètres de dithering restent verrouillés) → `dither()` canonique → sprite vertical `assets/intro_porte_anim.png` (390×6240, palette 2 couleurs, 164 Ko).
- **Lecture** : `.porte-anim` (globals.css) — `steps(15) infinite alternate`, 320 ms/frame (§5 SKILL), **ALLER-RETOUR** car la vidéo est une montée continue de lumière (un loop sec couperait) : la lumière derrière la porte enfle puis se retire, comme une respiration. Bande de dissolution reposée en overlay `.dissolve-bottom` (les frames ne l'ont pas cuite, contrairement aux extraits Figma). `prefers-reduced-motion` + `anim-reduced` → frame 13 fixe (la porte entière, équivalent de l'image Figma). `Clause.animSprite` optionnel dans `Intro.tsx` — `image` reste le repli.
- Vérifié Playwright : clause 2 = `.porte-anim` + sprite hashé + bande, 8 positions distinctes en 2,4 s (pas de -390 px = 1 frame/step), animations réduites figées à -4680 px, 0 erreur. Le 2e mp4 (`_1`) n'a pas été utilisé — celui envoyé par Patrick fait foi.
- `APP_VERSION` **1.46.0**, `CACHE_VERSION` **v76**. Déployé (gh-pages `9619e9c`, Pages success).

#### Retouche : lecture unique (v1.46.1)
Retour Patrick (« pas de boucle, on reste sur la dernière frame, avec toujours le dégradé de pixels en bas ») : `.porte-anim` passe de `steps(15) infinite alternate` à **`steps(15) 1 forwards`** — la lumière enfle une fois, la porte reste embrasée sur la frame 16 ; animations réduites figées sur cette même frame finale (l'état de repos). La bande `.dissolve-bottom` était déjà posée en overlay et reste. Vérifié Playwright (début −390 → fin −5850 px, stable à +1,5 s, bande présente, 0 erreur). `APP_VERSION` **1.46.1**, `CACHE_VERSION` **v77**. Déployé (gh-pages `1649014`).

### Session 2026-08-07 (suite #2, Claude Code, fable) — PLAYTEST COMPLET : 6 runs auto-jouées + diagnostic global
Demande Patrick : « teste mon jeu, plusieurs runs, regarde au max les alternatives, la cohérence des textes, scènes et images entre elles, puis un diagnostic détaillé ». Livré : **`data/diagnostic-playtest-2026-08-07.md`** (6 runs Playwright réelles — drags du dé compris — stratégies first/last/explore/random/risky + fin de traversée ciblée, ~1 600 écrans capturés, croisés avec les audits statiques).
- **État technique : sain.** 0 erreur JS, 0 asset en échec, 0 image cassée sur toutes les runs ; densité dans la grille ; 33 scènes/le procès/la mort/la Descente atteints en jeu réel.
- **Incohérences trouvées (détail et lignes exactes dans le diagnostic)** : réactions du monde des ÉTATS sans garde de contexte (le « barrage » de FIXÉ servi face à la Bête, Scene.tsx ~1576) ; narration de la Meute qui présuppose le hameau (scene-data ~4778) ; **écran de la Descente sur `dithering-portal.jpg`** (dernier placeholder du jeu, sur l'écran le plus rare) ; conséquence de combat lue sur l'image de marche ; sortie du village non dite après la séquence d'entrée (le Seuil classé dehors) ; Lisières vs Landes visible en jeu ; moulin ailé (3e rappel) ; « Tu uses » (Scene.tsx:2308) ; lignes intruses répétables verbatim ; drops génériques dupliqués ; `campement` absent d'INDICE_ROUTE ; la Bête des Chemins Creux tirée 5/6 runs et enchaînable avec « son » lieu.
- **Deux déséquilibres systémiques mesurés** : (A) l'économie du JOUR à l'envers — la run la plus exploratrice finit JOUR 1, le Registre classe par jours → échouer classe mieux que réussir, et les Besoins (en jours) ne se déclenchent jamais ; (B) **survivre ne laisse AUCUNE trace** (Descente = resetRun sec, pas de Registre/mémoire/marqueur — manque n°1 du jeu à mes yeux, volet-2 du 21/07 jamais construit). + Soupçon à 4-6 dans toutes les runs exploratrices (procès quasi garanti pour un joueur curieux — à surveiller).
- **Banc de test réutilisable** : `scratchpad/joueur.mjs` (auto-joueur générique : stratégies, seeds JSON, journal par écran). ⚠️ Pièges durement acquis : `hasText` ne matche JAMAIS un libellé qui inclut le tag de stat (span sans espace dans le textContent — retirer « COURAGE|RUSE|… » du filtre) ; exclure des candidats les cartes d'état, « Ne plus afficher », et les onglets LES 100/TES MORTS de l'écran de mort ; `pkill -f` dont le motif apparaît dans sa propre ligne de commande tue le shell de l'outil (exit 144, re-mordu 2×) — écrire le motif avec une classe (`jou[e]ur`).
- Aucun code du jeu modifié (mission = diagnostic). Rapport commité dans `data/`.

#### Correctifs playtest 3→5 (v1.47.0) — sur feu vert Patrick (« Fais les correctifs de 3 à 5 »)
Les chantiers 1-2 (trace du survivant, économie du Jour) restent EN ATTENTE d'arbitrage — options posées à Patrick en fin de message (ce sont des décisions de design : ce que gagne un survivant, sur quoi classe le Registre, comment le temps passe).
- **Réactions d'état gardées par le contexte** : `Etat.reactionsPartout?: number[]` (indices jouables hors village — seules « le chemin monte » de Boiteux, « l'odeur de corde » de Hanté et « Pas par là » du Gamin le sont) ; l'injection (Scene.tsx) filtre par `dansLeVillage()` (helper module : intérieur + toute la séquence hameau-*/serment/femme-seuil/gamin) et **jamais pendant un combat**.
- **Meute** : « Passé les murets du hameau » → « Loin de tout muret » (ne présuppose plus la provenance).
- **La Bête EMBUSQUE son creux** : retirée du pool/APPROACH/INDICE_ROUTE ; la branche toDest d'advance() la joue à la 1re route vers `chemin-creux` (visited compté immédiatement, `trav.current` sur la Bête pour la reprise), son `chainNext: "chemin-creux"` enchaîne sur le lieu sans Croisée. `studio_data.py` : accès « embuscade » + accès dédiés variantes/renoncer/troupeau (les 7 fausses « orphelines » du Studio ont disparu).
- **L'image du combat TIENT pendant sa conséquence** (liaison seulement) : `imageApresConsequence` ref — vue de marche appliquée au `nextChunk` OU à la révélation des choix (liaison courte d'un seul écran). ⚠️ Piège : `showScreen` purge les bascules différées → la poser APRÈS l'appel (`differeVueDeMarche` locale), sinon on s'auto-efface (trouvé au test).
- **Sortie du village dite après la séquence d'entrée** : le `sort` du franchissement traite serment-hameau/hameau-* comme intérieur (l'`entre` garde le strict intérieur — la narration du Seuil raconte déjà l'arrivée).
- **Lot** : rappel REPRENDRE « Les Lisières » → « Les Landes » (Lisières = nom d'ACTE sur son carton uniquement) ; `campement` repointé sur `scene_moulin_sans_ailes_d_d.png` (le moulin AILÉ `scene_moulin_campement_a` reste en réserve) ; « Tu uses » → « Tu sors « X » de la Besace » ; lignes intruses mémorisées (`run.intrusesVues`) ; drops génériques dédupliqués (`randomSoinMineur(exclure)` + `run.dropsServis`) — les 2 champs ajoutés à `loadRun` DANS LE MÊME GESTE ; prompt `scene_la_descente` écrit dans images-a-produire.md (asset à générer, l'écran reste sur le portail en attendant).
- **Faux positif corrigé au diagnostic** : l'« indice de route manquant du Moulin » n'existait pas — la clé `campement:` est SANS guillemets, ma regex d'audit la ratait (18/18 couvertes). Barré dans le rapport.
- Vérifié Playwright **8/8** (embuscade complète A1-A5 avec vrai jet, image tenue puis relais B1-B2, sortie dite C1). ⚠️ Pièges de test : cumuler le texte écran par écran (bannière/approche vivent sur les premiers chunks) ; les ports 8901/8902 restent bloqués par des serveurs zombies après un shell tué — prendre un port neuf plutôt que pkill.
- `APP_VERSION` **1.47.0**, `CACHE_VERSION` **v78**. Déployé (gh-pages `5431501`).

#### Arbitrages 1-2 tranchés par Patrick, implémentés dans la foulée (v1.48.0)
Réponses (AskUserQuestion) : **le Jour avance en marchant** (option recommandée) ; **le survivant = Registre + mémoire + Geôlier** (« et on avait parlé aussi d'un gain de Sceau qui modifie la zone des Landes — temps 2 si nécessaire » : le Sceau des Landes reste à specifier, noté dans le commentaire de `zonesCleared`).
- **Le Jour de marche** : +1 Jour tous les 3 lieux traversés (`trav.visited.length % 3 === 0`, posé dans la branche toDest d'advance, `jourDeMarche`) — puce « — JOUR X — » en TÊTE de l'écran d'arrivée (avant couture/approche), `run.day` dans le persist, `setDay` pré-persist avec la valeur qu'il écrira. Les Besoins (comptés en jours) se réveillent mécaniquement ; le Registre redevient juste (survivre longtemps = avoir marché loin).
- **La trace du survivant** : `recordTraversee` (player-memory) — `zonesCleared+1`, totalDays/bestDays, `fallen.unshift({cause: "a franchi la Descente", place: "la-descente"})` (le Registre l'affiche sans aucun changement d'écran), `derniereFinTraversee: true` (remis à false par recordDeath ET recordRenoncement). Champs ajoutés au TYPE + à `loadMemory` DANS LE MÊME GESTE (la règle). Appelé dans la branche `scene.terminal` non-renoncement de Scene.tsx, AVANT resetRun. Aucune relique (on ne forge rien d'une vie qu'on n'a pas perdue), `deaths` intact.
- **L'accueil du Geôlier regarde le survivant en face** : `DeathContext.traversee?` + 3 citations `trav-a/b/c` (« Tu l'as traversée. Ne prends pas ça pour une sortie… ») prioritaires car spécifiques ; Home sert une citation dès que le compte a un passé (`deaths > 0 || zonesCleared > 0` — un survivant sans aucune mort a aussi son accueil).
- Vérifié Playwright **8/8** (D : run.day 1→2 au 3e lieu + puce JOUR 2 en tête d'écran ; E : Descente → zonesCleared 1, derniereFinTraversee, ligne Registre « Braise — a franchi la Descente », run reset sans résurrection, accueil « Tu l'as traversée… »). ⚠️ Re-mordu puis corrigé : garde d'addInitScript en **localStorage** (`__t_seed`), jamais `window.__s` — le reload de fin de run ré-exécute l'init et écrase la mémoire qu'on veut lire.
- `APP_VERSION` **1.48.0**, `CACHE_VERSION` **v79**. Déployé (gh-pages `4affcc2`). Studio/couverture régénérés.

#### Retours playtest iPhone (captures IMG_5828/5829, v1.49.0)
Deux captures (build v1.46 — une partie des griefs déjà soldée en v1.47/1.48) + 6 retours. Tout livré, vérifié Playwright **12/12**.
- **Mention d'états sticky (capture 2)** : `etatLigne` (die-etats) n'était JAMAIS re-cachée après l'armement — elle collait en bas de tous les écrans. Cachée au lancer (toStage) ET au dismiss du verdict (Die3D).
- **Dosage des états négatifs** : plafond de DEUX parmi {fievreux, boiteux, affame, marque, hante} — un 3e ne se pose pas (`ETATS_NEGATIFS_DOSES`, garde dans poserEtatRun). FIXÉ exempt (mécanique du procès, posée par le village).
- **Paliers du Soupçon gardés par le village (capture 1 : la Doyenne à la Colline)** : les 5 manifestations mettent toutes des villageois en scène → le palier ATTEND la prochaine arrivée `dansLeVillage()` (soupconSeen ne rattrape qu'à l'affichage ; le Soupçon compte partout, il ne se LIT qu'au village).
- **Procès jamais pendant la séquence d'entrée** (« je venais de manger… emmené direct au tribunal ») : la branche soupçon≥6 exclut `serment-hameau|hameau-entree|hameau-accueil` — le héros SORT, et le hameau l'attend « au tournant du muret » (l'ouverture du procès le raconte déjà). + l'issue « Prendre le hameau à témoin » ne fait plus NOMMER le Bailli par le héros (« celui qui pend là-haut sans que personne n'ose plus dire son nom »).
- **Safe-area iOS** : burger ET croix du menu à `top: calc(env(safe-area-inset-top,0px)+11px)` (identiques — règle d'alignement du 16/07 ; hors PWA env()=0, rien ne bouge).
- **Étapes du Seuil** : 5 carrés (4 souvenirs + le Nom) sous les questions, langage des points de l'intro (jamais une barre).
- **La Descente** : `illustration: scene_landes_liaison_sud_c` (vue générique du sud, demande explicite « mets une photo générique ») — `scene_la_descente` reste le seul asset dédié à générer (prompt prêt).
- ⚠️ Pièges : le libellé des boutons d'orientation = APPROCHE (« Vers un marché muet »), jamais l'INDICE_ROUTE (« bâches tendues ») ; pour seeder un état réel : `run.faits = { fievreux: {id,kind:"state",scope:"run",value:1} }` (les `effects` hérités ne nourrissent pas etatHints) ; ports 8901-8903 zombies → 8904.
- `APP_VERSION` **1.49.0**, `CACHE_VERSION` **v80**. Déployé (gh-pages `822488a`).

#### L'AUDIT D'IMMERSION devient automatique (v1.49.1) — réponse au « c'est toi qui es censé savoir »
Patrick : « L'idée est que je ne fasse pas une capture pour chaque même anomalie, c'est toi qui es censé savoir si c'est cohérent ou pas. » Il a raison — toutes les ruptures du 7/08 étaient de la MÊME famille (texte écrit pour un contexte, servi dans un autre), donc détectables mécaniquement.
- **`tools/immersion.py`** (nouveau) : extrait chaque POOL de textes injectés à l'exécution (manifestations/réactions/intruses/guérisons des états, paliers du Soupçon, ambiances de liaison, bifurcations, loi du Domaine) AVEC le garde que le code applique réellement ; classe chaque texte par ce qu'il PRÉSUPPOSE (lexiques GENS / VILLAGE / LANDE) ; signale tout texte qui présuppose plus que son garde ne garantit. Exemptions EXPLICITES et justifiées dans le fichier (3 : l'habitude d'Affamé, la comparaison de l'ombre, le hameau-sujet de la guérison de Fixé).
- **Branché en GARDE DE BUILD** (`prebuild` : check_coherence && gen:manifest && immersion --strict) — un nouveau texte hors contexte CASSE le build au lieu d'attendre une capture de Patrick. ⚠️ Tout NOUVEAU pool d'injection dans Scene.tsx doit être déclaré dans `pools()` — c'est le prix de l'audit, dit en tête du script.
- **Premier passage : 4 signalements, 2 vraies ruptures jamais vues** — deux lignes intruses de HANTÉ supposaient un mur et une foule en pleine lande (« À ta gauche, il y a un mur », « Tu comptes les gens autour de toi ») → réécrites sans décor (« rien ne marche nulle part », « les ombres, les pierres »). Vérifié : « Personne ne relève » est une RÉACTION village-gated, pas une intruse — l'audit avait juste.
- Rituel acté pour les prochaines sessions : après tout changement de contenu/injection, `immersion.py` + une passe de `joueur.mjs` AVANT de déployer — la détection m'appartient, les captures de Patrick redeviennent l'exception.
- `APP_VERSION` **1.49.1**, `CACHE_VERSION` **v81**. Déployé (gh-pages `36efaae`).

### Session 2026-08-10 (Claude Code, opus) — LES SIX VAGUES DU PANEL, vagues 4-5-6 (v1.62.0 → v1.64.0)
Fin du chantier ouvert par le panel de 10 IA (mandat Patrick : « revoir tout le système afin de répondre à tous les problèmes liés au jeu »). Les vagues 1-3 étaient livrées ; voici les trois dernières, chacune construite comme un MÉCANISME général puis prouvée par un garde de build et des jets réels.

#### Vague 4 — le déjà-vu à trois portées + la Borne qui se souvient (v1.62.0 / v101)
- **`lib/dejavu.ts`** : un registre unique à trois portées (`ecran` / `run` / `compte`), **un COMPTEUR par clé et jamais un booléen** (amendement explicite du panel) — c'est ce qui permet au texte de dire « la deuxième fois » au lieu de signaler « déjà vu ». `RunState.vus` et `PlayerMemory.vus`, chacun ajouté à son `load*` dans le même geste. ⚠️ Dette assumée et documentée dans le module : les ~12 registres ad hoc antérieurs (`poiSeen`, `reactionsVues`, `liaisonVues`, `jailerVues`…) **ne sont pas migrés** — une migration en bloc réécrirait la lecture de sauvegardes existantes sans rien apporter au joueur. Règle pour la suite : tout nouveau besoin de « déjà vu » passe par là, et un registre ad hoc qu'on retouche pour autre chose en profite pour migrer.
- **`FAMILIARITE`** (scene-data) : **34 lignes, 17 lieux × 2 strates**, servies à l'arrivée au 2ᵉ puis au 4ᵉ passage du COMPTE (`visitesLieux`). C'est la réponse au « ~60 % de relecture verbatim en vie 2 » mesuré par le panel, et ça remplit enfin les champs `textes.familiarite` / `recontextualisation` de `landes.json`, à `null` depuis le 20/07 (14 lieux synchronisés — deux scènes partagent un lieu déjà pourvu, la Meute est une errante sans fiche de lieu).
  - **Règle d'écriture, non négociable** (inscrite sur la table) : le héros vient de naître et ne se souvient de RIEN. Aucune ligne ne dit « tu reconnais ». Ce qui porte la mémoire, c'est le MONDE (une trace, un objet déplacé, une usure à ta forme) ou le CORPS (la main qui trouve la prise avant l'œil) ; quand une ligne frôle la connaissance, l'ignorance est dite dans la phrase même (« et tu ne sais pas comment tu le sais »).
  - `borne-frontiere` en est volontairement ABSENTE : son écran d'ouverture est déjà le plus instrumenté du jeu (trace de la mort précédente, dette de relique, envFlags, fixations).
- **La Borne qui se souvient** (`PointInteret.borneSud` + `ligneBorneSud`) : l'examen des gravures posait la question « alors qui a gravé côté sud ? » depuis le 20/07 et ne répondait jamais. Une des trois marques du sud porte désormais le **nom du prédécesseur**, avec **une entaille par vie perdue** — et une ligne entièrement différente si ce prédécesseur a **SURVÉCU** (`cause` contenant « franchi ») : il est revenu graver, ce qui contredit la règle que l'examen vient d'énoncer. Rien à la première vie : c'est le silence qui rend la deuxième lisible.
- `PNJ_MEMOIRE` dédupé en portée run (le Fossoyeur est déclaré sur deux scènes qui partagent son lieu).
- **Garde** : `immersion.py` audite la nouvelle strate (34 pools, garde dérivé de la destination comme `APPROACH_NARRATION`) — **prouvé sur son mode d'échec avant branchement** (un mot de village injecté dans une ligne de lande est bien signalé).
- **Réplique** : `pactum.py` gagne une **mémoire de compte** (`compte.json` : passages par lieu, morts, noms). Sans elle le kit faisait passer tous les systèmes inter-vies pour absents — le biais exact documenté le 9/08. Mesuré : 1 strate lue en vie 1 (collision de chaîne de test) contre 4 en vie 2.
- Vérifié **17/17** en Playwright (jets réels).

#### Vague 5 — le Soupçon se lit dehors, et l'échec dur ferme une route (v1.63.0 / v102)
- **`SOUPCON_CRAIE`** : cinq marques à la craie qui **MIGRENT** — un muret, la besace, la manche, le bras, puis le dos. Depuis le 7/08 les cinq manifestations attendaient le village (elles mettent des villageois en scène) ; effet de bord trouvé par le panel : **le Soupçon monte dehors, sur des actes commis dehors, et ne se lit qu'à la porte du hameau** — la mine s'arme au large et saute au seuil. La craie ne demande personne. Les deux pistes marquent `soupconSeen` : un palier franchi se voit désormais partout. Contrainte d'écriture : aucun bâti, et la marque est toujours découverte APRÈS coup.
- **`SOUPCON_GEOLIER`** : il est le seul à voir les chiffres, et le Soupçon était le seul système majeur à n'en montrer aucun. Une ligne par palier — donc aucune ne peut se répéter dans une vie. Il ne prévient pas, il constate.
- **`ROUTE_FERMEE`** : un échec dur hors séjour n'avait **rien** à dépenser (le séjour, lui, consomme une option depuis la vague 3). Il arme désormais la Croisée suivante, qui n'offre plus qu'**UNE** direction, avec la raison dite en monde. Coût réel et diégétique — le panel avait explicitement écarté le re-durcissement du barème de santé, qui ne se lit nulle part. Porté par `trav.routeFermee` : **la reprise rebâtit la Croisée fermée à l'identique** (vérifié). Un seul échec ferme une seule route (drapeau consommé).
- **Garde** : les trois pools entrent dans `immersion.py` (304 pools). ⚠️ **Faux ami trouvé et documenté** : le lexique VILLAGE attrape « la porte » — y compris le VERBE (« quand on la porte ») ; il a signalé un texte parfaitement en contexte. Le texte a été reformulé (meilleure prose au passage) et l'avertissement est inscrit au-dessus du lexique.
- **Kit** : ⚠️ `record()` de `export_run_kit.py` ne lisait **pas les clefs NUMÉRIQUES** — un `Record<number, string>` ressortait en dict vide, **sans le moindre avertissement**. Corrigé ; les paliers, la craie et le Geôlier voyagent, et la réplique les joue (mesuré sur une vie : 3 craies, 2 lignes du Geôlier, 1 route fermée).
- Vérifié **13/13** en Playwright (dont un échec dur réel qui arme la fermeture).

#### Vague 6 — une signification par traitement visuel (v1.64.0 / v103)
- **Le verrou cesse d'être une panne.** Un choix VERROUILLÉ se rendait à `opacity: 0.4`, **exactement comme les fonctions mortes de l'écran Options** : quatre sens partageaient un lavis (verrouillé, désactivé, secondaire, voilé), et le plus intéressant des quatre — une porte qui existe, qu'un AUTRE héros aurait ouverte — se lisait comme un bouton en panne. Désormais : **le libellé reste à pleine opacité** (il faut le lire et le regretter), **le CADRE dit le refus** (nouvelle classe `.frame-px`, une file de pixels espacés au lieu d'un trait continu — l'exact inverse de `.bite`, qui MANGE le trait pour l'érosion de santé), et le **tag de stat passe en blanc-50** : cette stat-là n'est pas la tienne, l'orange reste réservé à ce que tu peux engager. Le losange d'accessibilité (4/08) est conservé. Le lavis d'opacité redevient le signe du MORT, rien d'autre.
- **Le traitement n'avait presque rien à montrer** : 4 verrous dans toute la zone. **Six de plus**, écrits dans la matière de leur propre scène et répartis sur les quatre stats (COURAGE 2 · RUSE 3 · INSTINCT 2 · EMPATHIE 3) — l'Hésitant (suivre son regard : il ne fixe pas le sud), le Marcheur (compter ses pas : la règle est un multiple de onze), le Veilleur (regarder par-dessus : les marches de la Descente sont balayées), les Époux (il ne compte pas ses coups de bêche, il récite onze prénoms), la Mare (le second Renonçant ne se penche jamais : c'est un témoin), la Veuve (« pourquoi vous ? » — c'est elle qui a tressé la corde la première fois). Les six écrans restent à **3 CTA**.
- **L'ANNEAU : MESURÉ avant de toucher.** Une encoche pleine porte **8,1× l'encre** d'une rongée (35,8 vs 4,4 ; 46 px allumés contre 27). La lecture est franche — je n'ai donc **rien changé** à un élément de maquette VERROUILLÉE sur une simple impression. Le banc de mesure est réutilisable.
- « L'image suit le beat » et « le moment visuel du Destin » étaient déjà en place (image par beat depuis le 24/07, contour de pixels blancs du Destin depuis le 7/08) : rien à refaire.
- Vérifié **23/23** en Playwright, **par mesure du rendu** (opacité calculée, largeur de bordure, comptage des pixels de cadre, couleur du tag) et non à l'œil.

#### Notes transverses de la session
- **Paquet de playtest** : les 2 transcripts embarqués datent de **v1.59.0** — l'outillage qui les enregistre (`joueur.mjs`, `formater_transcripts.py`) vivait dans le scratchpad d'une session précédente et n'a pas survécu. Le LISEZMOI le dit maintenant explicitement et liste les cinq chantiers absents des transcripts mais bien présents dans la réplique jouable, pour éviter le faux « pas implémenté » (piège du 9/08). À reconstruire quand on refera un lot de transcripts.
- ⚠️ Un message de commit passé en ligne de commande avec des **backticks** se fait manger par bash (deux noms de classe ont disparu du message). Passer par `-F fichier` dès qu'il y a du code dans le texte.
- ⚠️ `coverage.py --web` / `atelier.py --web` prennent un **chemin de FICHIER**, pas un dossier : donner un dossier écrit le HTML *à la place* du dossier, et le second script écrase le premier sans rien dire.
- Règle du worktree (6/08) appliquée mécaniquement aux trois déploiements : `cd /home/user/Pat` **avant** `git worktree remove`, jamais en chaînage, puis vérification `git log -1` + branche + `ls aldenhar/lib`.
- Déployé : gh-pages `ae3e904`, `7454855`, `95db78c` — les trois builds Pages en **success**.
- **Reste ouvert, en attente de Patrick** : maquette de personnalisation des reliques (Figma 1925:524, « 3 reliques max ») · le Sceau des Landes pour les survivants · l'asset `scene_la_descente` · la notation « J41 » du Registre · le pacte du 4ᵉ slot du Geôlier (Acte I ou II) · ménage Drive (`monstre_temoin_ruelle_v5_c/_v5_d` périmées, l'original v6_b encore dans `01_En attente`).

### Session 2026-08-10 (suite #2, Claude Code, opus) — relecture par agents : quatre fuites de Jour, cinq textes recalés (v1.69.4)
Patrick : « ils ont dit plein de choses pertinentes des erreurs qu'ils ont vues et j'ai l'impression que tu ne les as pas forcément vues. Donc revérifie. » Il avait raison : quatre points des rapports d'agents n'avaient eu aucune suite, et six agents relancés (3 audits image↔texte sur les 89 écrans illustrés, 3 relectures de doctrine/économie/optimisation) ont trouvé des défauts que **mes propres commits du matin venaient d'introduire**.

**L'ÉCONOMIE DU JOUR — quatre fuites, toutes de la classe que Patrick avait signalée le matin même** (une mécanique qui donne du score au joueur qui ne joue pas) :
1. **« Repartir sans s'attarder » offrait un Jour gratuit et muet.** `Scene.nuit` porte l'aube sur la SCÈNE, or deux scènes de nuit ont une sortie qui n'est ni dormir ni veiller : le libellé disait qu'aucun temps ne passait pendant que la mécanique créditait un Jour. Nouveau **`Choice.sansNuit`**. Mesuré : J4→J4 en repartant, J4→J5 en dormant (horloge 3→4, pas 5 : la nuit n'est plus comptée deux fois).
2. **Un lieu à rencontre optionnelle se créditait TROIS fois.** Le crédit se prend au changement de radical d'id, et `verger-noir → epoux → verger-noir-2 → liaison` en franchit trois : prendre le détour valait un Jour entier de plus, invisiblement, à cinq endroits de la zone. Nouveau `trav.credites` **+ `estUnLieu()`** — une rencontre n'est pas un lieu, exactement comme elle n'entre pas dans `visited`. Mesuré : 1 lieu crédité au lieu de 3.
   - ⚠️ **Piège attrapé au test, à ne pas refaire** : `radical("meute-grise-1")` vaut `"meute-grise"`, donc un `in APPROACH` nu aurait **cessé de créditer le seul combat à deux beats de la zone**. `estUnLieu` normalise les deux côtés par `radical`. Vérifié 20/20 en test unitaire (node `--experimental-strip-types` + hook de résolution `.ts`).
3. **`horlogeApres` écrasait l'heure que la nuit venait d'ajouter** — calculé en tête d'`advance()`, donc avant l'incrément, et posé après lui. Inatteignable aujourd'hui, piège posé pour la première scène de nuit tirable.
4. **`besoins.dormir` daté d'un cran dans le futur** (`+1` sur une horloge déjà incrémentée) : le besoin durait quatre crans au lieu de trois et ne s'accordait pas avec `repondreAuBesoin`.

**DEUX COÛTS QUI NE SE POSAIENT NULLE PART** :
- Un **échec surnaturel ne coûtait rien** quand le plafond de deux états négatifs bloquait l'état. `poserEtatRun` rend maintenant un booléen ; le repli prend de la santé **et le dit** (« ça ne s'accroche pas : ça prend, simplement »).
- **« Observer d'abord, à couvert »** facturait le Soupçon à la sélection ET à l'échec — alors que sa réussite dit mot pour mot « tu les as vus avant qu'ils te voient ». Le choix curieux était strictement dominé. Seul l'échec coûte : **le Soupçon naît d'un acte, jamais du regard** (règle du 8/08).

**POSITION TENUE, désormais écrite dans le code** : `engageIci` est posé au **LANCER**, pas à la réussite. L'échec se paie déjà en santé, en état et en route fermée ; le faire payer aussi sur l'axe du SCORE ferait trancher le dé à la place du joueur — la même inversion que le Jour-sanction.

**CINQ TEXTES RECALÉS SUR LEUR IMAGE** (l'image était bonne, la prose fautive) : Borne « haute comme un homme » → *plus haute qu'un homme* (les 3 vues validées s'accordent sur un monolithe) · Guetteur *assis* → **debout** · Époux *qui bêche* → **redressé** (les deux le sont) · Colline « Neuf cordes » → *toutes ces cordes* (reliquat de la réconciliation cercle→crête) · Champ « dalles basses » → **stèles penchées** · Petit Tribunal « grange » → **salle basse** (les trois bancs restent : cités 4 fois, ils portent un POI entier) · **Meute Grise** : l'image montre **cinq bêtes de front**, pas six points d'un cercle — plutôt que jeter douze issues qui parlent de cercle, **le cadrage est retourné** : ce qu'on VOIT est le front, le cercle est ce qu'on ne voit pas, derrière. L'image devient littéralement vraie et la prose gagne en menace.

**DEUX DOCBLOCKS QUI DÉCRIVAIENT LA RÈGLE ABROGÉE** (ils auraient rejoué l'accident) : `state.ts` présentait encore « le refus coûte un Jour » comme le levier retenu ; et **le journal du 25/07 affirmait que `scene_moulin_sans_ailes_d_d` était la version SANS ailes — c'est faux**. C'est cette ligne, crue sur parole par chaque relecture, qui a fait survivre le défaut à quatre signalements : personne ne rouvrait le fichier puisque le journal disait le correctif fait. **Règle qui en sort : ne jamais écrire au journal qu'un asset montre X sans l'avoir ouvert.**

**LA RÉPLIQUE MENTAIT AUX AGENTS** (`tools/pactum.py`) : le bloc `nuit` s'exécutait à l'affichage ET `repos` re-créditait un Jour au choix, sans clé commune — le dormeur encaissait 2 Jours, le veilleur 1, soit l'asymétrie exacte que le correctif supprime dans le jeu réel. Un agent qui mesurait sur le kit concluait donc « le passif gagne », l'inverse de la vérité. Corrigé, et **`sansNuit` ajouté aux DEUX listes blanches** (`studio_data.py`, la réplique) — rappel : un nouveau champ de `Choice` n'atteint la réplique que si on le nomme là aussi.

**RE-MESURE APRÈS CORRECTIFS** (12 graines par stratégie, `compte.json` remis à zéro entre les lots — sinon `entrySoftening` contamine les mesures) : la prime du dé passe de **0,00 à +0,33 Jour**, et surtout **aucune stratégie sûre ne dépasse 3 Jours sur 48 vies**, contre 5 pour la stratégie qui lance. Le Registre classant sur la meilleure ligne, la séparation est là où elle compte (+67 %). Dormir ne paie plus en score (2,08) mais en santé, contre veiller (2,17) : l'asymétrie visée est cassée.

**⚠️ CONSÉQUENCE MESURÉE, À TRANCHER — la mort est devenue préférable à la survie.** Le meilleur score du Registre est maintenant le **lanceur suicidaire** (3,08 de moyenne, max 5, 7 morts sur 12) parce que la comptabilité du compte est asymétrique : `recordDeath` inscrit les jours, met à jour `bestDays` **et forge une relique** ; `recordTraversee` inscrit les jours et pose `zonesCleared` — **qui n'a aucun effet mécanique**. Mourir n'efface rien et rapporte en plus. C'est exactement ce que le brief d'économie interdit (« une mécanique ne doit jamais rendre la mort préférable à la survie »). **L'arbitrage de Patrick — le survivant gagne un Sceau qui modifie les Landes — y répond, et ce n'est plus un chantier « plus tard » : c'est ce qui manque pour que la balance tienne.**

**Reste ouvert** : le levier de préparation (points d'intérêt qui ouvrent l'Anneau) n'atteint que **29 % des jets définis et 0 % des combats** — les cinq combats n'ont aucun point dans leur chaîne ; c'est du contenu d'observation à écrire. Et la traversée dépasse toujours sa cible d'un lieu (cosmétique, le compteur ment d'une unité).

Six gardes verts, lint et typecheck propres. `APP_VERSION` 1.69.2 → **1.69.4**, `CACHE_VERSION` v110 → **v112**.

### Session 2026-08-10 (suite #3, Claude Code, opus) — LA REPASSE : deux gardes muets, un crédit perdu (v1.69.6)
Trois agents relancés sur mes propres correctifs du 10/08, avec pour consigne de ne rien croire sur parole. Ils ont trouvé **six défauts que j'avais introduits ou laissés**, dont deux gardes de build qui n'auditaient rien.

**LE CRÉDIT PERDU DU VILLAGE.** `estUnLieu()` (posé le matin même) reposait sur `radical()`, qui **ne retire que les CHIFFRES finaux** : `radical("hameau-accueil-table")` vaut `"hameau-accueil-table"`. Les **six accueils sur sept** du Hameau — tous porteurs d'un jet — ne créditaient donc plus leur lieu, et le village valait **1 ou 2 lieux selon un tirage invisible pour le joueur**. Corrigé par `FAMILLES_DE_LIEU` (préfixes dont toutes les variantes sont le même lieu). Vérifié en jeu, jet réel : `lieuxEngages` 0 → 1, `credites: ["hameau-accueil-table"]`.

**DEUX EXTRACTEURS MUETS — la pire classe de défaut, un garde branché en `--strict` qui n'examine rien.**
- **La loi du Domaine : 0 texte sur 4 audités, depuis toujours.** `immersion.py` cherchait l'ancre `"manifestations:"`, qui **n'existe nulle part** dans `loi-substitution.ts` (le tableau s'appelle `MANIFESTATIONS_LANDES`, ses textes vivent dans des champs `texte:`). `bloc_tableau` rendait `""` sans rien dire. C'est le **4e extracteur muet** du projet. Corrigé → il a immédiatement sorti le signalement caché : une manifestation jouée en liaison **n'importe où** disait « Le hameau dit… » à un héros qui n'a peut-être jamais vu le village. Reformulée en « On dit » plutôt qu'exemptée.
- **Un état sur sept échappait à l'audit.** Le regex exigeait `id:` juste après l'accolade ; `accompagne` porte un docblock de 19 lignes. Sa manifestation, ses deux réactions et sa guérison n'avaient **jamais** été examinées. ⚠️ `sejour.py` avait déjà documenté ce piège et l'avait corrigé **pour lui seul** — la leçon n'avait pas traversé.
- **RÈGLE QUI EN SORT** : après avoir écrit un extracteur, **compter ce qu'il rend et le comparer à la source**. Un pool à 0 entrée est une alarme, jamais un succès.

**DEUX GARDES QUI AUDITAIENT UN INSTANTANÉ.** `aiguillage.py` et `strates.py` lisent `data/studio-data.json` et ne le régénéraient **que s'il était absent** — or rien dans `prebuild` ne le régénère. Prouvé dans les deux sens : un doublon injecté dans l'instantané les fait tirer, le même doublon injecté dans `scene-data.ts` les laisse aveugles. Ils rafraîchissent maintenant dès que la source est plus récente (`export_a_jour`), et la preuve a été rejouée : le défaut injecté dans la SOURCE est désormais attrapé.

**`auditEtats` manquait ENCORE trois champs d'effet** (`usureParJour`, `soupconParJour`, `amortitCritique`) après le correctif du matin — et l'avertissement que je venais d'écrire promettait précisément qu'ils y seraient. `usureParJour` n'est pas théorique : il est lu en jeu. Vérifié qu'un état qui n'a que lui n'est plus déclaré incomplet.

**Le Studio laissait tomber deux champs vivants** : `PointInteret.borneSud` (la Borne qui se souvient) et `.troupeau` (le comptage) étaient déclarés, posés et LUS en jeu, mais absents de la liste blanche d'export — donc invisibles dans le Studio et **absents de la réplique**, où un agent aurait conclu qu'ils n'existent pas. Même piège que `sansNuit` la veille.

**Trois autres correctifs du rapport « fuites de Jour »** : le commentaire au-dessus de `run.day += 1` décrivait encore le **Jour de refus abrogé** (deuxième fois que ce commentaire décrit une règle morte) · le bloc de crédit de la branche `toDest` était **du code mort** (à ce point `scene` est toujours une liaison, dont le radical n'est pas un lieu) et donnait l'illusion de deux compteurs concurrents · un besoin échu sur un tic de **nuit** n'était constaté qu'au tic de marche suivant, jusqu'à trois lieux plus tard.

**Commentaires qui décrivaient des mécaniques inexistantes** : `Scene.tags` annonçait que `rough_path`/`climb`/`chase` sont lus par BOITEUX — **aucun code ne les lit** (il n'agit que par le tag de CHOIX `fuite`), donc écrire du contenu en comptant dessus ne produirait rien ; `dejavu.ts` s'annonce « à trois portées » et la portée `ecran` est **entièrement inerte** (aucun appelant).

**Recensement exhaustif des sources de Jour** (demandé à l'agent) : deux seulement — la marche (conditionnée à l'engagement, donc inaccessible au joueur passif ✓) et **la nuit** (accessible sans risque : jusqu'à 3 Jours par vie, contre ~6 pour un joueur engagé). Ce n'est pas un Jour-sanction et c'est conforme à la règle écrite du 10/08 (« l'aube vient qu'on ait dormi ou veillé ») — **mais c'est la seule source non-risquée qui reste, à trancher si on veut la fermer.**

Sept gardes verts, lint et typecheck propres. `APP_VERSION` **1.69.6**, `CACHE_VERSION` **v114**.

#### Repasse image↔texte : les reliquats et la Chapelle retournée (v1.70.0)
Le 3e agent a rouvert 20 PNG et confirmé les 9 réécritures — mais a trouvé que **plusieurs ne portaient que sur la narration**, laissant les issues, les points d'intérêt et les copies de production dire l'inverse. Corrigé :
- **Guetteur** : l'issue funeste disait encore « Il **se lève** lentement » alors qu'il est debout ; et son point d'intérêt décrivait « un tas **haut comme un homme et régulier sur toute sa longueur** », ce que la narration recalée ET l'image contredisaient (les pierres lui arrivent aux chevilles).
- **Époux** : la bêche survivait dans trois textes après que les deux se sont redressés — une femme qui l'essuie alors qu'elle est à l'homme, un verrou EMPATHIE bâti sur des « coups de bêche » en cours, et le plan large du Verger qui annonçait « deux silhouettes **qui bêchent** ».
- **Meute** : le Geôlier annonçait encore « **Six** bêtes qui tournent en rond » (nombre faux + cercle visible affirmé) ; et l'ouverture posait « **loin de tout muret** » alors qu'un choix s'appelle « Gagner le muret ». C'est le hameau qui manque à l'horizon, pas la pierre.
- **« sec comme un piquet » avait été corrigé au mauvais endroit** : la phrase juste est au muret (le vieux), la fausse était au barrage — et c'est là que l'image montre le plus massif des trois.
- **Pendu qui parle** : « un gibet **bas, à hauteur d'homme** » contre une potence de trois hauteurs d'homme. C'est le CORPS qui est à hauteur de regard : deux mots, et le libellé du choix suit.
- **LA CHAPELLE EST RETOURNÉE** : le texte disait « au fond, le mur des cordes ; sur le côté, l'autel couché » — l'image dit exactement l'inverse (cordes sur les deux parois latérales, autel DEBOUT au fond sous la fenêtre). J'ai ouvert aussi le plan rapproché de l'autel : il est debout, sa dalle de base **tirée en avant**, avec le creux dessous. Le point d'intérêt (« la masse de pierre couchée sur le flanc ») a donc été récrit lui aussi — la mécanique de la cachette vidée est intacte, c'est la posture qui était fausse.
- **Copies de production** : 6 divergences dans `landes.json`, 3 dans `scene-meta.json` (qui alimente les prompts Leonardo — un prompt écrit dessus aurait regénéré le défaut).
- ⚠️ **Le garde des strates a tiré sur ma propre réécriture** — et c'était un faux ami : « ce n'**est** pas à toi qu'il le dit » comptait comme une négation de « est », que la scène affirmait. Les **copules et auxiliaires** (être, avoir, faire, aller) entrent dans la stoplist : le garde ne vaut que sur des verbes d'ACTION. Reprouvé ensuite sur un vrai défaut (une strate qui décroche les cordes que la scène fait pendre).
- **22 images marquées « à remplacer »** sur la page de couverture (12 nouvelles). Les plus lourdes : le 2e écran de la Colline montre encore le **cercle abandonné** avec une figure centrale que nul texte ne nomme · **le Moulin a ses ailes sur le PLAN RAPPROCHÉ aussi** (jamais relevé — l'examen décrit les bandes pâles « protégées par les ailes », l'image montre les ailes) · le **Marché muet est vide d'humains** alors que trois choix reposent sur des marchands qui négocient par gestes · le **Verger n'a aucun rang** alors qu'un point d'intérêt s'appelle « compter les rangs » · la **Borne n'a pas son homme immobile**, qui est le premier élément jouable nommé du jeu.
- `APP_VERSION` **1.70.0**, `CACHE_VERSION` **v115**.

### Session 2026-08-12 (suite, Claude Code, opus) — chantier feedback+fluidité, correctifs 4 et 5 (v1.79.0)
Suite des correctifs 1-3 (v1.78.0, déployés — Pages `01cbca7` en success). Les deux derniers de la liste que j'avais proposée à Patrick avant de coder.

**Correctif 4 — L'OBJET AGIT SUR PLACE (§2 du chantier).** « L'objet doit d'abord modifier la scène actuelle, PUIS ouvrir ou transformer une possibilité. » Jusqu'ici, sortir un objet appliquait son effet et **enchaînait l'écran suivant** : le joueur ne voyait donc jamais ce que son objet venait de changer — la « récompense invisible » du §9.
- **La branche `useItem` passe TOUJOURS par `resterSurPlace`**, quelle que soit la scène (avant : seulement sur un séjour). La conséquence s'écrit, l'écran se recompose, et les options que l'objet ouvre apparaissent dedans. Un objet ne fait plus passer le temps : il transforme l'endroit où on est.
- **Trois champs, portée ÉCRAN** : `Scene.usageObjet {objet, label, consequence, cle}` déclare l'objet qui sert ICI ; `Choice.requiresUsage` ouvre, `Choice.masqueSiUsage` ferme. Le jeton passe par **`choixFaits`**, vidé en quittant le lieu — donc aucune nouvelle donnée persistée, et une corde amarrée ici n'ouvre rien ailleurs. C'est la règle, pas une limite : l'objet transforme la scène, pas la partie.
- **Objet pilote n°1, l'OUTIL — la Corde coupée.** Le premier objet des Landes qui ne soigne rien et ne pèse sur aucun jet : il ouvre un endroit. Elle se **gagne** enfin à la Chapelle (le choix « Prendre la corde coupée » disait depuis toujours qu'elle s'enroule autour du poignet, et on repartait les mains vides — `grantsLoot` ajouté), et elle s'**amarre** à la margelle du Puits Condamné, ce qui fait apparaître **« Descendre par la corde »** (COURAGE 13, highStakes, quatre issues qui racontent le fond : la place du hameau à l'envers, le plancher de chaussures rangées par tailles, la main qui tient la corde comme on tient une porte).
  - ⚠️ **Le budget de 3 CTA tient sans exception** : `masqueSiUsage` retire les deux options d'observation (une fois la corde tendue par le fond, coller l'oreille aux planches n'est plus la question). Mesuré : **2 CTA après usage**.
  - ⚠️ Pas de `sortie` sur la descente : les quatre issues ramènent le héros à la margelle, donc l'écran suivant du lieu enchaîne juste. Une sortie l'aurait sauté, et le Puits serait le seul lieu dont on peut voir le fond sans jamais voir ce qui en sort.
  - **Icône** : `objet_corde_coupee_fille_a.png` (regardée — un rouleau de chanvre épais) servait à la **Mèche Nouée**, une mèche de cheveux. Le fichier a été NOMMÉ pour cette corde-ci. Il est donc partagé en attendant que la mèche ait la sienne.
- **Objet pilote n°2, le SOIN — le baume.** Il ne saute plus l'écran : la ligne dit ce qui se referme **et ce que ça change pour la suite** (« Ce que tu vas tenter maintenant, tu le tenteras avec ce corps-là »), sans un chiffre — l'Anneau du prochain jet montre de lui-même la différence, puisque le malus d'ENTAILLÉ vient de tomber. C'est le volet « transformer une possibilité » du §2.
- **Nouveau garde de build `A-usage`** (dans `acceptation.py`) : une scène qui déclare un `usageObjet` doit avoir un choix du **même écran** qui lit sa clé, l'objet doit exister dans `LANDES_OBJETS`, et un `requiresUsage`/`masqueSiUsage` sans `usageObjet.cle` correspondant est signalé. **Prouvé sur ses trois modes d'échec avant d'être branché.**

**Correctif 5 — LES HUIT EFFETS ORPHELINS (§5+§9).** Huit actions posaient un drapeau que personne ne lisait. Toutes avaient un feedback écrit (aucune n'était muette) : c'est le drapeau qui était mort. **Trois lecteurs, cinq coupes.**
- **`d.bailli_condamne` → une défense au procès.** La confession du Pendu qui parle (« j'ai signé trois cents noms, le trois cent unième était le sien, alors j'ai inscrit le mien en dessous ») dormait depuis le 6/08. C'est ici qu'elle vaut : le hameau juge par l'Ordonnance de la Fixation, et l'homme qui l'a écrite s'est condamné pour avoir refusé de l'appliquer. Nouveau choix **« Invoquer le trois cent unième »** (COURAGE 12, highStakes), gaté par `requiresDecouverte` — pas par `defense:`, parce que cette défense-là ne vient pas des témoins mais de ce qu'on est allé chercher, peut-être dans une autre vie. C'est le §8 en acte (« le jeu a retenu ça »).
- **`d.temoin_nomme` → « Ne rien lui donner »** au toit de la Grange. La Fille du Moulin est la seule à nommer ce qui est là-haut (« celle qui regarde — elle attend que vous le fassiez »). Qui l'a entendue peut donc refuser de faire un geste : la seule chose que la chose n'attendait pas.
- **`d.combles_cloues` → « Compter les combles clouées »** à l'aube. ⚠️ **PROMESSE TENUE, ET C'ÉTAIT MA FAUTE** : le 11/08 j'ai posé ce drapeau en écrivant dans le code qu'il « reçoit enfin un consommateur, à la Grange ». Il n'en a jamais eu. C'est exactement le défaut que le §12 interdit — et je l'ai commis en le documentant.
- **Cinq coupes** (`savoir_rangs`, `savoir_mur_ecrit`, `d.sonneur`, `d.nuit_du_hameau`, `d.plus_de_defenses`) : la prose reste intacte, seul le drapeau part. Garder des drapeaux morts « pour le Codex plus tard » est précisément ce qui a rendu cet audit nécessaire ; re-poser une ligne `decouverte:` quand le Codex existera coûte une ligne.
- Après coup : **0 action sans feedback, 0 effet sans consommateur** (`audit_fluidite.py`).

⚠️ **Deux pièges de test, tous deux du SETUP :**
- `page.$("button:has-text('REPRENDRE')")` juste après `waitUntil: "domcontentloaded"` rend **null** (React n'a pas rendu l'accueil) : on reste sur l'accueil et `.choices-bar` n'apparaît jamais. Il faut `waitForSelector`.
- Les découvertes ne sont **pas** un tableau `PlayerMemory.decouvertes` : elles vivent dans **`PlayerMemory.faits`** avec `kind: "discovery"`. Seeder le mauvais champ a fait passer les trois lecteurs — corrects — pour cassés.
- Rappel : `page.waitForFunction(fn, options)` passe les options en **argument** ; la signature est `(fn, arg, options)`.

⚠️ **PIÈGE DE DÉPLOIEMENT TROUVÉ CE JOUR — un déploiement qui paraît réussir sans rien livrer.** `git worktree add /tmp/ghpN origin/gh-pages` crée un **HEAD DÉTACHÉ**. Un `git push -u origin gh-pages` depuis ce worktree ne pousse alors PAS le commit qu'on vient de faire : il pousse la **branche locale `gh-pages`**, restée sur le déploiement précédent, et répond **« Everything up-to-date »**. Le rituel semble s'être déroulé normalement, et rien n'est en ligne. Vérifié : `git rev-parse HEAD origin/gh-pages` rendait deux sha différents après un push « réussi ».
**Toujours pousser explicitement `git push origin HEAD:refs/heads/gh-pages`**, puis re-vérifier que les deux sha sont égaux avant de retirer le worktree.
- Déployé : gh-pages `fd7c855` (jeu + couverture.html + atelier.html + studio.html + studio-data.json). `APP_VERSION` **1.79.0**, `CACHE_VERSION` **pactum-v125**.
